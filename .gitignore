# dependencies and service files
node_modules/

# the tool's own artifacts inside other projects (and in this one, if the need arises)
.size-report/
*.log
.DS_Store

# machine reports of the sensors (`tools/gates/*`): a person reads them, history does not keep them
reports/

# the client's working directory the work happens in (not the repository's contents)
.freebuff/
