# Russian strings out — meta plan (tracker)

2026-09-16. Subplans: one per subsystem, listed in the map below. **This file tracks them:** a
subplan appears here as *written*, a step as *done* or *dropped*, and nothing is marked done
without a measurement. One subplan is planned and one chunk of work is done per portion;
progress is journalled in `worklog/NNNN-*.md`, one file per portion (`worklog/0134-*.md` opened
this work).

Predecessor: `docs/plans/2026-09-15-docs-rework/` put the **prose** (comments, markdown) into
English and named user-facing strings out of scope. This work is exactly that leftover: the
strings. Its own definition of done listed them — "user-facing strings, locale dictionaries,
sensor verdicts, test assertion messages" — and its open question "the report's own language"
is answered here.

## Goal

Every **Russian string constant and literal** in the repository becomes English: what the tool
prints to a user, what a sensor prints to a developer, what a check says when it fails, what the
templates and the workflows say in their comments, and what the tools write into generated files.

Two readers, and each is a reason of its own. The tool is a published package: a refusal, the
help, a diagnostic answer and a commit subject the hook writes into someone else's history are
read by people who do not read Russian. The repository is worked on by agents: an assertion
message is the only description of a failure a machine gets, and a sensor's verdict is the whole
report.

## What stays Russian (the allow-list, not an oversight)

| Layer | Why |
|---|---|
| `worklog/**` | The journal is the record of the request and of the work; it is read in the language of the request and is not a product. |
| The `ru` sides of the dictionaries in `src/locales.js` and `src/metrics.js` | A locale is **data**: it is the Russian interface of the report, and deleting it deletes a feature. Its counterpart `en` is the target of every other string. `src/metrics.js` holds a dictionary of its own (the metric notes, methods and the degradation wordings) picked by `cfg.locale`, measured while planning S3. This is where the panel's service words ended up on 2026-09-16 by N25's decision — four keys instead of four literals in `src/page/panel.js`, so the allowance grew by four lines while the runtime file reached zero Cyrillic. |
| The Russian comment at `src/metrics.js:61-63` | Prose rather than a literal: a comment, left by the prose pass, out of this work's scope by definition. Recorded in `TODO.md` so that the next prose pass finds it (`measurement.md`, S3). |
| `fixtures/**` | Frozen references: `pnpm run check:standards` re-takes them and compares byte for byte, and `test/frozen.test.js` holds their hashes. Inventories: the consumer's `history.bundle` (2 270 lines of the tool's own output, frozen in September), `fixtures/parity/data.json` (214), `fixtures/*/config.json` (`title`, `heading`, column labels — the sample data being measured), `fixtures/*/golden.json` (commit subjects of the synthetic history) and the fixtures' `README.md` (written by the generator, compared byte for byte). Touching any of them means re-taking both references — and that price is not paid: **`BLOCKERS.md` N21 was decided 2026-09-16, the references stay Russian.** |
| The builders that write the frozen layer | `tools/synthetic/note.js` 37, `content.js` 31, `history.js` 20 and the instrument lines that write into a reference (`tools/parity-freeze.js:98-118` 16, `tools/make-fixture.js:56,57,64` 3) — **107 lines**. The reference's text is the reference's data: a translated builder changes the bytes `pnpm run check:standards` compares, so this row is part of N21's decision rather than a second one. |
| ~~The `ru` interface's own values in the draft~~ (left the allow-list 2026-09-17 by N19, which made the default locale `en`: the draft follows the default and carries the `en` heading), and a term (`eslint.metrics.config.js:30` 1) | 1 line. The row was two values of the settings draft plus one term. The draft's `title`/`heading` were **the same string** as `src/locales.js:8`'s `heading` (measured), so they were the locale's words placed in a file a person copies: translating them then would have given a `locale: "ru"` project a page titled in English while the rest of that page stayed Russian (the mixing N25 records) — **that is why they were left, and why the answer came from the other side:** N19's decision of 2026-09-17 made the default locale `en`, the draft followed the default, and both values left the allow-list with it. `отложено` is a **term the linter matches** rather than a message — translating it stops the sensor seeing the marker (behaviour, not wording). Named by D1 (`configs.md`). |
| `SKIP_WORDS` (`src/history.js:18`) | 1 line, part of the frozen `--json`: **N24 was decided 2026-09-16 — the three words stay Russian** (`BLOCKERS.md`), on the ground that the references stay Russian and that editing `src/history.js` would change behaviour rather than a literal. The decision is reversible and its price is recorded there. |
| Russian sample data (`docs/заметки.md`, subjects, file names, labels) | It is what the measurement is measured **on**: the traps of the synthetic fixture exist to prove that a non-ASCII path, a quoted subject and an HTML-escaped signature survive. |
| Quotes of old output in records (`BLOCKERS.md`, the archived plans, `worklog/`) | A record of what a reader saw then is true of then; `BLOCKERS.md` states the rule in its own header. |
| `docs/plans/2026-09-16-i18n-english/**` | A subplan quotes the strings it translates and carries the counter's own pattern; without the quotation a reader cannot see what is meant, and, with the language guard withdrawn (below), this folder simply joins the allowance. |

## Current state — measured 2026-09-16

The instrument (PCRE2, so the pattern carries no Cyrillic and cannot match itself):

```bash
git ls-files | rg -v '^worklog/' | while read -r f; do
  n=$(rg -cP '[\p{Cyrillic}]' "$f" 2>/dev/null); [ -n "$n" ] && printf '%6d %s\n' "$n" "$f"
done | sort -rn
```

Whole tree: **5 385 lines in 110 files**. Of them the frozen layer (`fixtures/` 2 602 lines in 9
files), the built report (`docs/size-report.html` 349 lines — the tool's own `ru` output, rebuilt
by the hook) and the archived plans (15 lines, `plans/archive/REFACTOR.md`). What this work has to
carry is **2 396 lines** in code, checks, tools and configs:

| Subsystem | Files | Lines | Where the Russian is |
|---|---|---|---|
| `src/**` | 24 | 351 | the help and refusal texts, diagnostics, the hook's commit subject, metric captions |
| `bin/**` | 2 | 2 | the post-install note |
| `tools/**` (instruments) | 12 | 387 | every instrument's console output: the sensor runs, the reference takings, the harness, the pack check |
| `tools/gates/**` (sensors) | 6 | 77 | each sensor's verdict, its hints and the profile's step names |
| `tools/synthetic/**` (fixture builders) | 4 | 89 | the generated fixture's note and content — **feeds the frozen layer** |
| `test/**` | 37 | 1 320 | test names and assertion messages, one per check |
| `templates/**` | 3 | 35 | the comments of the settings draft, the CI template, the note |
| `.github/workflows/**` | 3 | 78 | the comments of the three workflows |
| `package.json` | 1 | 1 | `description` — what the registry shows |
| `AGENTS.md`, `BLOCKERS.md`, `README.md` | 3 | 21 | quotations of printed Russian, allowed by the allow-list (re-measured 2026-09-16 after S1's step 4: **21 — `BLOCKERS.md` 18, `README.md` 3, `AGENTS.md` 0**; the row said 10, then 18 with `README.md` 4). **Measured while planning C3 and confirmed by doing it: `README.md`'s four were not all quotations** — the code-2 row is parsed by `readmeCauses()` and compared with `CONFIG_CAUSES`, so it is a contract, and S1's step 3 moved it in English (one line less, and the cell fell from 18 to 22 only because `BLOCKERS.md` grew by five quotation lines in the decisions N21–N27). The other three move with S4's `!` note and S5's hook subject. **Re-measured 2026-09-17 at C3's step 0: `AGENTS.md` 0, `README.md` 0, `BLOCKERS.md` 33 — the row reads 33, and `README.md` is empty of Cyrillic altogether** (S1's causes, S4's `!` note and S5's subject all moved it); the two later readings of the cell (18, then 22) were taken before those steps and before the journal of this campaign grew |
| `docs/size-report.html` | 1 | 349 | generated; turns English with the source of its texts, not by hand |

Of these 2 396 lines only **10 are comments** (`src/` 3, `tools/` 6, `test/` 1 — leftovers of the
prose pass); the rest are string literals, test names and YAML/markdown comments. The measurement
that separates them:

```bash
rg -c '^\s*(//|\*|/\*).*\p{Cyrillic}' <file>
```

## The map: one subplan per subsystem

`T0` is this file. `S*` — the published runtime, `W*` — the repository's instruments, `C*` — the
checks, `D*` — the declarative files. The fifth letter, `G*` — the guard that was to hold the
allowance — is withdrawn (below), so the map closes with `D1`. Sums are the measured lines above.

| # | Subplan | Owns | Lines | Status |
|---|---|---|---|---|
| T0 | `plan.md` | this tracker: the map, the allow-list, the criterion, the order | — | **written 2026-09-16** |
| S1 | `surface.md` | `src/refusal.js` (46), `src/args.js` (26), `src/cli.js` (4), `src/modes.js` (13) — the help, the cause registry, the CLI refusals and what the modes print; `src/locales.js` (45) is named there as staying Russian (**default locale** = `BLOCKERS.md` N19, not decided) | 89 | **done 2026-09-16** — steps 1–4 landed (`c0df1fb` the help, `cb64f12` the grammar's messages, `a7c869b` the cause registry, `230640e` what the modes print; the four files read 0 Cyrillic). **The wash-up is deferred, not dropped** (`BLOCKERS.md` N28): its counter half holds, while narrowing `ADVICE_LINE` waits for S2, S3, S4 and S5 and belongs to W1's step 8 |
| S2 | `settings.md` | `src/config.js` (35), `src/project.js` (4), `src/init.js` (18) — what a settings file says when it is wrong, and what `--init` prints; follows S1's cause names and advice markers, and owns `outsideFix`, which S4 prints | 57 | **done 2026-09-16** — all four steps landed and the wash-up met: `src/config.js` 34 → 0 (`ea6b743`, six catalogue cases), `src/init.js` 18 → 0 with `tools/refusals.js` 78 → 77 (the `config already exists` case moved with the text), `src/project.js` 4 → 0 with its three readers, and step 4 recorded **empty** by measurement (the cause arguments were already English after S1's step 3). The counter over the three files answers nothing; `ADVICE_LINE`'s tolerance stays for S3–S5 (**N28**) |
| S3 | `measurement.md` | `src/metrics.js` (20), `src/minify.js` (4), `src/strip.js` (1), `src/strip/guard.js` (6), `src/parse.js` (1), `src/optional.js` (1) — the sensor note, the two refusals of the measurement, the internal errors; **18 of the 33 lines are in scope**, 12 are the `ru` dictionary of `src/metrics.js` and 1 is a comment (`TODO.md`) | 18 | **done 2026-09-16** — three commits and the wash-up met: step 1 `e9a1c03` (`src/metrics.js` 20 → 16, `src/optional.js` 1 → 0), step 2 `f4bc3a2` (`src/minify.js` 4 → 1, `src/strip/guard.js` 6 → 1, `src/parse.js` 1 → 0), step 3 (`src/metrics.js` 16 → 15, the other three to 0). The counter answers `src/metrics.js` **15** (the 12 `ru` dictionary lines and the 3-line comment) and nothing for the other five. Two expectations of the plan were corrected by measurement: the wash-up reading is 15 rather than 13, and the guard's internal error **does** have a reader (`test/module.test.js` 3), with two negative matches beside it |
| S4 | `diagnostics.md` | `src/git.js` (3), `src/history.js` (12), `src/check.js` (12), `src/doctor.js` (33), `src/explain.js` (24) — `check`, `explain`, `doctor`, the git boundary and the skip reasons. **`SKIP_WORDS` (`src/history.js:18`) is the only frozen literal of the runtime** — and **N24 was decided 2026-09-16: it stays Russian** (`BLOCKERS.md`), so its gated step becomes a named exception and the three words join the allow-list; everything else lands green without touching a reference | 84 (83 free + 1 allowed) | **in progress 2026-09-16** — step 1 landed 2026-09-16 (`src/git.js` 3 → 0, `src/history.js` 12 → 1 with `SKIP_WORDS` alone, the catalogue's `EXIT.SHALLOW` and both `EXIT.VIOLATION` cases, `test/disk.test.js` 68 → 58 and `test/cli-paths.test.js`'s two machine markers, plus the quotations in `README.md` 3 → 2 and `BLOCKERS.md` 21 → 20; allowed quotations in `README.md`/`BLOCKERS.md` 21 → 19): the git boundary and the disk comparisons are English, their counters hold. **Step 2 landed 2026-09-16 too** (`src/check.js` 12 → 0, `src/explain.js` 24 → 0, `tools/refusals.js` 66 → 61, `test/check.test.js` 83 → 78; `test/doctor.test.js` 82 → 82), and it found a negative match (`test/check.test.js:235`) that had gone **empty** at S1's step 3 — the C2 rule re-pointed it at the sibling answer's phrase. **Step 3 landed 2026-09-16 as well** (`src/doctor.js` 33 → 0, `test/hook.test.js` 113 → 111, `test/doctor.test.js` 82 → 82), and so did the **wash-up**: the counter over the five files answers `src/history.js:1` alone — `SKIP_WORDS`, N24's named exception — so **S4 is closed** (step 4 was dropped by that decision). The step also measured itself: with doctor.js put back to Russian except the four literals that have readers, the whole profile stayed green; and S5 is now the only module of `src/**` printing a Russian advice marker (`src/hook.js:156,164,171,247`), which leaves N28's condition one subplan away |
| S5 | `automation.md` | `src/hook.js` (40), `src/page/panel.js` (3), `bin/postinstall.js` (2) — the installed hook's text and its commit subject, the panel's service words. The panel's four literals are **not localized by translation** (`BLOCKERS.md` N25, decided 2026-09-16): they were **moved into the locale dictionaries**, so a report whose `locale` is `ru` keeps Russian chrome and this project's own report is such a report | 45 | **steps 1–5 and the post-install half of step 6 done 2026-09-16, and the panel's four literals moved into the locale dictionaries the same day — **S5 closed**, its step 6 changed in shape rather than taken as a translation.** `src/hook.js` 40 → 0, `bin/postinstall.js` 2 → 0, `tools/refusals.js` 61 → 55, `test/hook.test.js` 111 → 109, and `README.md` 2 → **0** (its last Russian lines were the commit subject). Readers moved: `test/hook.test.js:151,191,254,323`, the four catalogue cases, the README quotation; the fixture layer was measured clean of any hook word, and the commit subject moved nothing in it (`check:standards`, `parity:live`, `pack:check` green, artifact byte-identical). **N28's condition now holds for `src/**`** — the only Russian advice markers left are W2's two sensors, which the extractor never reads |
| W1 | `tools.md` | the 11 instruments: `tools/refusals.js` (169), `parity-live.js` (35), `parity-freeze.js` (31), `run-tests.js` (25), `pack-check.js` (21), `check-standards.js` (21), `make-fixture.js` (17), `harness.js` (13), `yaml.js` (10), `docs-facts.js` (4), `page-harness.js` (3) — **no hole in this owner**: every file under `tools/**` belongs to W1 or W2, and `gate-probe.js`/`gates/common.js` carry no Cyrillic (measured). It also inherited the fixture builders on 2026-09-16 (below): `tools/synthetic/repo.js:74`, one internal error written into no file, while the folder's other 88 lines are allow-listed by N21. The catalogue's `must` phrases and cause names are **not** W1's (the owner of the printed text); `manifestNote` and the fixture config values stay Russian — **19 lines**, allow-listed by N21's decision, so 330 are this owner's own (measured); the numbers' decimal comma is `BLOCKERS.md` N26 | 331 planned, **235 measured** (23 now) | **steps 0–1 done 2026-09-16**: the baseline is re-measured in `tools.md` — 235, not 331, and the whole drop is S1–S5's (`tools/refusals.js` 169 → 55 by measurement); of the 235, the final composition is **23 left and 212 translated**, and each of the 23 is named by file rather than by subtraction: `parity-freeze.js` 16 + `make-fixture.js` 3 (N21, permanent), `docs-facts.js` 3 (the two section names of the pinned revision, which N20's release moves, and the comment naming them) and `run-tests.js` 1 (the example naming a Russian test). `tools/harness.js` reads **0**: its 13 lines first fell to 3, blocked by `BLOCKERS.md` **N29** — where a literal met the `dup` baseline: `firstDiff` here and the copy in `tools/parity-live.js:74` were an accepted clone, a translated string is a different token, so rewording made the twin new (measured three ways) — and the repair of 2026-09-16 took the copy out and the three literals with it. No machine reads a word of that file (proved red-first by the **full** profile staying green on the Russian file: 175 checks). **Step 2 done 2026-09-16**: `tools/yaml.js` 10 → 0 and `tools/page-harness.js` 3 → 0, neither read by any check (proved red-first by the full profile staying green on the Russian files) nor by the near duplicates at `test/parity.test.js:32,41` and `tools/make-fixture.js:127,150`, and `pnpm run dup` asked its own question here and answered no new twin. **Step 3 done 2026-09-16**: `tools/run-tests.js` 25 → **2** (the comments of the prose pass), `MODES`' keys kept as the protocol while its values moved, no check reads a word of it (the full profile stays green on the Russian file, and `tools/gates/coverage.js:63` runs it for the exit code alone) — and the counter's own measurement found **N30**: `plural` encodes Russian grammar, so English prints `21 check`, and no choice of the three words is right for both 1 and 21. **Step 4 done 2026-09-16**: `tools/check-standards.js` 21 → 0, `tools/pack-check.js` 21 → 0, `tools/parity-live.js` 35 → **3** (only N29's pair, nothing else touched); their interface is the exit code — all three returned to Russian together leave the **full 8-step `verify`** green — and neither `fixtures/parity/artifact.sha256` (`1bdb27e1…`) nor the built page (`40eda37d…`) moved, so no translated word sits inside a compared artifact. **Steps 5 and 7 done 2026-09-16**: step 5 closed by measurement with **no edit** — `tools/docs-facts.js` already carries both spellings, the pin `v2.4.0` really holds Russian section names (`git show v2.4.0:src/refusal.js:99,111`), taking the Russian half out reddens `test/docs-pin.test.js` with the sentence the plan predicted, and none of the four instruments imports the file, so a translated name would touch the standards sensor not at all; step 7 took `tools/synthetic/repo.js:74` (1 → 0), demonstrated live by putting a clean merge in front of the fixture builder, and proven to reach no frozen byte (`fixtures/**` names the message nowhere, the artifact hash stands, the built page is byte-identical). **Step 6 done 2026-09-16**: `tools/parity-freeze.js` 15 → 0 and `tools/make-fixture.js` 14 → 0 of this owner's own, while the 19 allow-listed lines stand word for word — and the reason is measured rather than assumed: both builders re-take the frozen layer (`README.md` from `manifestNote(ctx)`, `config.json` from `CONFIG`), the synthetic re-take is byte-identical to `fixtures/synthetic` file by file, and the parity re-take differs from `fixtures/parity` only in the two documented fields of the source path (including one interpolated word inside the Russian `manifestNote`). **Step 8 done 2026-09-16 and W1 closed with it**: `tools/refusals.js` 55 → **0** (49 `truth`/`why` lines, the two case `id`s, the one `uncatchable` reason's two lines, and the stale `template` comment, which now names `<file>`/`<commit>` as the advice prints them); **`ADVICE_LINE` is narrowed** and `BLOCKERS.md` **N28 is closed** with it — the plan's red experiment ran twice: green on the shallow case (two advice lines, and the extractor asks about texts rather than markers — a finding, not a failure) and red on `config already exists` (one line) with the promised sentence; the extractor's only call site is `test/refusals.test.js:245` and the only Russian advice markers left are W2's two sensors, so nobody else needed the tolerance. `tools/refusals.js` is **not** a gate file, so the commit carries no trailer (the hook printed “гейт-файлов 0”). **N29 and N30 closed the same day by the mission agent, in one commit:** the copy of `firstDiff` went out of `tools/parity-live.js` in favour of the shared one (measured after the repair: `dup` reads **8 clones, 47 lines** instead of 10 and 62, and the tool that prints diffs stays green), and `plural` now returns the singular only for exactly one (measured: `1 check`, `21 checks`, `101 checks`; the runner's own line reads `✓ fast run: 70 checks`). Seven of the twelve files' remaining lines went with the two repairs. **N26 closed the same day** — the four call sites of two files format their numbers through `localeNumber` (`tools/harness.js`) in the machine's locale, so no number is Russian by accident and `tools/gates/run.js` carries the `Gate-Change:` trailer for it |
| W2 | `sensors.md` | `tools/gates/**` (77: `run.js` 21, `dup.js` 20, `coverage.js` 18, `gatefiles.js` 7, `metrics.js` 6, `deps.js` 5, `common.js` 0), the `why` of every entry in `tools/suites.js` (38), `.githooks/**` (4) and the two baselines' `note` the sensors write (2) — **121 holds by measurement, and every commit of this owner is a gate-file commit: `Gate-Change:` trailer required**. Seven verdicts are read word for word by the sensors' own probes (`test/gates-*.test.js`), so each moves with its assertion | 121 | **step 0 done and step 1 with it, 2026-09-16**: the baseline re-measured to the line (121, file by file, none of it moved by W1's decisions) and `tools/gates/run.js` took its whole 21 — the eleven step labels, the `:hermetic` mark on a step's name, the unknown-profile refusal and the profile's own words (summary header, `s`, `steps`, red-steps verdict, the advice, the green verdict). No machine reads a word of that file (measured: `--list` prints commands, the guard parses those command lines and the hooks' **call** lines, a step's output is not captured), and the commands are unchanged — `--list slow` still prints the same ten lines, which is what CI is compared with. **Step 2 done 2026-09-16**: `tools/gates/deps.js` 5 → 0 and `tools/gates/metrics.js` 6 → 0, each verdict with its reader in the same commit (`test/gates-deps.test.js:48` → `/no findings/`, `test/gates-metrics.test.js:144` → `/new violations 1/`), and the red-first experiment run literal by literal: only those two verdicts redden their probe, the other eight lines leave it green (the measurement of "no reader"), and neither probe has a negative match over this text. **Step 3 done 2026-09-16**: `tools/gates/dup.js` 20 → 0 and the `note` inside `dup-baseline.json` with it (1 → 0), the two probe reads moved (`:72` → `/new clones 1/`, `:88` → `/no baseline/`), and the red-first experiment per printed message named exactly those two readers while the other twelve messages left the probe green; **the script's re-take of the baseline was measured and refused** — it would prune seven stale fingerprints (15 in the file against 8 in the tree), so the note was moved by hand with the fingerprints byte-identical and the text proved equal to the script's literals (**N31**, the pruning left to the user). The translation moved no finding: `clones 8, lines 47`, 15 fingerprints, two looks. **Step 4 done 2026-09-16**: `tools/gates/coverage.js` 18 → 0 with `coverage-baseline.json`'s `note` (1 → 0); the two probe reads moved (`:49` → `/… was 80, now 50/`, `:63` → `/… was not in the baseline, now 0/`), and the red-first experiment named exactly **one** reader — the regression detail line — while the other twelve messages left the probe green. **The ratchet is red on the tree before this work** (`✗ cover: regressions 13 (the baseline holds 39 files)`, totals 80.6/89.05/92.37, and the same thirteen after the translation), and the scripted re-take was measured and refused: the same 39 keys, **20** metric values moved (13 falls in 13 files, 7 rises over 4 of them) — the decision is **N32** and stays with the user. **Step 5 done 2026-09-16**: `tools/gates/gatefiles.js` 7 → 0 with the three reads in `test/gates-files.test.js` (`:77`, `:115` → `/a gate edit with no Gate-Change: trailer/`, `:106` → `/the commit can be made/`); the red-first experiment per printed message named exactly those two readers while the other five messages left the probe green, and `GATE_FILES`, the `TRAILER` pattern and the twelve-character reason were not touched — this step reached no behaviour, since only words a person reads moved. Measured on its own commit: the hook printed `✓ gatefiles: gate files 2, the Gate-Change: trailer is there` and `✓ gatefiles: the commit can be made (5 files, gate files 2)`, and the range call answered in English. **Step 6 done 2026-09-16**: `tools/suites.js` 38 → 0 (the thirty-seven `why` texts and the unknown-run error), with every one of the 38 put back to Russian on its own leaving the probe **green** — the measurement of "no reader of the text" — while the two experiments that do redden it (a reason shorter than thirty characters, the field renamed) show what the guard really holds: presence, field name and size. The keys (`file`) and the profile names (`fast`/`full`) were not touched: the file lists are identical to `HEAD` (19 + 18), `--list` prints the same commands (`fast` 5, `full` 8, `slow` 10), and the runs keep 70 and 175 checks. **Step 7 done 2026-09-16 and W2 closed with it**: `.githooks/pre-commit` 2 → 0 and `.githooks/pre-push` 2 → 0 — all four messages put back to Russian on their own left the probe green (no reader of the wording), while the probe's real reading was measured the same way (the call line `exec pnpm run verify:fast` reddens it at `:142-143`) and both branches were walked by hand (a PATH without pnpm printed the first two messages, a temporary repository without `origin/main` printed the fourth). Both hooks still parse (`sh -n`); no order, condition or command moved. A finding, recorded in `TODO.md` and not chased: the sweep at `:146-149` cannot fail, so a wrong script name inside a message would pass. **The counter over the owner's twelve files answers 0** |
| C1 | `tests-page.md` | the checks of the page and the contract (355 lines at planning time): `page-view` 72, `page-choice` 67, `module` 66, `page-tree` 55, `contract-data` 51,`contract-derived` 18, `guard` 11,
`runner` 11, `api` 4. **The first owner whose lines are read by two other subplans at once**, and one of those reads is `assert.equal(/стриппер/.test(res.stderr), false, …)` — a *negative* match that stops checking, green, the moment S3's sentence changes; `test/module.test.js:163` is a reader of the internal error that S3's plan counted as unread (corrected in `measurement.md`) | 347 measured (was 355) | **step 0 and step 1 done 2026-09-16.** Step 0 re-measured the owner: the nine files answer **347**, and the drop is exactly `test/module.test.js` 66 → **58** — the eight joint lines of section (2) are English with their owners' steps, the negative matches at `:120`/`:195` now name `/stripper/`, and the mutation read at `:163` reads `/the stripper broke/`, so sections (2) and (3) are already in line and nothing there waits for C1; the check counts are unchanged (44 over the nine files). Step 1 took `test/runner.test.js` 11 → **5** and `test/guard.test.js` 11 → **1** — 19 names and messages, each put back to Russian on its own with the two files' probe staying green (no reader of a test's words); the six lines that stay are the fixture's data, and the sharpest of them was proved by experiment: an ASCII payload in `runner.test.js` reddens the check, because the split there is between the two bytes of a multi-byte letter. `dup` green (8 clones, 47 lines). **Step 2 done 2026-09-16**: `test/api.test.js` 4 → **0** and `test/contract-derived.test.js` 18 → **1** — twenty-two names and messages, each put back to Russian on its own with the probe green (no reader of a check's words). Both reds of this step proved the two kinds of data rather than the words: editing one name in the frozen list reddens `test/api.test.js`, and translating `['fixture: удаление файла']` reddens `test/contract-derived.test.js`, that string being the fixture's frozen commit subject (`fixtures/synthetic/golden.json:468`). **Steps 3 and 4 done 2026-09-16**: `test/contract-data.test.js` 51 → **1** and `test/page-tree.test.js` 55 → **1** — 104 names and messages, each put back to Russian on its own with the check green (no reader of a check's words), and the three data sides proved by experiment: the fixture's file label `'заметки.md'` reddens the check when translated, the dictionary read `/не измеряется: /` in `page-tree` reddens it when replaced by the English value, and translating `SKIP_WORDS`'s source reddens **both** `contract-data` and `parity` — N24's documented price, re-measured live — while changing the message at `:47` keeps them green. Both files keep one Cyrillic line each: the pin, not a stale expectation. **Steps 5 and 6 done 2026-09-16**: `test/page-choice.test.js` 67 → **1** and `test/page-view.test.js` 72 → **1** — 137 names and messages, each put back to Russian on its own with the check green, and the data sides named by measurement: the `ui.<key>` comparisons parse `ui` from the page’s own embedded `<script id="ui">`, so a dictionary change moves **both** sides and the check stays green (self-consistent — data in the strongest sense), the broken-record payload `'{ это не JSON'` stays green in ASCII (its Cyrillic is incidental), and the fixture’s label `'заметки.md'` reddens the check when translated (a pin). **Step 7 done 2026-09-16 and C1 closed with it**: `test/module.test.js` 58 → **7** (fifty-one names and messages; the seven that stay are the fixture's payloads — a JS sample's comment and greeting strings, the git identity and two commit subjects), each of the forty-four phrase pairs put back to Russian on its own with the probe green, and the diff over the file carrying only lines that held Cyrillic — the five joint lines and `:163` are untouched, measured rather than promised. Both negative matches were re-checked on their own paths: `:195` reddens when a temporary probe pushes the word `stripper` into the minifier's printed text (`src/minify.js:54`, restored byte-identical), and `:120`'s healthy path really carries text (**201 bytes** on stderr), so it is a bound rather than a vacuous line. `dup` green (8 clones, 47 lines), the artifact hash stands (`cdda8d01…`), the built page is byte-identical (66 427 B). **The counter over the nine files answers 18, and all 18 are data** (`runner` 5, `guard` 1, `contract-derived` 1, `contract-data` 1, `page-tree` 1, `page-choice` 1, `page-view` 1, `module` 7); checks 44 over the nine files, 70 and 175 in the runs — unchanged. **C1 is closed.** |
| C2 | `tests-cli.md` | the checks that drive the tool (739 lines): `hook` 113, `check` 84, `doctor` 83, `cli` 79, `minify` 75, `disk` 68, `tokens` 54, `release` 41, `templates` 33, `cli-paths` 31, `refusals-catalog` 26, `refusals` 25, `environment` 18, `crlf` 9 | 708 measured (was 739) | **step 0 and step 1 done 2026-09-16.** Step 0 re-measured the owner: **708**, and the whole drop belongs to the owners' earlier steps (`hook` 113 → 109, `check` 84 → 78, `doctor` 83 → 82, `cli` 79 → 74, `minify` 75 → 72, `disk` 68 → 58, `cli-paths` 31 → 30, `refusals-catalog` 26 → 25); the joint lines were checked **by line number** — **58 named lines, none carrying Cyrillic** — and all four negatives of the plan's section (2) are English and live (`check.test.js:184` → `/fix:/`, `:235` → `/is not a revision name and not the start of a sha/`, `disk.test.js:219` → `/carrying the state/`, `cli.test.js:69` → `/settings derived from the project/`), so no owner missed its own and nothing went to `TODO.md` or `BLOCKERS.md`. **Step 1 done 2026-09-16**: `test/refusals-catalog.test.js` 25 → **0** and `test/refusals.test.js` 25 → **4**, the four being the scenarios' own fixture content (`echo чужой` in a foreign hook, the note file, the side branch's commit subject, the comment appended to the drifted table). **Both halves of the step's experiment redden**, each measured on its own: a source's advice marker put back to Russian (`src/check.js:121`) reddens at `«the codes of the table / coverage is incomplete»: в отказе нет «fix: add these paths as a column or to "skip"»`, and one `must` phrase renamed in `tools/refusals.js` alone reddens at `«command line / unknown flag»: в отказе нет «a phrase that is not printed as such»`; both files restored byte-identical. The two files' own words are read by nobody: **39** phrases went back to Russian one at a time and neither file reddened once, and the catalogue's `coveredBy` entries point at `hook`/`module`/`disk`, never here. Checks unchanged (1 and 4; 70 fast, 175 full), `dup` green over 46 fresh messages, the artifact's digest `1bdb27e1…` and the built page 66 427 B unmoved. **Steps 2 and 3 done 2026-09-16**: `test/crlf.test.js` 9 → **0** and `test/environment.test.js` 18 → **1** (the one being the frozen fixture's commit subject — a **pin**, measured: translating it reddens the file), then `test/cli-paths.test.js` 30 → **3** and `test/cli.test.js` 74 → **2** (five pieces of the scenarios' own content, all measured green when translated). Red first: twenty-four phrases back to Russian one at a time in `crlf`/`environment` (all green), the two larger files put back to their `HEAD` version whole (both probes green — W1's method, chosen over 97 per-phrase runs), and one test name renamed leaving both `checksIn` (**9**) and the probe green, so the count is of `test(` calls rather than of names. The machine half was guarded too: `cli-paths.test.js:104`'s anchored marker reddens check 6 when the marker is put back to Russian in `src/config.js:69` (restored byte-identical), and `cli.test.js:69`'s negative match is not vacuous — the same phrase is asserted positively at `:45`. Found and named: the translation **dissolved** an accepted clone (`cli-paths.test.js:28-33` ↔ `doctor.test.js:176`; clones 8 → 7, lines 47 → 42), which step 6 can re-form. Checks unchanged (1/4/6/9; 70 fast, 175 full), `dup` green, the artifact digest `1bdb27e1…` and the file's own `cdda8d01…` unmoved, the built page 66 427 B. **The count of steps 2–3 is 125 lines turned English, not 116 as the first version of this row and of `tests-cli.md` said; the owner therefore read **537**, not 546 (corrected while doing steps 4–5).** **Steps 4 and 5 done 2026-09-16**: `test/templates.test.js` 33 → **0**, `test/release.test.js` 41 → **7**, `test/minify.test.js` 72 → **10**, `test/tokens.test.js` 54 → **5** — 176 lines of names and messages, with the leftovers named by measurement: seven workflow step names read as **exact strings** by `release.test.js` (D1's contract, and the measurement corrected the plan's six to seven — the missing one is `:86`) and the file's own payloads together with the **`ru` dictionary reads** (`minify.test.js:124,195`, `tokens.test.js:111,113`). **Measured, not assumed:** all four files put back to their `HEAD` version wholesale leave their probes green; the seven step-name reads are live (renaming a workflow step reddens `release` at check 2); and the dictionary reads are live too (translating the `ru` side of those phrases in `src/metrics.js:40,55` reddens `minify` at check 5 and `tokens` at check 3 — both files restored byte-identical). **N33 came out of this step and was decided the same day:** the translation made an accepted clone pair *new* for `dup` (`✗ dup: new clones 4` over `minify.test.js:252 ↔ tokens.test.js:158`), so the shared part was taken out rather than the baseline edited — one helper (`draftedRepo`) replaced the hand-rolled fresh-project setup of both files in a commit of its own (`897a780`), after which `dup` reads `clones 5, lines 29` against `clones 7, lines 42` before. Checks unchanged (3/3/9/7 there; 70 fast, 175 full), the artifact digest `1bdb27e1…` and the file's own `cdda8d01…` unmoved, the built page 66 427 B. The owner reads **359**. **Steps 6 and 7 done 2026-09-17**: `test/doctor.test.js` 82 → **10**, `test/check.test.js` 78 → **2**, `test/disk.test.js` 58 → **16** — 190 lines of names and messages turned English, and the leftovers are the scenarios' own fixture content (code comments and commit subjects, named file by file in `tests-cli.md`). Red first, each by the method its size asked for: the three files put back to their `HEAD` version wholesale left their probes green (`doctor` re-measured after this portion's extraction, so the evidence covers the file as it stands), and no regex literal with Cyrillic is left in any of the three (measured). **The negatives of the plan's section (2) were re-measured by mutation, not by argument, sources restored byte-identical:** with the empty-state comparison made to read as lost (`src/history.js`) the deleted-file scenario answers code 1 and `carrying the state` **is** in stderr while the clean tree answers code 0 with the phrase absent (`disk.test.js:219`; the regression reddens `:218` first, since the refusal precedes the negative); with that sentence merged into the sibling refusal (`src/explain.js`, the outside-history cause) `check.test.js:235` reddens with its own message `a commit that exists is called non-existent`; and with the renderer made to print a repair that names nothing (`src/explain.js`) `check.test.js:184` reddens with `the text carries a repair command without names`. **N34 came out of this step and was answered by N33's rule:** translating for `doctor` re-formed the clone pair step 3 had dissolved (`cli-paths.test.js:28` ↔ `doctor.test.js:176`, six lines of shallow-clone setup with the same two messages), so the shared part was taken out — `shallowClone` in `tools/harness.js` — rather than a word varied or the baseline edited; measured `clones 5, lines 29` before and after the extraction, `dup` green, baseline untouched. Checks unchanged (11/13/6 in the three files, 3 in `cli-paths`; 70 fast, 175 full), the artifact's content digest `1bdb27e1…` and the reference file's own sha256 `cdda8d01…` unmoved, the built page 66 427 B. The owner reads **169**; step 8 (`hook`, 109) is what was left. **Step 8 done 2026-09-17 and C2 is closed with it**: `test/hook.test.js` 109 → **27** — 82 names and messages plus the messages of the five lines that also carry a scenario subject. The 27 that stay are the scenario's own content, named in `tests-cli.md`: the sample project's files and its two commits, and the working copies' commits, edits and foreign hook bodies (with the regex that reads one of them, `/чужой хук/`, the file's only Cyrillic literal). **Red first by the whole file**: at its `HEAD` version the file ran **11 of 11 checks green**, so no machine reads a word of it (restored byte-identical). **S5's printed texts were checked read by read, not assumed** — `/already installed/` (`:151`), `/hooks.*enabled/` (`:152`), the exact report subject (`:191`), `/CI/` (`:295`), `/detached HEAD/` (`:323`), `/^✗ size-report: /` (`:339`), `/hook-run/` (`:404`), `/core\.hooksPath/` (`:412`) — all ASCII, and the tool's mark asserted at `:136` (`'size-report'`) is ASCII too, so none of them moved with the words. `dup` before and after: `clones 5, lines 29` both times (this file's shallow-clone block carries the identity `git config` lines, so its shape is not the harness pair's — no new clone, no helper needed). Checks unchanged (11; 70 fast, 175 full), the artifact's content digest `1bdb27e1…` and the reference file's own sha256 `cdda8d01…` unmoved, the built page 66 427 B. **The owner now reads 87, and every one of those lines is allow-listed data or D1's contract — C2 is done.** |
| C3 | `tests-guards.md` | the checks of the repository itself (226 lines, confirmed file by file): `gates-verify` 26, `gates-files` 26, `docs-commands` 23, `gates-metrics` 22, `suites` 16, `parity` 16, `frozen` 16, `gates-coverage` 16, `docs-pin` 14, `gates-dup` 13, `git-pins` 12, `docs-numbers` 12, `gates-deps` 8, `docs-paths` 6. **Six of them are gate files** (111 lines) and those commits carry the trailer; the eight assertion lines W2's steps move with their verdicts; **no negative match in this owner** (measured); one kept tolerance (`требовани|requirements?`, dead in the tree) named as an exception of the allow-list; and the finding that `README.md`'s code table is a contract parsed by `readmeCauses()`, not a quotation | 226 planned, **222 measured** | **step 0 and step 1 done 2026-09-17.** Step 0 re-measured the owner at **222**, not 226 — the drop is `docs-commands` 23 → 22, `parity` 16 → 15, `gates-coverage` 16 → 14 — and verified the lines other owners had moved: all nine reads of W2's verdicts (`gates-files` 77/106/115 → `/a gate edit with no Gate-Change: trailer/`, `/the commit can be made/`; `gates-dup` 72/88 → `/new clones 1/`, `/no baseline/`; `gates-coverage` 49/63 → `/src\/x\.js — lines: was 80, now 50/`, `was not in the baseline, now 0`; `gates-deps` 48 → `/no findings/`; `gates-metrics` 144 → `/new violations 1/`) are **English**, and so are S1's readers (`usageCauses()` splits on `'Causes of a code-2 refusal'`, `docs-commands:147`'s `/refusal cause is not declared/`, `parity:57`'s `/: (\d+) rows × (\d+) files/`) and `gates-verify`'s protocol reads (the hooks' `/pnpm run verify:fast/`, `/tools\/gates\/gatefiles\.js/`, `.gitignore`'s `/^reports\/$/m`). No negative match over Cyrillic in the owner (measured again, none), and the tolerance stays **dead in the tree**: `docs/requirements.md` and `docs/module-design.md` carry no Cyrillic at all, and the only Russian instances of the three regexes are the file's own comment at `:155`. **The allow-list's quotation row was re-measured as the plan required: 33, all of it `BLOCKERS.md` — `README.md` and `AGENTS.md` read 0** (the code-2 row is S1's contract, the `!` note S4's, the subject S5's). **Step 1 done 2026-09-17**: `test/docs-paths.test.js` 6 → **0** and `test/git-pins.test.js` 12 → **5**, the five being the witness's own data (`'заметки.md'` written and read by the check, the identity `'Тест'`, the subject `'заметки'`). **Both halves of the step's experiment measured:** a row removed from `README.md`'s file table reddens `docs-paths` at check 2 (`the tree holds files that are missing from the table of README`) with the table restored byte-identical; and the pin — `'core.quotePath=false'` taken out of `GIT_PINS` in `src/git.js` — reddens `git-pins` at check 2 **only where the machine's settings are not in the way**: on this machine it stays green, because its global config already carries `core.quotePath=false` (measured: the env-forced `GIT_CONFIG_GLOBAL=/dev/null` run reddens, the ordinary one does not), which is B1's own note met from this side. The Cyrillic file name was proved load-bearing the other way round: made ASCII, the check reddens **even with the pin in place**, telling its own story — `the unpinned reading handed the path back as it is: the pin then proves nothing`. Both files run green at their `HEAD` version (2 of 2 each), so no machine reads a word of them; checks unchanged (2 each; 70 fast, 175 full); `dup` unchanged (`clones 5, lines 29`); the artifact's content digest `1bdb27e1…` and the reference file's own sha256 `cdda8d01…` unmoved. **The owner reads 209**, of which the remainder splits in two: the **data** (the fixture's own contents and names, the witnesses' Cyrillic paths and identities) and **D1's debt** (the workflow step names and the `.gitignore` reader, which C3 verifies rather than edits). **The next portion (2026-09-17) took the two files the mission named — `docs-commands` and `parity`, which this list numbers 7 and 8 — so those two steps went first and their siblings (`docs-pin`, `frozen`) and steps 2–6 (the six gate files) follow.** `test/docs-commands.test.js` 22 → **5** and `test/parity.test.js` 15 → **0**; the owner reads **177**. **Measured experiments, sources restored byte-identical:** renaming the help's section marker alone (`src/refusal.js:136`) reddens `docs-commands` at check 3, and so does renaming one cause in `CONFIG_CAUSES` alone (`'unknown flag'` → `'unrecognised flag'`) — the measured proof that `README.md`'s row is a contract read through `readmeCauses()` — while `docs-pin` stays green through both, its help coming from history; one value of `SKIP_WORDS` made English reddens `parity` at checks 1 and 3 while `frozen` stays green (4 of 4) — the pair that tells a moved standard from a broken engine; and `' rows × '` changed in `src/modes.js:89` reddens `parity` at check 2, so S1's summary shape is a live read. The five lines left in `docs-commands` are named one by one: the invented cause the check feeds to `refuseCause` (`:146`, an input — the assertion reads only the English sentence), the comment quoting the word the tolerance exists for (`:155`) and the three tolerance regexes (`:181,184,186`). Red first by the whole file (both green at `HEAD`: 4 of 4 and 3 of 3), checks unchanged (4 and 3; 70 fast, 175 full), `dup` unchanged (`clones 5, lines 29`), the artifact's content digest `1bdb27e1…` and the reference file's own sha256 `cdda8d01…` unmoved. Neither file is a gate file, so **that commit carries no trailer** — step 2 is the first of the six that does. **Steps 2–5 done 2026-09-17 in one commit with the trailer** (all six files are gate files, and the hook confirmed it): `test/gates-deps.test.js` 8 → **0**, `test/gates-dup.test.js` 13 → **1**, `test/gates-coverage.test.js` 14 → **0**, `test/gates-metrics.test.js` 22 → **5**, `test/gates-files.test.js` 26 → **8**, `test/gates-verify.test.js` 26 → **0** — 95 lines of names and messages, and the owner reads **82**. What stays is named: `gates-dup`'s path of a baseline that is not there (an input), `gates-metrics`'s probe payload (the debt comment and the four injected `test(` bodies the sensor measures), `gates-files`'s own repository (identity, submit subjects and the `Gate-Change:` bodies the experiment writes — the guard reads the trailer as a shape, so the wording is free). **Every verdict read of the six was re-measured by mutation of its sensor, sources restored byte-identical:** `deps: no findings (` → reddens `gates-deps` check 1; `dup: new clones ` → `gates-dup` check 2 and `dup: there is no baseline (` → check 4; `: was ` → `gates-coverage` checks 1 and 2; `metrics: new violations ` → `gates-metrics` check 4; `: a gate edit with no Gate-Change: trailer` → **all three** checks of `gates-files`; the hooks' `pnpm run verify:fast` → `gates-verify` check 4; `.gitignore`'s `reports/` → check 5. **One honest finding:** making the **ok** verdict of `dup` (`dup: no new clones (`) Russian leaves `gates-dup` green — the ok line is printed on a green run and none of its checks reads those words, while both `bad` lines are read. Red first by the whole file: all six at their `HEAD` version ran green (23 checks in total, `fail 0`). `dup` before and after unchanged (`clones 5, lines 29`); checks unchanged (4/4/3/4/3/5; 70 fast, 175 full); the artifact's content digest `1bdb27e1…` and the reference file's own sha256 `cdda8d01…` unmoved; `pnpm run verify` (8 steps) green before the push. **The order of steps in this row is the mission's, not the plan's** (note from the portion above). **The closing portion of 2026-09-17 took the rest — `suites` 16 → 0, `docs-numbers` 12 → 0, `docs-pin` 14 → 1, `frozen` 16 → 0 — and C3 is closed at 25 lines.** Four experiments, sources restored byte-identical: a `why` dropped from an entry of `tools/suites.js` reddens `suites` at check 2; a check added without updating `README.md`'s table reddens `docs-numbers` at check 1; renaming the `Fast` row label in `README.md` reddens `docs-numbers` too (so the parse cannot rot in silence — it reddens its own `assert.ok`); and dropping the Russian branch of `commandsAt` (`tools/docs-facts.js`) reddens `docs-pin`, whose pin still leads to a revision with the Russian section marker — the branch goes dead only when **N20**'s release moves the pin, and that is said rather than discovered later. Measured about `docs-numbers`: it reads the table of runs in `README.md` (by the labels `Fast`/`Full` and the command cells) and `tools/suites.js`'s `checksIn`/`FAST`, **no other document** — the tracker included. The 25 lines left in C3 are `gates-files` 8, `docs-commands` 5, `gates-metrics` 5, `git-pins` 5, `gates-dup` 1, `docs-pin` 1: **data** (the scenario repositories, the probe payloads, the witness's Cyrillic path) and **a past's own word** (the pinned revision's `«Команды»`). Red first by the whole file (all four green at `HEAD`: 3/1/1/4); checks unchanged (3/1/1/4; 70 fast, 175 full); `dup` unchanged (`clones 5, lines 29`); the artifact's content digest `1bdb27e1…` and the reference file's own sha256 `cdda8d01…` unmoved; the quotation row still reads **33, all of it `BLOCKERS.md`** (this portion added none). No gate file was touched, so that commit carries no trailer (checked by the hook). |
| D1 | `configs.md` | `.github/workflows/**` (78), `templates/**` (35), `package.json` `description` (1), the declarative dotfiles (17: `.dependency-cruiser.cjs` 7, `eslint.metrics.config.js` 6, `.gitignore` 4) — the declarative files and what the registry shows. Measured while planning it: **131 gross = 127 this owner's own + 3 allow-listed (the draft's two values, the debt term) + 1 English comment**; four of its files are gate files, so **six of its commits carry the `Gate-Change:` trailer**; six workflow step names are read as literals by `test/release.test.js`, and the `verify` job key is a branch-protection contract outside the tree | 131 gross, **127 own** confirmed | **step 0 and step 1 done 2026-09-17.** Step 0 re-measured the owner and the expectation held **to the line** — `release.yml` 48, `templates/ci.yml` 33, `ci.yml` 19, `verify-slow.yml` 11, `.dependency-cruiser.cjs` 7, `eslint.metrics.config.js` 6, `.gitignore` 4, `size-report.config.json` 2, `package.json` 1, `templates/README.md` **0** = **131 gross** = 127 own + 3 allow-listed + 1 English comment; this is the first owner whose planned number survived its own step 0 without a correction. The owner's row also re-measured clean of holes: `.jscpd.json`, `.c8rc.json`, `eslint.config.js` and `.eslint-suppressions.json` carry no Cyrillic. **Step 1 done 2026-09-17: `templates/ci.yml` 33 → 0** — 28 comment lines and 5 step names. The plan called the file "comments only"; the measurement corrects it to "comments **and** step names", which matters because the next commit of this owner (`release.yml`) is exactly about step names. **Four probes over the file, each restored byte for byte, give the split by measurement rather than by argument:** a step name renamed → `test/templates.test.js` green (3 of 3); a flag in a comment broken (`--write` → `--writ`, and the flag's only occurrence in the file is that comment at `:28`) → green; a comment rewritten → green; a flag inside a `run:` broken (`--data` → `--dat`, `:60`) → **red** at check 2, and `fetch-depth: 0` → `1` → **red** at check 2 as well. So the flags of commands and the parsed document are contract, while comments and step names are read by nobody — and a mis-summary of an earlier probe ("a flag in a run line", which was in fact the comment) is corrected here. `pnpm run dup` cannot see this file at all (`.jscpd.json` scans `src`, `bin`, `tools`, `test`), and `tools/pack-check.js:110-112`'s byte comparison is unaffected — it compares the tree against the tarball built from that same tree. Checks unchanged (**70** fast, **175** full), `dup` unchanged (`clones 5, lines 29`, the baseline holds 15 fingerprints), the artifact's content digest `1bdb27e1…` (of `docs/size-table.html`) and the reference file's own sha256 `cdda8d01…` unmoved. No gate file was touched, so that commit carries no trailer (checked by the hook: `gate files 0`). The remaining steps of the owner: `.gitignore` 4, `package.json` 1, `.dependency-cruiser.cjs` 7, `eslint.metrics.config.js` 4 messages, then `verify-slow.yml` 11, `ci.yml` 19 and `release.yml` 48 — the last three, and steps 3–5, carry the trailer. **Steps 2 and 3 done 2026-09-17 in one commit that carries the trailer** (the portion's rule is one commit, and `package.json` is a gate file, so the hook demanded the `Gate-Change:` line and accepted it — `gate files 1`): `.gitignore` 4 → **0** and `package.json` 1 → **0**, five lines — four comments and the manifest's `description`. Red first, each half by the experiment its step named, every file restored byte for byte: folding the `reports/` line into the comment above it reddens `test/gates-verify.test.js` (fail 1, 4 of 5) with its own message — `the directory `reports/` is not in .gitignore: the reports would go into the history` — while the same four comments translated leave it green (5 of 5); and the translated `description` leaves `test/templates.test.js` green (3 of 3), no reader anywhere — measured, `rg` for its words over `test/`, `tools/`, `src/` and `bin/` answers nothing, so the registry is the only place it shows. `pnpm run pack:check` green, `pnpm run deps` and `pnpm run metrics` unmoved, `dup` unchanged (`clones 5, lines 29`), checks unchanged (**70** fast, **175** full), the artifact's content digest `1bdb27e1…` and the reference file's own sha256 `cdda8d01…` unmoved. The counter over the ten files reads **93** (89 own + 3 allow-listed + 1 English comment), so **the owner reads 89 own**. Remaining steps: `.dependency-cruiser.cjs` 7, `eslint.metrics.config.js` 4 messages, then `verify-slow.yml` 11, `ci.yml` 19 and `release.yml` 48 — the last three, and steps 4–5, carry the trailer. **Steps 4–7 done 2026-09-17 in one commit that carries the trailer** (all four files are gate files, so the hook demanded the `Gate-Change:` line and accepted it — `gate files 4` of 7): `.dependency-cruiser.cjs` 7 → **0**, `eslint.metrics.config.js` 4 messages → **0**, `.github/workflows/verify-slow.yml` 11 → **0**, `.github/workflows/ci.yml` 19 → **0** — 41 lines, after which `release.yml`'s 48 are what is left of this owner. Measured that only values moved: `require`ing the dependency config gives the same seven rule names with `severity: 'error'`, and `pnpm run deps` answers `no findings (113 modules, 472 relations)` before and after; `pnpm run metrics` answers `no new violations (the baseline holds 0 in 0 files)` both times — `.eslint-suppressions.json` is `{}` and carries no message text, the `{{what}}`/`{{term}}` placeholders survive, and nothing outside the file quotes the four messages. **Red first, each file restored byte for byte:** a `run:` prefix broken (`git fetch` → `git clone`, in either workflow) reddens `gates-verify` with its own message; renaming `ci.yml`'s job key reddens it with `the CI description holds no job «verify»` — branch protection's required check, left untouched — while a workflow's **top-level `name:`** and a step name translated leave it green (5 of 5), and `verify-slow`'s job key is read by no check at all (renamed in a scratch copy → green) and was left as an identifier rather than a text. The comment that quotes `verify-slow`'s display name moved with it (the C2 rule). **A slip worth keeping:** an apostrophe put into one rule's `comment` broke the string and the sensor caught it — `pnpm run deps` died at `tools/gates/deps.js:33` and `test/gates-deps.test.js` went red at four checks — the cheapest proof that the config is parsed rather than ignored; and the command a rule fires with is a person's text, measured to be printed **and carried into the machine report** (a scratch config made to fire answered 17 findings with the comment in them). `dup` unchanged (`clones 5, lines 29`); checks unchanged (**70** fast, **175** full); the artifact's content digest `1bdb27e1…` and the reference file's own sha256 `cdda8d01…` unmoved. The counter over the ten files reads **52** (48 own + 3 allow-listed + 1 English comment). `release.yml`, step 8, went to a portion of its own with its reader `test/release.test.js`, as the mission asked. The acceptance sentence that read `eslint.metrics.config.js` **1** was corrected by measurement to **2** — the English comment above the debt term quotes the term in Cyrillic. **Step 8 done 2026-09-17 and D1 closed with it:** `.github/workflows/release.yml` 48 → **0**, and `test/release.test.js`'s seven step-name reads moved **in the same commit**, as the rule demands — that file now reads 0 as well, which pays C2's last debt. Red first, every file restored byte for byte: the step name «Публикация» translated **alone** reddens `release` (2 of 3) with its own message — `the release workflow holds no single step «Публикация»`; the hint rewritten without the basename reddens it with `the hint does not name the workflow file…`, so the file's self-naming is load-bearing; a step name no check reads (`Установка`) leaves it green (3 of 3); and the translated document still parses — `parseWorkflow` answers the same **11 steps** with `input.default` still `true`. Identifiers untouched and named: the `jobs:` key `release`, `permissions`, the two `if:` conditions, the four pinned `uses:` SHAs, `runs-on`, every `run:` command and the `tags: ['v*']` range. The bullet that said **six** step names and called `:86`'s name English was corrected by measurement to **seven** — `npm поновее (для trusted publishing)` carries the Russian word and is found by `step()` through its name. **The wash-up (step 9), measured 2026-09-17: the counter over the ten files reads 4** — `templates/size-report.config.json` 2 and `eslint.metrics.config.js` 2 — and nothing else, so **D1 is closed**, and with it every row of the map. |

**G1 is withdrawn — decided 2026-09-16 by the mission agent.** The guard that was to redden on a new
Russian literal outside the allow-list is not written, and the reason is that the question it asked has
stopped being live: the instruction this work follows says English everywhere but the chat, so Cyrillic
arriving in new code or documentation is a near-impossibility rather than a risk to watch. What holds
the allowance instead is the work's own acceptance — the counter per file, before and after, quoted in
each subplan and each journal entry — and the allow-list above as a recorded enumeration. **The price is
named:** a stray Russian literal added later is caught by a person running the counter rather than by
the suite, and a future reader finds the enumeration in this file rather than in a failing check.
`BLOCKERS.md` N27 holds the decision and its reversibility. The measurements taken for the design are
kept there too, so that a later hand does not have to take them again: a counter over `git ls-files`
with the pattern `\p{Cyrillic}` (no Cyrillic in the pattern itself) runs in **3.0 s** over the tree,
**122 files** carry Cyrillic outside `worklog/**` today, and such a check would be a test file whose
declaration `test/suites.test.js` demands in `tools/suites.js` — a gate file, hence a `Gate-Change:`
trailer.

`AGENTS.md`, `BLOCKERS.md`, `README.md` and the built report are not a subplan of their own: the
first three hold allowed quotations of printed output (**re-measured 2026-09-17 at C3's step 0: 33
lines, all of them `BLOCKERS.md` — `AGENTS.md` and `README.md` read 0**; the readings of 2026-09-16 were
21 and 22, and the growth is this campaign's own journal quoting printed Russian), and the report is
regenerated by whichever subplan changes the texts it is built from (S1 first, since it owns the
dictionaries).

### Map correction — measured 2026-09-16 while planning S5

The map's own total (2 396) is its first reading; the split below is today's, and it is exact.
The instrument (unchanged) now answers **5 512 lines** outside `worklog/**`, against the 5 385 of
the map above; this folder's own subplans and the notes they added to `BLOCKERS.md` are the growth.
Two files the map did not name, found by grouping the instrument's own output:

- **`.githooks/**`** (4 lines: `pre-commit` 2, `pre-push` 2) — the hooks that run the profiles;
they are **gate files** (`tools/gates/gatefiles.js`), so they belong to W2 and need the trailer.
- **The declarative dotfiles** (17 lines: `.dependency-cruiser.cjs` 7, `eslint.metrics.config.js` 6,
  `.gitignore` 4) and **the two baselines' `note`** (2 lines, written by `tools/gates/dup.js:128`
  and `tools/gates/coverage.js:81`, so W2's) — D1 and W2 respectively. The first two of the
  dotfiles are gate files.

With those two corrected the map is **complete** and the sum is exact: the instrument's 5 512 lines
split into the allow-list — the frozen layer 2 602, the archived plans 15, `docs/plans/**` 165, the
builders that write a reference 107 → **2 889** (re-measured 2026-09-16, when N21 was decided) — and
everything else → **2 623**, held by the map's owners: `src/**` 351, `bin/**` 2, `tools/**` 368,
`tools/gates/**` 77, `tools/synthetic/**` 1, `test/**` 1 320, `templates/**` 35,
`.github/**` 78, `.githooks/**` 4, the declarative dotfiles and the baselines' `note` 19,
`package.json` 1, the allowed quotations 18, the built report 349 (2 623 = 5 512 − 2 889). Every
line the instrument names has an owner, and the rows above are gross of the 58 allow-listed
dictionary lines inside `src/**`.

### Re-measured 2026-09-16, after S1's step 4 (the work, point 5)

The instrument over `git ls-files` answers **13 024 lines**, of which `worklog/**` is 7 569, so the
tree outside the journal is **5 455** — the reading taken after S1's step 4 and before this section's
own text, which adds a few lines every time it is rewritten. The map's own reading was 5 512, the
first re-measurement 5 474 and then 5 480: the subplans written meanwhile add to `docs/plans/**`, the
runtime steps take out of `src/**`, `test/**` and the catalogue, and this section's own text is part
of the last figure. That is why what follows is measured per bucket of one reading rather than
subtracted. Measured per bucket: the allow-list — the frozen layer `fixtures/**` 2 602, the archive
15, `docs/plans/**` **282** (the row above says 165, which was its reading before the subplans
existed), the builders that write a reference 107 → **3 006** — and everything else → **2 449**:
`src/**` 261, `bin/**` 2, the `tools/**` instruments 306, `tools/gates/**` 77,
`tools/synthetic/**` 1 (the folder's 89 less the builders' 88), `test/**` 1 312, `templates/**` 35,
`.github/**` 78, `.githooks/**` 4, the dotfiles with the two baselines' `note` 19, `package.json` 1,
the allowed quotations **21**, the built report 349 — the same 349 the plan recorded, because no
step touches the dictionary the page is built from. The 19 lines of the two instrument ranges are
counted inside `tools/**`, not twice.

What the work's own acceptance measures — the counter per file, before and after each step, quoted in
every journal entry — reads like this so far: `src/refusal.js` 46 → 11 → **0** (`c0df1fb` the help,
this commit the registry), `src/args.js` 26 → 14 → **0** (`cb64f12` the messages, this commit the
causes), `src/config.js` 35 → 34, `tools/refusals.js` 149 → 94, `README.md` 4 → 3,
`test/cli.test.js` 79 → 76, `src/cli.js` 4 → **0** and `src/modes.js` 13 → **0** (step 4, with the
summary's shape kept: `: N rows × M files`) — **149 lines out of the eight**, while the live catalogue
of refusals (`test/refusals.test.js`, one run per case) is green after each step and
`test/refusals-catalog.test.js` counts every refusal site, so no cause was renamed in one file alone.
The artifact is untouched by all of it, measured rather than assumed: `docs/size-report.html` hashes to
`dbcb24d1f7be373d8b9099d718a21dbdc4a840d4dc7c9f08a2efd228745ba5a4` before and after step 4 (the page
is built from the `ru` dictionary, not from the CLI's own lines), and `fixtures/**` is clean through
every step (`git diff --stat -- fixtures` empty; the parity reference's
`artifact.sha256` = `1bdb27e14bac2036f96e50fa5ec6623f7f37faeea4e45b6f91c54e6bc3ba59cd` for
`docs/size-table.html`, and `check:standards` reproduces both).

### What is left, measured 2026-09-16

The only row left is unwritten; everything else on the map is written — S1–S5 293 net of the
dictionary lines, W1 331, W2 121, C1 355, C2 739, C3 226 (1 320: the whole of `test/**`) and D1 127.
**W1's 331 is a planning figure: measured 2026-09-16, after S1–S5 and W1's step 1, its twelve files
read 23** (235 before W1 started: −10 in step 1, −13 in step 2, −23 in step 3, −74 in step 4, −29 in step 6, −1 in step 7, −55 in step 8, −7 with the two decisions of 2026-09-16 — N29's repair, which took the six `firstDiff` literals, and N30's, which took the word half of `run-tests.js:40`), and the `tools/**` family reads 226 rather than 452 — the rows of the
map are what the counter said when they were written, and the tracker's W1 row carries today's number.
The numbers are the counter's, not a subtraction: the map's rows sum **exactly** to the instrument's
reading — `src/**` 351 (of which the allow-listed dictionaries 57, one comment of 3 and `SKIP_WORDS` 1;
four of the dictionary's lines are N25's decision of 2026-09-16, which took the panel's four literals out
of `src/page/panel.js` — that file reads 0 today, the dictionaries read 61, and `src/**` reads 65),
`bin/**` 2, the `tools/**` family 452 (W1 331 + W2 121), `test/**` 1 320 (C1 355 + C2 739 + C3 226),
D1 131 (of which 127 are this owner's — 3 allowed values and 1 comment), the allowed quotations 18, the
built report 349 — and with the allow-list (the frozen layer 2 602 + `docs/plans/**` 165 + the archive
15 + the builders 107 = 2 889) the sum is 5 512, today's reading.

**Nothing is left to write.** The last row, `G1`, is withdrawn (`BLOCKERS.md` N27), so every row of the
map is written and point 4 of the work is complete. **After 2026-09-16's W1 the row is finished as
well:** every instrument is English except the 30 named lines above, and the campaign's remaining rows
are the ones not started — **W2** (the sensors and `.githooks/**`, every commit of it a gate file),
~~**W2**~~ (**closed 2026-09-16**: all seven steps in one day, the counter over its eleven files and two
baselines answers zero, and each commit of it carried the `Gate-Change:` trailer), ~~**C1**~~ (**closed
2026-09-16**: eight steps, the counter over its nine files answers **18** and every one of the 18 is data
proved by experiment in `tests-page.md`), ~~**C2**~~ (**closed 2026-09-17**: eight steps, the counter over its fourteen files
answers **87** and every one of the 87 is allow-listed data or D1's contract, named step by step in
`tests-cli.md`), ~~**C3**~~ (**closed 2026-09-17**: the fourteen files answer **25**, every line of it allow-listed — the
scenario repositories, the probe payloads, the witness's Cyrillic path, the past's own word; the planned
226 was re-measured to 222 at its step 0, exactly as C1's and C2's numbers were), and **D1** (the
declarative files) — ~~**closed 2026-09-17**~~: all nine steps, six of its commits carrying the
trailer, and the counter over its ten files reads **4** (the two allow-listed values of the draft and
the debt term with the English comment quoting it). **With D1 closed the campaign's last owner is
done.** **`test/**` reads 123** in total: C1 18 + C2 80 + C3 25 — C2's 87 minus the seven step-name
lines of `test/release.test.js`, which moved with `release.yml`, while C1's and C3's numbers stand. The two tolerances that would have
been named to G1 — W1's `commandsAt` and C3's `требовани|requirements?` resolver —are named in the allow-list above instead.

**The campaign's closing inventory, measured 2026-09-17** (`rg -cP '[\p{Cyrillic}]'` over the tree,
`node_modules` and `.git` aside): every owner's translatable literals are English, and what carries
Cyrillic is exactly the named exceptions — `src/**` **65** (`src/locales.js` 49 and
`src/metrics.js` 15, the `ru` dictionaries of N19/N25, plus `src/history.js` 1, `SKIP_WORDS` of N24),
`bin/**` **0**, `tools/**` **111** (`tools/synthetic/**` 88 — the payload the fixture generators
write, `tools/parity-freeze.js` 16 — the `manifestNote` of N21, `tools/make-fixture.js` 3 — the
draft's values, `tools/docs-facts.js` 3 — the two Russian names of the pinned help and the comment
above them, live until N20 moves the pin, and `tools/run-tests.js` 1 — the example inside a usage
comment), `test/**` **123** (the scenario repositories, commit subjects, witness paths and
identities, probe payloads — named file by file in the three check plans), `templates/**` **0** — since
N19's decision of 2026-09-17 made the default locale `en` and the draft followed it — and
`eslint.metrics.config.js` **2** (the term the linter matches and the English comment quoting it),
`BLOCKERS.md` **33** (the allowed quotations) and `TODO.md` **3** (bug notes quoting Russian
patterns), `fixtures/**` **308** (the frozen standard itself), `docs/**` **706** — of which the
generated page **349** (it is built by the engine of the attached copy in `node_modules`, still `2.4.0`
with `locale: 'ru'`, so N19's English default reaches it with the release that updates that copy — measured) and `docs/plans/**` **357** (the campaign's
own plans, quoting what they planned) — `worklog/**` **7 669** (the journal;
`worklog/archive/WORKLOG.md` alone 4 412) and `plans/archive/REFACTOR.md` **15** (the archived
originals). The tree's total reading is **9 037**, and the difference from 2026-09-16's 5 512 is
mostly the journal itself: every portion of this campaign writes an entry quoting the Russian it
moved, which is why the same measurement grows while the work shrinks. **The fixture payload was
re-measured as data rather than as an oversight:** translating one string of
`tools/synthetic/content.js` leaves the fast suite's `frozen.test.js` green (4 of 4) and reddens
`check:standards` with **8 divergences** (exit 1) — the reader lives in the full profile, as
everywhere in this campaign. **Decisions still with the user:** N19 (the report's default locale),
N20 (the release cadence — it also moves the pin and with it the last Russian branch of
`commandsAt`), N21 (the values that stay reserved), N31 (seven stale fingerprints in the `dup`
baseline), N32 (the red coverage cap); N24 is decided — `SKIP_WORDS` stays Russian.

**The order has been respected, and is now decided once.** The table's row order has been followed
without skipping a row: T0, S1, S2, S3, S4, S5, W1, W2, C1 — and C1 is closed. The one place where this file contradicted
itself — the **Order** section below grouped D1 with W1 in step 2 while the table listed D1 after W3 —
is settled in that section, which now follows the table's row order and names its reasons. It was the
third place where the map's own two readings differed from the measurement (the twelfth instrument
became eleven, and `.githooks/**` with the dotfiles had no row); all three are corrected by
measurement, and this one by a decision written down rather than left to the reader.

**A fourth correction, 2026-09-16, decided rather than measured:** N21's answer left the fixture
builders with one line of their own, so **W3 is folded into W1** and the map no longer promises a
subplan for a single internal error. The measurement behind it is in `BLOCKERS.md` N21 (107 lines
allow-listed, one left) and the fold is recorded here, in the W1 row and in `tools.md`.

## Order — decided once, 2026-09-16; the table above is the only authority

1. **S1 → S5** (the published runtime) — the reader who installs the package is the reason the
   work exists, and the texts of `src/` are what the generated report and the templates quote.
2. **W1 → W2** — the instruments, then the sensors: they print to a developer and to CI, and
   nothing else depends on their wording; W2's commits carry the `Gate-Change:` trailer.
3. **C1 → C3** — the checks last of the code: after the runtime, because a message that repeats
   the printed text has to match it, and because a translation of a message is proven by the run
   of the check that carries it.
4. **D1** — the declarative files (the workflows, the templates, `package.json`, the dotfiles):
   they speak to CI and to a registry rather than to a person, four of them are gate files, and
   they are the last of the non-test files whose text a check reads — measured while planning it,
   **six** workflow step names are read as literals (`test/release.test.js:62,66,101,114,127,132`),
   and they move with D1's own commits (the rule: a reader inside another owner's file follows the
   text, so the order cannot break it).
5. ~~**W3** — the fixture builders.~~ **Folded into W1 on 2026-09-16**, by the mission agent's
   instruction and confirmed by measurement: after N21 this owner was one line
   (`tools/synthetic/repo.js:74`),which is no portion. W1's row
grew by that line, and the map has one row fewer.
6. ~~**G1** — the guard that closes the allowance.~~ **Withdrawn 2026-09-16** (`BLOCKERS.md` N27): the
alternative to a guard is the counter run by a person per portion, which is what the acceptance of
every subplan already asks for.

Nothing in this order is a prerequisite in substance: S*, W*, C* and D1 do not import each other's
texts — a line that reads another owner's text moves with that text, not with the file it stands in.
The order is a choice (the reader who pays is first), not a dependency, and from here on the row
order of the table above is what a portion follows.

## Rules shared by every subplan

- **Behaviour is not changed.** Only a literal's value changes: no condition, no argument, no
  control flow, no API. A subplan that needs one stops and records the need in `BLOCKERS.md`
  rather than widening itself.
- **A message read by a check is edited together with the check.** Several guards read text
  rather than behaviour, and each has to be named before its strings are touched:
  - `tools/docs-facts.js` (`commandsAt`) finds the help's command list by the literal section name
    `'Команды:'` in the revision the install example pins, read **out of git history**
    (`git show <pin>:src/refusal.js`), and `test/docs-pin.test.js` fails with "у ревизии … нет
    справки с разделом «Команды»" when it finds nothing. The pin moves with every release, so the
    commit that renames the section and the commit that moves the pin have to be the same one —
    otherwise the guard is green on the old revision and red on the new.
  - `test/refusals.test.js` and `test/refusals-catalog.test.js` match refusal **causes** (the
    registry in `src/refusal.js` and the `README.md` code table are compared with each other, so
    a cause renamed in one place must be renamed in the other) and read `refuseCause('…')`'s first
    argument as a literal.
  - The documentation guards (`test/docs-*.test.js`) parse the shell's shape from the help
    (`USAGE`) and the instruction documents — S1 changes both sides at once.
  - `test/release.test.js` and `test/hook.test.js` read the text of workflow and hook files.

  Each subplan names the readers of the strings it translates.
- **A literal quoted in a document is a claim.** `README.md`, `BLOCKERS.md` and the plans quote
  printed output; a quotation that stops being what the tool prints is updated in the same commit.
- **The allow-list is the only exemption.** No `eslint-disable`, no threshold moved, no baseline
  edited: a red sensor is fixed in the code (`AGENTS.md`).
- **Commit budget: ≤ 600 lines and ≤ 10 files.** A bigger chunk is justified in the commit body.
- **Gate files need the trailer**: `tools/gates/**`, `tools/suites.js`, `test/gates-*.test.js`,
  `package.json`, the workflows, the hooks, the baselines, the sensor configs. W2 meets it as a matter
  of course, and `configs.md` (D1) meets it in six of its commits; for the others, needing it means the plan is wrong.
- **Release per portion** — a string that ships changes the tarball's bytes, so
  `AGENTS.md`'s rule applies: a PATCH release, a journal section and the pin in `README.md`.
  Whether to release per subplan or to batch is a question for the mission agent
  (`BLOCKERS.md` N20).
- **The `post-commit` hook** rebuilds and commits `docs/size-report.html` by itself; a portion
  therefore ends with two commits, and the second one is expected rather than a defect.

## Definition of done

- `rg -lP '[\p{Cyrillic}]'` over the tree names **only** the allow-list:  `worklog/**`, the `ru` dictionary, `fixtures/**`, the builders that write them, the Russian sample data of the fixtures and the quoted old output of
  `BLOCKERS.md` and the archived plans. The measurement is quoted per subplan, before and after,
  with the file list.
- Every subplan's own acceptance holds, and `pnpm run verify:fast` is green after every commit and
  `pnpm run verify` before the portion is pushed.
- A literal that a test matched on is proven by the test still reddening where it should: the
  subplan quotes the run that would have caught a mistake (a reversed `assert`, a missing cause).
- The guard `G1` is in the suite, or its absence is a recorded decision with a reason — which is the
  case: withdrawn 2026-09-16 (`BLOCKERS.md` N27), with the counter per portion named as what holds the
  allowance instead.

## Out of scope

- `worklog/**` — the journal, per the allow-list.
- The consumer project `safe-resets`, which holds its own repository and its own report.
- The package's public API: a name in `src/size-table.js`'s re-export list is frozen by
  `test/api.test.js` and does not change.
- The frozen layer, the builders that write it and the Russian sample data of the fixtures — allow-listed,
  `BLOCKERS.md` N21 decided 2026-09-16.

## Open questions

- **The default locale.** `DEFAULT_CONFIG.locale` is `'ru'`, so a project with no settings gets
  a Russian report and a fresh install prints Russian. Making the package English by default
  means changing a default — a behaviour change, so it is a decision rather than a step: S1 owns
  it, the options and their price are `BLOCKERS.md` N19.
- ~~**The frozen layer.**~~ **Decided 2026-09-16: it stays Russian** (`BLOCKERS.md` N21, option 1), and
  the 107 lines that write it — `tools/synthetic/note.js`/`content.js`/`history.js` and the two
  instrument ranges — are in the allow-list with it. The decision is what made W3 a one-line owner, and that
  line has since been folded into W1 (see the order section).
- **Releases.** A portion that changes a shipped literal changes the package's bytes. Release a
  PATCH per portion, or batch the releases and say so in the commits: `BLOCKERS.md` N20.
- **The numbers were formatted with a Russian decimal comma made by code** (`tools/run-tests.js:43,64`
  and `tools/gates/run.js:111,114`), so a translated profile printed "total 12,2 s". **Decided
  2026-09-16 (`BLOCKERS.md` N26):** `Intl.NumberFormat` in the locale the machine runs in, through one
  helper (`localeNumber` in `tools/harness.js`) — `en_US.UTF-8` reads `12.20`, `ru_RU.UTF-8` reads
  `12,20`, and no locale is pinned because the question "which language should the tooling print in"
  is one nothing here asks. Measured: a whole fast run under the Russian locale prints `6,17 s` and
  stays green.
- **The panel's chrome was not localized.** `src/page/panel.js:9-11` held four Russian literals while
  every other caption of the page is picked by `cfg.locale`, so translating them would have given a
  `locale: "ru"` report English chrome. **Decided 2026-09-16 (`BLOCKERS.md` N25):** they went into the
  dictionaries instead — four keys in `src/locales.js` each, read through `appUi` — which is why S5's
  own counter reaches zero without a single word being translated, and why the `ru` side of the
  dictionary (data, allow-listed) grew by four lines.
- **The `--json` contract carries Russian of its own.** Measured 2026-09-16 while planning S1: the
  skip words of `src/history.js` (`SKIP_WORDS`: `только таблица` 49 times and `без изменения
  объёма` 5 times inside `fixtures/parity/data.json`, twice in `fixtures/synthetic/golden.json`)
  are part of the frozen contract, and `test/parity.test.js` compares `--json` byte for byte with
  the synthetic reference. So S4's skip reasons are the one place where a translation moves a
  reference already — the options are `BLOCKERS.md` N24, and  the decision belongs with N21
  (may the frozen layer move) — and **decided 2026-09-16: the three words stay Russian and are named as
  an exception** (`BLOCKERS.md` N24), on the same ground as N21: the references stay Russian, and
  editing `src/history.js` would change behaviour rather than a literal. No reference moves, S4's
  gated step becomes a named exception, and the decision is reversible at a recorded price. S1's own claim of an untouched reference is checked in
  `surface.md` and holds: neither reference carries the help, the causes or the mode texts.
