# D1 steps 4–7: the dependency rules, the metric messages and the two workflows

What was asked: the next portion of the D1 subplan — `.dependency-cruiser.cjs`, the messages of
`eslint.metrics.config.js`, then `.github/workflows/verify-slow.yml` and `.github/workflows/ci.yml`,
with `release.yml` left for a portion of its own together with `test/release.test.js`. Translations
only, one commit with an English message and a journal entry, trailer (all four files are gate
files), full `verify` before the push, CI checked afterwards.

## Done

- **`.dependency-cruiser.cjs` 7 → 0:** the seven rules' `comment` values. The file's own header calls
  them the sensor's verdict rather than a remark for the reader, so they are the printed text.
- **`eslint.metrics.config.js` 4 messages → 0:** the four user-facing messages of the local rules.
  `DEBT_TERMS` and the English comment above it are allow-listed and were left.
- **`.github/workflows/verify-slow.yml` 11 → 0** and **`.github/workflows/ci.yml` 19 → 0:** the
  comments, three step names each, the two preparation steps' echo text and `verify-slow`'s display
  name. The job key `verify` and the `run:` values stayed; `release.yml` untouched.
- **One commit that carries the trailer** — the portion's rule is one commit, and all four files are
  gate files, so the hook demanded `Gate-Change:` and accepted it (`gate files 4` of 7 files).
- The diff is 41 changed lines in pairs (7 + 4 + 11 + 19), and every one of them is a `comment:`,
  a `messages:` value or a comment/`name:`/echo line — no rule name, no `severity`, no `schema`, no
  threshold, no `run:` command, no job key, no path moved.

## Measured, not argued

- **Only values moved:** `require`ing the dependency config gives the same seven rule names with
  `severity: 'error'`; `pnpm run deps` answers `no findings (113 modules, 472 relations)` before and
  after; `pnpm run metrics` answers `no new violations (the baseline holds 0 in 0 files)` both times.
  `.eslint-suppressions.json` is `{}` and holds no message text; the `{{what}}` and `{{term}}`
  placeholders survive; `rg` for the four messages answers nothing outside the file.
- **Red first, each file restored byte for byte:** a `run:` prefix broken (`git fetch` → `git clone`)
  reddens `test/gates-verify.test.js` with its own message — `the step «git clone --no-tags origin
  main || echo …» is in no profile`; renaming `ci.yml`'s job key reddens it with `the CI description
  holds no job «verify»` (branch protection's required check — left alone); a workflow's top-level
  `name:` and a step name translated leave it green (5 of 5); and `verify-slow`'s job key is read by
  no check at all (renamed in a scratch copy → green) and was left as an identifier rather than a
  text. The comment quoting `verify-slow`'s display name moved with it.
- **A slip worth keeping:** an apostrophe that got into one rule's `comment` broke the string, and
  the sensor caught it immediately — `pnpm run deps` died at `tools/gates/deps.js:33` and
  `test/gates-deps.test.js` went red at four checks. The cheapest proof that the config is parsed
  rather than ignored.
- **The comment is a person's text carried by a machine too:** a scratch copy of the config with one
  rule made to fire answered 17 findings, and the rule's `comment` is in the machine report as well
  as printed. Nothing compares that text (`reports/` is gitignored, the sensor counts findings).
- `pnpm run dup` cannot see any of these four files (`.jscpd.json` scans `src`, `bin`, `tools`,
  `test`), and its verdict is unchanged anyway: `clones 5, lines 29`, the baseline's 15 fingerprints
  untouched; `pnpm run pack:check` green.
- Checks unchanged (**70** fast, **175** full); the artifact's **content** digest
  (`fixtures/parity/artifact.sha256`, of `docs/size-table.html`) `1bdb27e1…` and the reference file's
  own sha256 `cdda8d01…` unmoved; full `pnpm run verify` (8 steps) green before the push.

## Suggested

- The plan's acceptance sentence expected `eslint.metrics.config.js` to answer **1**; the measurement
  says **2** — the English comment above the debt term quotes the term in Cyrillic. Corrected in the
  plan rather than worked around.
- What is left of D1: **`release.yml` 48** — the seven step names read as exact strings by
  `test/release.test.js:62,66,86,101,114,127,132`, which have to move in the same commit as their
  assertion lines (the plan says six; the earlier measurement corrected it to seven). Then the
  wash-up over the ten files.
- The counter reads **52** (48 own + 3 allow-listed + 1 English comment); the owner reads **48 own**.
- This owner ships: `description` is what the registry shows and `templates/**` travels in the
  tarball. The release question is `BLOCKERS.md` N20 and is not decided here; the version stood
  still, as in the previous portions.
