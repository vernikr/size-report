# 0184 — i18n C1 steps 5 and 6: the memory of a choice, and the page

**Request.** Point 5: steps 5 and 6 of subplan C1 (`tests-page.md`) — `test/page-choice.test.js` and
`test/page-view.test.js`. Only literals; the number and composition of the checks stay put.

**What moved — 137 lines, and both files now read 1 each.** `test/page-choice.test.js` 67 → **1**: sixty-six
names and messages. `test/page-view.test.js` 72 → **1**: seventy-one names and messages. The line left in
each file is **data**: `'{ это не JSON'` (the payload of the broken record the page is fed) and
`f.label === 'заметки.md'` (the fixture's own file label).

**Red first, one phrase at a time: 65 + 73 phrases stayed green** when put back to Russian on their own, with
`node --test <file>` answering the same both ways. No reader of a check's words, no negative match over
Cyrillic text in either file, and none of the old names appears outside its own file.

**The three groups the mission asked to separate, named by measurement.**
1. **Dictionary reads** — `ui.<key>` comparisons (`ui.linkForeign` and `ui.linkBroken` in `page-choice`,
   `ui.empty` and `ui.noFiles` in `page-view`). They are **self-consistent**: `ui` is parsed from the page's
   own embedded `<script id="ui">`, so changing the value in `src/locales.js` moves both sides and the check
   stays green (measured for `linkForeign` and for `empty`). What such a line proves is that the page shows
   the dictionary's own words; the value itself is data, which is why it is not translated.
2. **The words of another owner's printed text** — none is left in these two files: the fixture and the
   panel wording come from the page's own data, and the checks read them through variables rather than
   through Russian literals.
3. **Prose for a person** — the 137 test names and messages, all translated.

**The data sides were proved by experiment rather than by reading.** The broken-record payload in ASCII
leaves `page-choice` green — its Cyrillic is incidental, and it is the fixture's own record; the fixture's
label `'заметки.md'` reddens `page-view` when translated, exactly as it did in `contract-data` at step 3.

**Counters.** The nine files of the owner 206 → **69**; `page-choice.test.js` 67 → 1, `page-view.test.js`
72 → 1. Check counts unchanged (6 and 8; 44 over the nine files, 70 in the fast run and 175 in the full one).
Untouched: the `ui.<key>` comparisons and their values, the fixture's label and payload, the frozen name list
of `test/api.test.js`, the golden comparison, `SITES` 27, `PRINTED` 2/2/1/2/4, `CASES` 38. References stand:
`check:standards` green, the artifact hash `1bdb27e1…`, the built page byte-identical (66 427 B), the
package's report the same. `dup` green (8 clones, 47 lines, 15 fingerprints) over 137 fresh messages.

**No gate file in this portion**, so no trailer; checks do not ship, so no release.

**C1 is not closed** — steps 5 and 6 of the plan's eight. Left: `module` 58 of its own (step 7), the eight
data lines of the passed files, and the verification commit (step 8), whose three reads were measured in line
by step 0.
