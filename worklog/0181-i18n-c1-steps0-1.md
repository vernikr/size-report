# 0181 — i18n C1 steps 0 and 1: the first owner of the checks

**Request.** Point 5: the first step of subplan C1 (the checks of the page and the contract, `tests-page.md`
— the mission named it `checks.md`). Only literals; the number and composition of the checks stay put.

**Step 0 — the re-measurement the plan asked for.** The nine files answer **347** Cyrillic lines, not the 355
of the plan, and the whole drop is `test/module.test.js` **66 → 58**: the eight lines of the plan's section
(2) are English with their owners' steps. Measured one by one rather than assumed: `/esbuild did not parse
src\/view\.js/` (`:197`), `/is not JavaScript/` (`:218`), `remove this extension from minify.guard` (`:223`),
`give this extension a simplification in minify.ext` (`:205`), the `--init` hint's regex
`/pin them with a file of their own[^:]*: (.+)$/m` (`:258`), and section (3)'s two negative matches
(`:120`, `:195`) already name `/stripper/` — the silent-tautology trap the plan carried is closed. The
positive read at `:163` is `/the stripper broke/`, so S3's correction holds too. **Check counts: 44 over the
nine files, unchanged** — `page-view` 8, `page-choice` 6, `module` 6, `page-tree` 5, `contract-data` 8,
`contract-derived` 5, `guard` 3, `runner` 2, `api` 1.

**Step 1 — `test/runner.test.js` 11 → 5 and `test/guard.test.js` 11 → 1.** Nineteen names and messages:
"output chunks are joined as buffers: a torn character survived", "reading the output hands back the code, the
output and the errors separately", "a module is parsed in a worker thread rather than by a run", "both paths
of parsing give the same verdict", "hundreds of parses are cheaper than one Node run" and the sentences
beside them.

**Red first, one phrase at a time: all nineteen stayed green** when put back to Russian on their own, with
`node --test test/runner.test.js test/guard.test.js` answering the same both ways. No reader of a test's words
anywhere in the tree either (measured: none of the six old names appears outside its own file).

**The six lines that stay are data, and one of them was proved so by experiment.** `runner.test.js` keeps the
child's payload (`-конец`, `й-конец`) and the two sentinels (`из вывода`, `из ошибок`); the split in that
check is between the **two bytes of one multi-byte character**, so replacing the payload letter with an ASCII
one turns the check **red** — measured — and would leave it green and proving nothing had the expectation
been changed with it. `guard.test.js` keeps its sample `'export const a = `текст;\n'`, the fixture's own text.
None of these is printed by the tool, and none is compared with a product string.

**Counters.** The nine files 347 → **331**; `runner.test.js` 11 → 5, `guard.test.js` 11 → 1. Untouched:
`test/api.test.js`'s frozen name list, `test/contract-data.test.js`'s golden comparison, `SITES` 27,
`PRINTED` 2/2/1/2/4, `CASES` 38, 70 checks fast and 175 full, and every reference. `pnpm run dup` green
(8 clones, 47 lines, 15 fingerprints): nineteen fresh messages are exactly where a twin appears, and none did.

**No gate file in this portion**, so no trailer: checks do not ship either, so no release is owed.

**C1 is not closed** — steps 2–8 remain: `api` + `contract-derived` (22), `contract-data` (51), `page-tree`
(55), `page-choice` (67), `page-view` (72), `module` (58 of its own) and the verification commit.
