# D1 steps 0 and 1: the re-measure and `templates/ci.yml`

What was asked: the first step of the D1 subplan — the declarative files — with step 0's re-measure
beforehand. Translations only, one commit, English message and journal entry, full `verify` before the
push, CI checked afterwards, `Gate-Change:` only if the portion touches a gate file.

## Done

- **Step 0 — the owner re-measured, and the plan held to the line** (the first time in this campaign):
  `release.yml` 48, `templates/ci.yml` 33, `ci.yml` 19, `verify-slow.yml` 11, `.dependency-cruiser.cjs`
  7, `eslint.metrics.config.js` 6, `.gitignore` 4, `size-report.config.json` 2, `package.json` 1,
  `templates/README.md` **0** = **131 gross** = 127 this owner's own + 3 allow-listed + 1 English
  comment. The plan's own list of holes was re-measured too: `.jscpd.json`, `.c8rc.json`,
  `eslint.config.js` and `.eslint-suppressions.json` carry no Cyrillic.
- **Step 1 — `templates/ci.yml` 33 → 0:** 28 comment lines and 5 step names. The plan called the file
  "comments only"; the measurement corrects it to **comments and step names**, which matters because
  the owner's last step is exactly about step names.
- The diff is `33 insertions / 33 deletions` over one file, and a filter over the changed lines shows
  every one of them is a comment or a `- name:` — no command, flag, path or value moved.

## Measured, not argued

- **Four probes, the file restored byte for byte after each:** a step name renamed
  (`- name: Установка` → `- name: Setup`) — `test/templates.test.js` green (3 of 3); a flag broken
  **in a comment** (`--write` → `--writ`; the flag's only occurrence in the file is that comment at
  `:28`) — green; a comment rewritten — green; a flag broken **inside a `run:`** (`--data` → `--dat`
  at `:60`) — **red** at check 2, and `fetch-depth: 0` → `1` — **red** at check 2 as well. So the
  parsed document and the commands' flags are contract, while comments and step names are read by
  nobody. A mis-summary of an earlier probe ("a flag in a run line", which was in fact the comment) is
  corrected here and in the tracker.
- `pnpm run dup` cannot see this file at all — `.jscpd.json` scans `src`, `bin`, `tools`, `test` — so
  no new clone pair can come out of this step, by construction rather than by luck.
- `tools/pack-check.js:110-112` compares `templates/**` between the tree and the built package, which
  come from the same tree, so the shipped bytes follow the tree; `templates/README.md`, the only
  template the documents' guards read, is untouched (it was already English) and no file was added, so
  `README.md`'s file table does not move.
- Checks unchanged (**70** fast, **175** full); `dup` unchanged (`clones 5, lines 29`, the baseline
  holds 15 fingerprints); the artifact's **content** digest (`fixtures/parity/artifact.sha256`, of
  `docs/size-table.html`) `1bdb27e1…` and the reference file's own sha256 `cdda8d01…` unmoved; full
  `pnpm run verify` (8 steps) green before the push.

## Suggested

- No gate file was touched — `templates/**` is not one (`tools/gates/gatefiles.js` does not name it) —
  so this commit needs no trailer, and the hook confirmed it (`gate files 0`).
- What is left of D1: `.gitignore` 4 (no trailer) and `package.json` 1 (trailer), then
  `.dependency-cruiser.cjs` 7 and `eslint.metrics.config.js`'s 4 messages (trailer), then the three
  workflows — `verify-slow.yml` 11, `ci.yml` 19 and `release.yml` 48 with its six step names read as
  exact strings by `test/release.test.js`, which have to move in the same commit.
- **This owner ships:** `package.json`'s `files` names `templates`, so `templates/ci.yml`'s words now
  travel in the tarball, and `description` is what the registry shows — the release question is
  `BLOCKERS.md` N20 and is not decided here.
