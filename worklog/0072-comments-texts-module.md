# 0072 — комментарии отказа и словаря (модуль M2 подплана `comments.md`)

**Запрос.** Пройти модуль M2 (`src/refusal.js`, `src/locales.js`): датировать утверждения через
`git blame`, отбросить неактуальное и неверное, перевести комментарии на английский и сократить
до того, что нельзя вывести из кода; словари и тексты — данные, их не трогать.

**Сделано.** `src/locales.js` 133 → 131, `src/refusal.js` 138 → 139; модуль 271 → 270, правка —
91 строка в 2 файлах. Диффом проверено: изменены только строки комментариев и одна пустая
строка между слитыми шапками.

**Что отброшено как неактуальное:**

- `src/locales.js`: две шапки подряд говорили одно и то же («тексты отчёта — это словарь, а не
  рендер») — слиты в одну; причина «правка слова стоит пересборки отчёта» сохранена, потому что
  её из кода не видно.
- `src/refusal.js`: убрана отсылка к таблице кодов `PLAN.md` §4.1 — таблица кодов есть тут же, в
  `USAGE`, а раздел плана при переработке документации может исчезнуть: отсылка из кода на
  документ, который переписывает эта же работа, — обещание сломаться.
- Там же **исправлено неверное**: свой `__filename` в файле нужен не «подсказке в `loadConfig`»
  (она только пользуется зовом `invocation()`), а самому `invocation()`; причина ESM
  (`import.meta.url`) осталась.

**Проверено.** `git diff`: изменены только строки комментариев; `pnpm run verify:fast` зелёный
целиком (5 шагов, 72 проверки из 177); словари, `USAGE`, `CONFIG_CAUSES` и тексты отказов не
тронуты — счёт мест отказа и знаков «✗» тот же (`test/refusals-catalog.test.js`).

**Предложено.** Дальше — M3 (`src/metrics.js`, `src/minify.js`, `src/tokens.js`, `src/optional.js`,
`src/strip.js`, `src/strip/*`, `src/parse.js`, `src/parse-worker.js`, 891 строка): при бюджете
коммита в 600 строк модуль, скорее всего, придётся делить на два захода. Открытый вопрос плана о
выпуске на правку комментариев всё ещё ждёт решения агента mission.
