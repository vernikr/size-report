# 0187 — i18n C2 steps 2–3: the CRLF/environment checks and the command-line checks

Requested: point 5 of the i18n campaign, the next portion of
`docs/plans/2026-09-16-i18n-english/tests-cli.md` (C2).

Step 2 — `test/crlf.test.js` 9 → **0**, `test/environment.test.js` 18 → **1**:
- Nine names and messages in the first file, seventeen in the second.
- Red first, one phrase at a time: all 24 phrases put back to Russian on their own left the file's
  probe green — no reader of a check's words.
- The one line that stays is the fixture's own commit subject, pinned in
  `fixtures/synthetic/golden.json` and `manifest.json`: translating it **reddens**
  `test/environment.test.js` (a pin, measured).
- Measured the other way round too: the five pieces of the two files' own fixture content (the README
  written into a fresh project, its commit subjects, the comments appended to a file and to the table)
  stay green when translated — no reader, kept as the scenario's own data.
- One test name renamed in `test/cli.test.js` left `checksIn` (**9**) and the probe green, so the
  counter is of `test(` calls rather than of names.

Step 3 — `test/cli-paths.test.js` 30 → **3**, `test/cli.test.js` 74 → **2**:
- Twenty-seven names and messages in the first file, seventy-two in the second.
- Red first by the whole file (W1's method, chosen over 97 per-phrase runs): each file put back to its
  `HEAD` version wholesale left its own probe **green**.
- Corrected 2026-09-16 while doing C2's steps 4–5: the four files of steps 2–3 turned **125** lines English
  (9 + 17 + 27 + 72), not 116 as this entry first said, so the owner read **537**, not 546. The per-file
  figures in this entry were right; the sum was not.
- The machine half was guarded: `cli-paths.test.js:104`'s anchored marker
  (`/^\s*fix: .*git init$/m`) reddens check 6 when the marker is put back to Russian in
  `src/config.js:69` — restored byte-identical.
- `cli.test.js:69` is the only negative match over printed text in the four files, and it is not
  vacuous: the same phrase is asserted positively at `:45`.
- Found and named: the translation **dissolved** an accepted clone, `cli-paths.test.js:28-33` ↔
  `doctor.test.js:176` (five lines of the shallow-clone setup whose two Russian messages matched
  `doctor.test.js` verbatim) — clones 8 → 7, lines 47 → 42. Step 6 (`doctor`) is where it can form
  again, with different wording.

Measured after: check counts unchanged (1/4/6/9; **70** checks in the fast run, **175** in the full
one); `dup` green; the artifact's digest `1bdb27e1…` and the sha256 of the reference file itself
`cdda8d01…` both unmoved; the built page 66 427 B; `verify:fast` and the full `verify` (8 steps)
green. No gate file in the portion, so no `Gate-Change:` trailer.

Recorded in `TODO.md`, not chased: `test/cli.test.js:311`'s message points at `PLAN.md §4.1` while the
file lives at `plans/archive/PLAN.md`.

Left in C2: steps 4–8 (546 lines over eight files). Left in the campaign: C2, C3, D1. Open with the
user: N19, N20, N21, N24, N31, N32.
