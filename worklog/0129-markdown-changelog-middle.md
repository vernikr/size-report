# M24 подплана markdown: середина `CHANGELOG.md`

**Запрошено:** пройти выпуски 2.0.0 … 1.3.0 (262–422 по замеру), перепроверив каждое число, имя файла,
поле и обещание по коду и живой истории; сжать и перевести; строки таблиц с именем файла фикстуры — данные,
их не трогать; сторож `test/changelog.test.js` — тем же коммитом, если правка формы его задевает; замер
адресов до и после.

**Сделано.** Пройдено **262–441** — 2.0.0, 1.3.1, 1.3.0, **1.2.0** (нарезка обрывала его на 422, то есть
посреди раздела о числах; граница исправлена замером: конец 1.2.0 = 441, дальше `## 1.1.1`). 98 добавлено /
102 удалено = **200 изменённых строк в одном файле** при бюджете 600. Кириллицы в файле 260 → **161 строки**,
из них **10 — строки данных фикстуры** (`заметки.md`); в пройденном диапазоне прозы по-русски не осталось
(первая кириллическая проза — строка 444, это 1.1.1). `git diff` не содержит **ни одной** изменённой строки
заголовка и ни одной строки таблицы — адреса и данные не сдвинулись. `verify:fast` зелёный (5 шагов,
72 проверки из 177). Сторож не тронут: раздел о числах во всём файле уже английский с M23, данные те же,
поэтому его правка этому проходу не нужна. Выпуска нет.

**Найдена ошибка в 2.0.0 — про имя и адрес отчёта.** Прежний текст говорил «имя больше не выводится из
настроек: файл это и есть отчёт». Замер по тегам: в v1.3.1 `outputOf` давал `size-table.html`
(`exists('docs') ? 'docs/' : ''`), в v2.0.0 — уже `size-report.html` при той же развилке, в 2.0.1 развилка
по `docs/` снята; ключ настроек `output` существует и сегодня (`src/config.js:18`, умолчание
`size-report.html`) и остаётся перебивающим. Теперь сказано то, что верно: **имя сменилось**, а адрес —
один ответ пакета (`outputOf`), не имя из настроек.

**1.3.1: порог был один на обе половины и с нахлёстом.** Замер по тегу: `@media (min-width: 900px)` и
`@media (max-width: 900px)` — то есть при ровно 900 px применялись обе, тот самый дефект, который 2.1.0
закрыл, сдвинув узкую половину на 899 px. Сказано и это, и что сетка лежит на `body` (`src/page/app.css`),
а статический артефакт тогда собирался из `src/table.css` (`src/css.js` v1.3.1: `ARTIFACT_CSS + TABLE_CSS`
против `PAGE_CSS`), поэтому выпуск его не касался.

**1.3.0: правило колонок было другим.** Прежний текст называл «крупнейшие файлы, по одному от каждого
расширения» как правило; замер по тегу подтверждает его для v1.3.0 — кольцо по расширениям с потолком
`COLUMN_LIMIT` (двенадцать), а сегодня колонкой становится **каждый** отслеживаемый файл
(`src/project.js`, «A column is a file»). Дописано, что смену сделал 2.3.0, — иначе запись читалась бы как
описание нынешнего поведения. Остальное сверено по коду и подтвердилось: файла настроек может не быть
(`derivedConfig`), вывод берётся из проекта (крупнейшие, журнал — первый из `JOURNALS`, команда починки —
объявленный `sizes` или путь к установленному пакету, ссылка — только GitHub/GitLab, метрики
`['raw','min','tok']`), всё, что не может быть колонкой, названо в `skip` (`generated`), строка вывода в
stderr даёт `--init` (`derivedLines`), а отказ остаётся только у названного `--config`
(`src/config.js:105-118`, код 2; у умолчательного имени отказa нет вовсе).

**1.2.0: числа страницы старятся, число артефакта — нет.** Замер живьём (`pnpm run check:standards`):
фикстура 4 из 4 побайтово, 16 коммитов; паритет 3 из 3; бандл живой истории HEAD `bd6ef9d`, 149 коммитов —
то есть «эталоны не переснимались» подтверждено сегодняшним прогоном. Артефакт фикстуры — те же
**22 112 Б** (`fixtures/synthetic/manifest.json`), а **страница мерилась 59 376 Б тогда и едет вместе с
программой и разметкой самого инструмента**: сегодня она **66 277 Б** (`REFACTOR.md` §0). Так и записано,
вместо прежнего «страница 59 376 Б не переснималась». Остальное подтвердилось: заметка о датчике печатается
всегда, а вердикт важнее (`src/modes.js:41-49` — `code === EXIT.OK ? sensors : code`), правила R-4.23,
R-4.24, R-5.8 существуют, `§54` инструкции — в `worklog/archive/WORKLOG.md`.

**Адреса замерены:** заголовков в диффе нет, ключей в `BLOCKERS.md` — **21** (`B1`–`B3`, `N1`–`N18`),
`R`-адресов в `REFACTOR.md` — **59**, все названные по дереву адреса разрешаются, сторож ссылок
(`test/docs-commands.test.js`) — 4 из 4.

**Остаток:** M25 — 1.1.1 … 1.0.0 (442–661) — последний выпуск несёт ещё разделы `### Что вошло`,
`### Исправлено`, `### Не входит в 1.0.0`; M26 — критерий готовности подплана.
