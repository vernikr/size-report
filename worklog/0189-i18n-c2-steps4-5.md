# 0189 — i18n C2 steps 4–5: the templates/release checks and the minifier/tokens checks

Requested: point 5 of the i18n campaign, the next portion of
`docs/plans/2026-09-16-i18n-english/tests-cli.md` (C2), steps 4 and 5.

Step 4 — `test/templates.test.js` 33 → **0**, `test/release.test.js` 41 → **7**:
- Thirty-three names and messages in the first file, thirty-four in the second.
- Red first by the whole file (both back to their `HEAD` version wholesale → both probes green).
- The seven that stay are **D1's contract**: workflow step names read as exact strings. The measurement
  corrected the plan's own figure — the plan (and `configs.md`) named six (`:62,66,101,114,127,132`) and
  the file has seven; the missing one is `:86`, `step(doc, 'npm поновее (для trusted publishing)')`.
  The workflow has eight step names, so seven of the eight are read.
- Live, measured: renaming one step in `.github/workflows/release.yml` (restored byte-identical) reddens
  `test/release.test.js` at check 2.
- `templates` reads no wording of D1's: it fetches the template **files** by name.

Step 5 — `test/minify.test.js` 72 → **10**, `test/tokens.test.js` 54 → **5**:
- Sixty-one names and messages in the first file, forty-eight in the second.
- What stays is data, named by measurement: the payloads the two files write into their scenarios and the
  **`ru` dictionary reads** — `minify.test.js:124` (`'приближение'`), `:195` (`'esbuild недоступен'`),
  `tokens.test.js:111` (`'1 токен ≈ ' … ' знака'`), `:113` (`'недоступен'`).
- Live, measured the other way round: translating the `ru` side of those phrases in `src/metrics.js:40,55`
  (restored byte-identical) reddens `minify` at check 5 and `tokens` at check 3, so the reads assert the
  `ru` data and must not be translated.
- Red first by the whole file: both back to `HEAD` wholesale → both probes green.

**N33, and the repair of its own commit.** The translation made an accepted clone pair *new* for the `dup`
sensor (`✗ dup: new clones 4`: `minify.test.js:252 ↔ tokens.test.js:158` and `:260 ↔ :165`; with the two
files as at `HEAD` the same pairs are accepted — `clones 7, lines 42`, measured), because a fingerprint is
a shape and the words inside it had changed. Put to the user rather than decided here; the answer was to
**take the shared part out**: commit `897a780` gave `tools/harness.js` a `draftedRepo(dir, code)` helper
(`initRepo` + the file under test + the two git commands + `--init` + the assertion) and both checks call
it, so the twin left by removing the duplication — `clones 7, lines 42` → `clones 5, lines 29`, both files
green, no baseline touched. No word was varied to hide the twin.

Measured after the portion: check counts unchanged (3/3/9/7 there; **70** checks in the fast run and
**175** in the full one); `dup` green; the artifact's digest `1bdb27e1…` and the sha256 of the reference
file itself `cdda8d01…` both unmoved; the built page 66 427 B; `verify:fast` and the full `verify` (8
steps) green. No gate file in either commit, so no `Gate-Change:` trailer.

Also corrected in this portion, both by the counter rather than by reading: C2's steps 2–3 turned **125**
lines English (not 116), so the owner read **537** (not 546) before steps 4–5 — the row of `plan.md`, the
step entry in `tests-cli.md` and the journal entry `0187` now say so.

Left in C2: steps 6–8 (`doctor` 82, `check` 78 + `disk` 58, `hook` 109) — 359 lines in all. Left in the
campaign: C2, C3, D1. Open with the user: N19, N20, N21, N24, N31, N32.
