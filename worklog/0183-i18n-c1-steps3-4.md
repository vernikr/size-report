# 0183 — i18n C1 steps 3 and 4: the data contract, and the panel's tree

**Request.** Point 5: steps 3 and 4 of subplan C1 (`tests-page.md`) — `test/contract-data.test.js` and
`test/page-tree.test.js`. Only literals; the number and composition of the checks stay put.

**What moved — 104 lines, and both files now read 1 each.** `test/contract-data.test.js` 51 → **1**: fifty
names and messages, the N24 message at `:47` among them. `test/page-tree.test.js` 55 → **1**: fifty-four
names and messages. The line left in each file is **a pin, not a stale expectation**: `f.label ===
'заметки.md'` is the fixture's own file label, and `/не измеряется: /` is `appUi.notMeasuredRule`'s value in
the `ru` dictionary the report under test actually carries.

**Red first, one phrase at a time: all 104 stayed green** when put back to Russian on their own, with
`node --test test/contract-data.test.js test/page-tree.test.js` answering the same both ways. No reader of a
check's words anywhere, no negative match over Cyrillic text in either file, and no old name appears outside
its own file (measured).

**The three data sides were proved by experiment, not by reading.**
1. **The fixture's label.** Translating `'заметки.md'` reddens `contract-data` (check 7): the label finds
   the md column, and the fixture's file names are its own frozen data.
2. **The dictionary read.** Replacing `:128`'s regex with the English dictionary value (`/not measured: /`)
   reddens `page-tree` (check 2): the report under test is the `ru` one, so the tooltip carries the Russian
   sentence and only a Russian pattern matches it.
3. **N24's price, re-measured live.** The line `:47` carries a message that is C1's own while the values it
   compares are the gated ones. Changing the **message** keeps everything green; translating the **source**
   of those values (`SKIP_WORDS` in `src/history.js`, one word) reddens `contract-data` ("the contract
   carries the same truth as the frozen reference") **and** `test/parity.test.js` ("--json byte-identical to
   the reference") in the same run — exactly the consequence `BLOCKERS.md` N24 documents, now measured
   rather than quoted. The source was restored byte for byte.

**Counters.** The nine files of the owner 310 → **206**; `contract-data.test.js` 51 → 1, `page-tree.test.js`
55 → 1. Check counts unchanged (8 and 5; 44 over the nine files, 70 in the fast run and 175 in the full one).
Untouched: the golden comparison itself, the frozen name list of `test/api.test.js`, the fixture's label and
dictionary text, `SITES` 27, `PRINTED` 2/2/1/2/4, `CASES` 38. References stand: `check:standards` green, the
artifact hash `1bdb27e1…`, the built page byte-identical (66 427 B), the package's report the same. `dup`
green (8 clones, 47 lines, 15 fingerprints) over 104 fresh messages.

**No gate file in this portion**, so no trailer; checks do not ship, so no release.

**C1 is not closed** — steps 3 and 4 of the plan's eight. Left: `page-choice` 67 (step 5), `page-view` 72
(step 6), `module` 58 of its own (step 7), the six data lines of the passed files and the verification
commit (step 8), whose three reads were measured in line by step 0.
