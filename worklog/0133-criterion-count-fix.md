# Критерий подплана markdown: число строк приведено к замеру

**Запрошено:** поправить в подплане расхождение замера и записи — команда критерия даёт 9 файлов и 133 строки
против записанных 132, `comments.md` 3 строки вместо 2; назвать там же, откуда взялась лишняя строка, показать
замером, что группы сходятся с итогом, и ничего больше не трогать.

**Что исправлено.** В `docs/plans/2026-09-15-docs-rework/markdown.md` три числа в двух записях: итог критерия
**132 → 133** (запись M26 и раздел критерия), группа планов **41 → 42** (`comments.md` 2 → 3) и там же названа
причина — третья строка `comments.md` это цитата печатаемой причины отказа разборщика (`src/parse.js`),
добавленная хвостовым проходом комментариев (**коммит `3636723`**) уже **после** замера M26. Новых
кириллических строк в подплан не добавлено: причина названа словами и адресом файла, а не цитатой, поэтому
`markdown.md` остаётся на 37 строках.

**Замер после правки (та же команда):** `rg -cP '[\p{Cyrillic}]' $(git ls-files '*.md' ':!worklog/*')` —
`BLOCKERS.md` 10, `CHANGELOG.md` 15, `README.md` 4, `REFACTOR.md` 15, `comments.md` 3, `markdown.md` 37,
`plan.md` 2, `fixtures/parity/README.md` 15, `fixtures/synthetic/README.md` 32; **файлов 9, строк 133**.
Сходятся и виды: цитаты 15 + 10 + 4 + 2 = 31, данные фикстуры 13, эталоны 32 + 15 = 47, планы 37 + 2 + 3 = 42 —
**31 + 13 + 47 + 42 = 133**, ни одна строка не осталась без причины. Эта арифметика записана в критерий
отдельным пунктом, чтобы следующий проход не пересчитывал её на глаз.

**Журнал `0131` не правится:** в нём остаётся число, измеренное тем проходом (**132**), — это запись о том,
что было сделано тогда, а не сводка на сегодня; расхождение объяснено в самом подплане.

**Чего не делал:** код, сторожа и другие документы не тронуты; причина, по которой записка паритета
(`fixtures/parity/README.md`) остаётся русской, в этом проходе названа не была — по коду её сравнивают не
побайтово (набор сравнения у паритета — `data.json`, `config.json`, `artifact.sha256`), а пишет её само снятие
эталона; это отдельная запись и отдельный проход.

**Проверки:** `pnpm run verify:fast` зелёный (5 шагов, 72 проверки из 177). Выпуска нет: план и журнал в
тарболл не входят.
