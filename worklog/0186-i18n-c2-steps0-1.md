# 0186 — i18n C2 steps 0–1: re-measure, then the two refusal checks

Requested: point 5 of the i18n campaign, the first portion of `docs/plans/2026-09-16-i18n-english/tests-cli.md`
(C2 — the checks that drive the tool).

Step 0 — re-measure, done 2026-09-16:
- The fourteen files answer **708**, not the planned 739; the whole drop is the owners' earlier steps
  (`hook` 113 → 109, `check` 84 → 78, `doctor` 83 → 82, `cli` 79 → 74, `minify` 75 → 72, `disk` 68 → 58,
  `cli-paths` 31 → 30, `refusals-catalog` 26 → 25).
- Joint lines checked **by line number**: 58 named lines, **none carrying Cyrillic** — including the
  data reads the plan exempts (`minify.test.js:124,195`, `tokens.test.js:113`).
- All four negatives of the plan's section (2) are English and live: `check.test.js:184` → `/fix:/`,
  `:235` → `/is not a revision name and not the start of a sha/`, `disk.test.js:219` →
  `/carrying the state/`, `cli.test.js:69` → `/settings derived from the project/`. No owner missed its
  own, so nothing was recorded in `TODO.md` or `BLOCKERS.md`.

Step 1 — `test/refusals-catalog.test.js` 25 → **0** and `test/refusals.test.js` 25 → **4**:
- 21 names and messages in the first file (two of them diagnostic labels for a scratch engine), 25 in
  the second. The four that stay are the scenarios' own fixture content: `echo чужой` inside a foreign
  hook, the note file `заметка.txt`, the side branch's commit subject, the comment appended to the
  drifted table.
- Red first, both halves of the step's experiment, each on its own and each restored byte-identical:
  a source's advice marker put back to Russian (`src/check.js:121`) reddens `test/refusals.test.js` at
  `«the codes of the table / coverage is incomplete»: в отказе нет «fix: add these paths as a column or
  to "skip"»`; one `must` phrase renamed in `tools/refusals.js` alone reddens at
  `«command line / unknown flag»: в отказе нет «a phrase that is not printed at all»`.
- The files' own words are read by nobody: all **39** translated phrases went back to Russian one at a
  time (19 runs of the refusals file, 20 of the catalogue) and neither reddened once. The catalogue's
  `coveredBy` entries point at `test/hook.test.js`, `test/module.test.js` and `test/disk.test.js` —
  never at these two — measured.

Measured after: check counts unchanged (1 and 4); **70** checks in the fast run, **175** in the full
one; `dup` green over 46 fresh messages (8 clones, 47 lines, 15 fingerprints); artifact digest
`1bdb27e1…` and the built page 66 427 B unmoved; `verify:fast` and the full `verify` (8 steps) green.
No gate file in the portion, so no `Gate-Change:` trailer.

Left in C2: steps 2–8 (662 lines over twelve files). Left in the campaign: C2, C3, D1. Open with the
user: N19, N20, N21, N24, N31, N32.
