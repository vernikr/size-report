# C2 steps 6–7: `doctor`, `check` and `disk` speak English

What was asked: the next portion of point 5 — steps 6 and 7 of `tests-cli.md` (C2), translations only,
one commit, English message and journal entry, full `verify` before the push, CI checked afterwards.

## Done

- `test/doctor.test.js` **82 → 10**, `test/check.test.js` **78 → 2**, `test/disk.test.js` **58 → 16**;
  the owner C2 reads **169** (was 359). 190 names and messages turned English.
- The leftovers are the scenarios' own fixture content, named file by file in `tests-cli.md`: the code
  comments and commit subjects the checks write, which the tool merely trips over.
- No regex literal with Cyrillic is left in any of the three files (measured by extracting them).

## Measured, not argued

- **Red first, whole file** (the method steps 2–5 used): each of the three put back to its `HEAD` version
  left its own probe green — `doctor` re-run after this portion's extraction, so the evidence covers the
  file as it stands. Restorations verified byte-identical.
- **The three negatives of the plan's section (2), each by mutation, sources restored byte-identical:**
  - `disk.test.js:219` — clean tree: code 0, two-line stderr, the phrase absent; with the empty state
    compared as lost (`src/history.js`) the same scenario answers code 1 and `carrying the state` is in
    stderr. The regression reddens `:218` first, since the refusal precedes the negative.
  - `check.test.js:235` — the sentence merged into the sibling refusal (`src/explain.js`) reddens `:235`
    with `a commit that exists is called non-existent`.
  - `check.test.js:184` — a repair printed without names (`src/explain.js`) reddens `:184` with
    `the text carries a repair command without names`.
- Checks unchanged: 11/13/6 in the three files (3 in `cli-paths`), **70** fast and **175** full.
- The artifact's **content** digest (`fixtures/parity/artifact.sha256`) is `1bdb27e1…` and the reference
  file's own sha256 is `cdda8d01…`, both unmoved; the built page 66 427 B; `check:standards` green.
- Full `pnpm run verify` (8 steps) green before the push; gate files none (the hook said so), so no
  `Gate-Change:` trailer; `dup` green.

## Suggested

- **N34** written into `BLOCKERS.md`: the translation re-formed the clone pair step 3 had dissolved
  (`cli-paths.test.js:28` ↔ `doctor.test.js:176`, 6 lines / 52 tokens), answered by N33's rule — the shared
  part taken out (`shallowClone` in `tools/harness.js`) rather than a word varied or the baseline edited.
  Measured before and after: `clones 5, lines 29`.
- What is left in C2 is step 8 (`test/hook.test.js`, 109 lines); the campaign then has C3 (226) and D1
  (127) ahead.
