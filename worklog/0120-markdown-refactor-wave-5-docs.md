# 0120 — документация: волна 5 и хвосты, `REFACTOR.md` закрыт (M16e)

**Запрос.** Пройти §7–§9 `REFACTOR.md` (≈72 русские строки) вместе с пятью строками закрытых частей, которые
называли размер собранной страницы константой, и тем самым закрыть файл целиком: каждое утверждение проверить
по коду и живому прогону, сжать, перевести; держать правило цитирования из §1; замерить ссылки до и после.

**Числа.** 94 добавленных / 96 удалённых = **190 изменённых строк в одном файле** при бюджете 600; файл 254 →
252 строки. **Кириллицы в `REFACTOR.md` не осталось ни строки, кроме названных цитат** — 16 строк, и все они
данные инструмента: имена причин отказа (`нет git`, `не git-репозиторий`, `коммит вне истории`, `причина отказа
не объявлена`, `нечем звать инструмент`, `файл не JavaScript`), печатные разделы справки (`Команды:`,
`Режимы:`, `Причины отказа кодом 2`), два русских слова, которые держит сторож журнала выпуска (`### Что
изменится в числах` и ячейка `Файл`), печатные строки прогонов («цель по времени не объявляется», «ЦЕЛЬ НЕ
ДОСТИГНУТА»), цитата стека мутации, отказ старой ревизии на `tok` и строка подсказки `--init`. `verify:fast`
зелёный (5 шагов, 72 проверки из 177), гейт-файлов в коммите нет. Выпуска нет: файл в поставку не входит.

**Четыре утверждения устарели, и все проверены кодом.** (1) Начало §7 называло режим **`--page`** и путь до
него — того же класса, что R-0.2: такого режима нет, режимы сегодня `--init`, `--write`, `--data`, `--json`.
(2) `R-5.2` держала историю бюджета и ссылалась на таблицу целей в §3, а целей нет с 2026-09-15 (R-5.8):
оставлена как история, а действующее состояние названо замером (быстрый прогон 72 из 177, 4,6 с). (3) `R-5.7`
говорила, что CI зовёт `pnpm test:all`, — сегодня CI зовёт **одну** команду `pnpm run verify`; счёт наборов
тоже сдвинулся: 20 файлов и 72 из 177 в быстром, 38 файлов и 177 в полном. (4) §8 называл командами
`init/measure/render` и держал в списке «что не в этом плане» то, что уже есть (автообновление отчёта по
коммиту, шаблоны с их CI) либо решено иначе (семейство токенов одно — `openai`, R-5.6); поверхность названа
по справке. Правила категорий и правда живут в движке (`src/data.js`), а полка категорий в панели уже есть
(`src/page/panel.js`).

**Размер страницы — не константа, и моё прежнее объяснение было неверным.** Отчёт в `pack:check` собирается
**по замороженной фикстуре** (`BUNDLE` и `CONFIG` в `tools/harness.js`), поэтому размер едет за программой и
разметкой самой страницы, а не за деревом репозитория: 37 193 / 54 772 / 59 358 / 59 376 Б в прежних строках,
**66 277 Б** на 2026-09-16. В M16c и M16d я записал, что проверка собирает страницу по этому репозиторию, —
это ошибка, и она исправлена здесь: правило стоит один раз в строке `pack:check` §0, обе строки (`R-4.21`,
`R-4.22`) и `R-4.10` ссылаются на него, а **пять строк закрытых частей** (`R-1.4`, `R-1.5`, `R-1.6`, `R-1.7`,
`R-2.7`) больше не называют число — принимают страницу побайтово. Журнал 0119 исправлен там же.

**Два сторожа поймали мою же правку, и оба раза я чинил документ.** (1) Безымянная ссылка «§0» на строке,
где раньше назван `README.md`, разрешается в раздел README — три раза; поэтому указатель теперь называет
строку про `pack:check`, а не раздел. (2) «requirement §8.2 of `module-design.md`» сломало правило «имя сразу
после ссылки» и упало в `requirements.md` — стало «§8.2 `module-design.md`, a requirement». Сторожа не
ослаблялись, правила не расширялись.

**Два осиротевших русских ряда нашлись в §5** — остатки снятой таблицы бюджета (с целями «≤ 18 с», «≤ 28 с»),
стоявшие сразу под абзацем о том, что таблицы убраны. Удалены; переиспользуемый вывод из них (цена нового файла
берётся парным прогоном) в файле уже записан правилом.

**Сверено по коду и замерам, а не пересказано.** `R-5.1` («один проход по истории на вызов») оставлена **без
✅** сознательно: сегодня истории строится по одной на режим (`src/modes.js`, `src/data.js`), а внутри прогона
замер запоминается по содержимому (`src/history.js`), но условие приёмки — «время режима не выросло», а с
отменой целей (R-5.8) сравнивать не с чем. `R-5.3` подтверждена кодом: `tools/parity-live.js` держит клон на
среду и объясняет, почему общий клон — гонка. `R-5.4`: цитата стека оставлена дословно и без кодового
фрагмента; в тарболле сегодня 31 исходник. `R-5.5`: по таблице фикстуры из `CHANGELOG.md`, которую сверяет
`test/changelog.test.js`, сжатие меньше упрощения в **4 колонках из 10** и равно в 6, ни разу не больше; итог
**2511 / 1597 / 1483** (raw→min −1028, strip→min −114); черновик `--init` несёт `minify` и `metrics`;
`src/minify.js` сегодня 68 строк. `R-5.6`: токены перемерены на `src/code.js` фикстуры — **168** в `o200k_base`
и **196** в `cl100k_base`; разброс байт на токен 2,56…6,30 подтверждён таблицей; а **число колонок страницы
заменено замером** (jsdom, 2026-09-16): с двумя метриками фикстуры шапка несёт **34** ячейки, с одной — **23**
(11 на метрику = файлы + итог), и выключение метрики пишет выбор в адрес. `R-5.7`: 20 + 18 = 38 файлов,
`tools/suites.js` 99, `tools/run-tests.js` 181, `test/suites.test.js` 77. `R-5.8`: бюджетов в коде нет
(`rg BUDGET|LIMIT|MEASURED` пусто), `test/docs-numbers.test.js` секунд не читает, проверок сегодня 177. §8:
`dist/` в дереве нет. §9: адреса найденных аудитом пунктов сверены, `BLOCKERS.md` §B3 закрыт.

**Входящие ссылки замерены до правки и не сдвинулись:** снаружи файла цитируются 58 разных `R`-адресов, ни
одного неразрешённого, перенумеровано ноль. Единственный неразрешённый ключ реестра — `§B7` в строке
архивного журнала: это свидетельство прошлого действия, а не действующий указатель.

**REFACTOR.md закрыт целиком** (M16c + M16d + M16e). **Предложено.** Дальше по очереди подплана — `PLAN.md`
(M17–M22), где §8.4 и §8.5 всё ещё описывают неопубликованный пакет, семь шагов CI и приватный проект.
