# 0169 — i18n W1 steps 5 and 7, and what holds 6 and 8

**Request.** Point 5, portion of the turn: step 5 (`tools/docs-facts.js` — a comment and two Russian
names) and step 7 (`tools/synthetic/repo.js`), plus a measurement of what exactly holds steps 6 and 8.
Only literals; N29, N30, N21 and N20 are the user's decisions and were not approached. Full `verify`
before the push.

**Step 5 — closed by measurement, nothing to translate, no file edited.**

- Measured: `tools/docs-facts.js` reads **3** Cyrillic lines and all three stay. The comment at `:170`
  quotes the pinned revision's section name as the very thing the code scans; `:182` and `:184` carry
  **both** spellings already (`'Команды:'` or `'Commands:'`, `'Режимы:'` or `'Modes:'`) — the English
  half this step was to add came in with S1's joint edit.
- **Why the Russian half is live rather than tolerated:** the pin of `README.md:593` is `v2.4.0`, and
  `git show v2.4.0:src/refusal.js` holds `'Команды:'` at `:99` and `'Режимы:'` at `:111`.
- **The plan's red experiment, run:** with the Russian alternative taken out (what translating it means),
  `node --test test/docs-pin.test.js` answers `у ревизии «v2.4.0» нет справки с разделом «Команды» —
  она старше того, чему учит текст`.
- **And the question about the standards sensor is answered by measurement: nothing.** None of the four
  instruments under `pnpm run verify` imports `tools/docs-facts.js`; its readers are eight files under
  `test/**`, and only the pin guard reads a revision older than the rename.
- `git status` while closing the step named no change to the file: it is byte-identical to the tree.

**Step 7 — `tools/synthetic/repo.js:74`, 1 → 0.** The thrown message of the merge the fixture builder
expects to conflict, now `the merge in the fixture went through without a conflict`. Red first and taken
**live** rather than by grep: a clean merge was put in front of `mergeConflicted` in a scratch repository
whose two branches merge without a conflict, and the builder answered with exactly that sentence.
Measured: the message stands in **no** file of `fixtures/**`, so it reaches no frozen byte;
`check:standards` re-took both references green, `fixtures/parity/artifact.sha256` still reads
`1bdb27e1…`, and the built page is byte-identical to the committed one (`git status` names no change to
it — the hook rebuilds it per commit, so its hash tracks the commit list rather than this portion's
text). `pnpm run dup` answered no new twin.

**What holds steps 6 and 8 — the measurement asked for.**

- **Step 6** is held by **N21 alone** and by nothing else: of the 31 Cyrillic lines of
  `tools/parity-freeze.js`, **16 are `manifestNote` (`:98-118`)** and 15 are this owner's console lines;
  of the 17 of `tools/make-fixture.js`, **3 are the fixture's config values (`:56`, `:57`, `:64`)** and
  14 are console. The 19 allowed ones are the fixture's own data and permanent; the other 29 are waiting
  on a portion, not on a decision.
- **Step 8** is held by **nothing**: its condition holds by measurement (no `src/**` module prints a
  Russian marker since S5, and the sensors' two markers are never parsed by the extractor), N29 and N30
  touch `firstDiff` and `plural` rather than the catalogue, and the tree is green around it.

**Counters.** `tools/synthetic/repo.js` 1 → 0, `tools/docs-facts.js` 3 → 3 (unchanged), the twelve files
of this owner 115 → **114**, `tools/**` 317. `SITES` 27, `PRINTED` 2/2/1/2/4, `CASES` 38, 70 fast /
175 full checks, artifact 66 277 B byte-identical, both references reproduced, full `verify` green.

**Left in W1.** Step 6 (29 lines, 19 of them N21's) and step 8 (the catalogue's 55 lines with the
narrowing of `ADVICE_LINE`), plus N29's six literals — three here, three in `tools/parity-live.js` — and
N30's one line. The two decisions are still on the mission agent's table.
