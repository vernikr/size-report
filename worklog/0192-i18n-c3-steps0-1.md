# C3 steps 0–1: the guards' owner measured, and its two shape readers translated

What was asked: the next portion of point 5 — step 0 of `tests-guards.md` (C3), which settles the
owner's own number, and then its first step. Translations only, one commit, English message and
journal entry, full `verify` before the push, CI checked afterwards.

## Step 0 — the number and the moved lines

- The fourteen files answer **222**, not the planned 226: `docs-commands` 23 → 22, `parity` 16 → 15,
  `gates-coverage` 16 → 14, everything else as written in the plan.
- **All nine reads of W2's verdicts are English** (`gates-files` 77/106/115, `gates-dup` 72/88,
  `gates-coverage` 49/63, `gates-deps` 48, `gates-metrics` 144 — patterns in the tracker's row), and so
  are S1's readers: `usageCauses()` splits on `'Causes of a code-2 refusal'`, `docs-commands:147` reads
  `/refusal cause is not declared/`, `parity:57` reads `/: (\d+) rows × (\d+) files/`. Nothing was
  missed, so `TODO.md` gets nothing.
- **The allow-list's quotation row re-measured as the plan demanded: 33, all of it `BLOCKERS.md`;
  `README.md` and `AGENTS.md` read 0.** `README.md` is empty of Cyrillic altogether — its code-2 row is
  a contract (S1), its `!` note S4's, its hook subject S5's.
- The tolerance stays **dead in the tree**: `docs/requirements.md` and `docs/module-design.md` carry no
  Cyrillic, and the only Russian instances of the three regexes are the file's own comment (`:155`).
- No negative match over Cyrillic in this owner (re-measured).

## Step 1 — `docs-paths` and `git-pins`

- `test/docs-paths.test.js` **6 → 0**, `test/git-pins.test.js` **12 → 5**; the owner C3 reads **209**.
- **`docs-paths` red first, measured:** a row removed from the file table of `README.md` reddens check 2
  (`the tree holds files that are missing from the table of README`), the table restored byte-identical.
- **`git-pins` red first, measured, and more interesting than the plan expected:** the pin taken out of
  `GIT_PINS` (`'core.quotePath=false'`, restored byte-identical) reddens check 2 **only where the
  machine's settings do not stand in the way** — this machine already carries `core.quotePath=false`
  globally, so the ordinary run stays green, while the same run with `GIT_CONFIG_GLOBAL=/dev/null` and
  an empty `HOME` goes red with `the shared reading quoted a non-English path`. B1's own note, met from
  this side.
- **The Cyrillic file name is load-bearing, proved the other way round:** made ASCII it reddens *even
  with the pin in place*, with its own verdict `the unpinned reading handed the path back as it is: the
  pin then proves nothing`. So the five lines that stay are the witness's data, not decoration.
- Both files at their `HEAD` version ran green (2 of 2 each) → no machine reads their own words.
  `dup` before and after: `clones 5, lines 29` both times. Checks unchanged (2 each; 70 fast, 175 full).
  Neither file is a gate file, so no `Gate-Change:` trailer.

## Suggested

- The owner's remainder now splits in two, and it is written that way in the tracker: the **data** (the
  fixtures' own contents and names, the witnesses' Cyrillic paths and identities) and **D1's debt** (the
  workflow step names and the `.gitignore` reader, which C3 verifies rather than edits). The six
  gate-file steps of C3 carry the trailer; step 2 is the first of them.
