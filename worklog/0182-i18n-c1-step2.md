# 0182 — i18n C1 step 2: the frozen list, the fixture's subject, and the words around them

**Request.** Point 5: step 2 of subplan C1 (`tests-page.md`) — `test/api.test.js` and
`test/contract-derived.test.js`. Only literals; the number and composition of the checks stay put.

**What moved — 21 of 22 lines.** `test/api.test.js` 4 → **0**: the test's name and the three messages around
the frozen list of exported names. `test/contract-derived.test.js` 18 → **1**: seventeen names and messages
("the page totals agree with the artifact totals", "what is switched off takes part neither in the table nor
in the total", "the delta of a total equals the sum of the deltas over the files" and the sentences beside
them).

**Red first, one phrase at a time: all twenty-two stayed green** when put back to Russian on their own, with
`node --test test/api.test.js test/contract-derived.test.js` answering the same both ways — no reader of a
check's words. None of the old names appears outside its own file, and neither file carries a negative match
over Cyrillic text (measured).

**The two data sides were proved by experiment rather than by reading.**
1. **The frozen list is data.** Editing one name in it (`'CATEGORY_EXTS'` → `'CATEGORY_EXT'`) reddens the
   check — the list is compared with the module's real exports, so the one place in this owner where the
   wrong target looks like the right one is guarded by the check itself. The list was left byte for byte.
2. **`['fixture: удаление файла']` is a pin on the frozen copy.** Translating it reddens the check: the
   string is the fixture's own commit subject, pinned in `fixtures/synthetic/golden.json:468` and
   `manifest.json:64`, written by `tools/synthetic/history.js:91`. So the one remaining Russian line of this
   step is a pin, not a stale expectation — the distinction the mission asked to state, and it is recorded
   here rather than left to a reader's guess.

**Counters.** The nine files of the owner 331 → **310**; `api.test.js` 4 → 0, `contract-derived.test.js`
18 → 1. Check counts unchanged (1 and 5 checks; 44 over the nine files, 70 in the fast run and 175 in the
full one). Untouched: the frozen name list, the golden comparison of `test/contract-data.test.js`, the
fixture's subject, `SITES` 27, `PRINTED` 2/2/1/2/4, `CASES` 38. No reference moved: `check:standards` green,
the artifact hash `1bdb27e1…` stands, the built page is byte-identical (66 427 B). `pnpm run dup` green
(8 clones, 47 lines, 15 fingerprints) — twenty-two fresh messages and no new twin.

**No gate file in this portion**, so no trailer (the hook confirmed); checks do not ship, so no release.

**C1 is not closed** — it is step 2 of the plan's eight. Left: `contract-data` 51 (step 3), `page-tree` 55
(step 4), `page-choice` 67 (step 5), `page-view` 72 (step 6), `module` 58 of its own (step 7) and the
verification commit (step 8), whose three reads were already measured in line by step 0.
