# 0125. PLAN.md §5, шаги 3–6: конец раздела, заголовки `Step N` (M20 подплана markdown)

## Просили
Продолжить подплан `docs/plans/2026-09-15-docs-rework/markdown.md`: M20 — `PLAN.md` §5, шаги 3–6 (от 617-й
строки до конца раздела) — со сверкой каждого шага по коду и живому прогону, сжатием и переводом на
английский; заголовки шагов привести к форме `Step N`, на которую указывают ссылки снаружи; замер входящих
ссылок на §-номера и ключи обоих реестров до и после; `worklog/**` русский.

## Сделано
- §5 закрыт целиком: его голова и все семь шагов — по-английски. 158 добавлено / 171 удалено = **329
  изменённых строк в одном файле** при бюджете 600; файл 1162 → **1150** строк, кириллицы в нём 421 → **281**,
  в диапазоне **ни одной** (первая кириллическая строка теперь 778 — §6, соседний проход). Сторожа документации
  8 из 8, `pnpm run verify:fast` зелёный (5 шагов, 72 проверки из 177). Выпуска нет: файл в тарболл не входит.
- Заголовки шагов 3–6 переведены в `Step N` (были «Шаг N») — та форма, которой их называют снаружи, — и вместе
  с ними ушли четыре метки, под которыми переезд планировался (`v0.3.0` … `v0.6.0`): первый выпуск был
  **`1.0.0`** 2026-09-14.
- Записан журнал прохода; правка предыдущей сессии (она умерла, не закоммитив) перепроверена замером целиком.

## Что оказалось неверным
- **Потребитель берёт пакет по тегу, а не по коммиту:** его манифест читается
  `"size-report": "github:vernikr/size-report#v1.2.0"` — прежний текст называл коммит `d63468d`. Замер: `tools/`
  у потребителя пуст, `tests/size-table.js` нет, а `tests/harness.js` запускает команду пакета **волной 1**.
- **Три красных сторожа на моём же тексте, все починены в документе, ни один сторож не ослаблен:** безымянная
  `§10` на строке, где ближайшее имя — `README.md` (сторож читал её как ссылку на README, где §10 нет) — фраза
  перенесена на строку без имени; спецификация потребителя, записанная одиноким код-фрагментом с `#v1.2.0` на
  конце, задела сторожа путей (хвост `.0` делает последнюю часть похожей на имя файла) — теперь она **цитируется
  строкой манифеста**, как и есть. Первую поймало чтение перенесённой цитаты, добавленное в M18a.

## Что сверено по коду и прогонам
- `MINIFY_ENGINES = ['strip', 'esbuild']`, `strip` — умолчание; JSX/TSX намеренно вне `MINIFY_LOADERS` с
  причиной в коде (`src/minify.js`, `src/metrics.js`); `stripHtml` есть, а разметка идёт упрощением.
- Проверок: `test/minify.test.js` **9**, `test/tokens.test.js` **7**, `test/hook.test.js` **11**,
  `test/environment.test.js` **4**, `test/templates.test.js` **3**. `CHARS_PER_TOKEN = 3`.
- Порядок важности у `doctor` — порядок ключей `WEIGHT`, коды **2 → 3 → 1 → 4** (`EXIT` в `src/refusal.js`).
- Шаблоны — те три файла, что названы, держит `pack:check`; CI — **одна работа с `pnpm run verify`**; «умолчание
  — полный прогон» — фраза из шапки `tools/suites.js`, причины быстрых файлов стоят построчно.
- Требования подтверждают обещания: §3.1 называет **два движка и откат**, §4.2 считает пропущенное изменение
  **нарушением**, §7.1–§7.3 держат весь хук (самоустановка, плумбинг, невозможность зацикливания по устройству,
  три выключателя), §8.2 требует **MAJOR с миграцией** при смене формата.
- `test/changelog.test.js` сверяет таблицу чисел с живым прогоном; шаблон `CHANGELOG.md` даёт `code.js`
  735 / 276 / 185 / 168 — те же числа, что стоят в тексте.

## Входящие ссылки
35 строк вне плана называют `§5` или шаг (шесть документов: 27 «step N», 12 «§5»), ни одна не потеряна; четыре
слепые ссылки, записанные после M18a, стоят на месте; сторож разрешает каждый `§`-адрес шести документов
(8 из 8). Ключей реестров: `BLOCKERS.md` — 19 (B1–B3, N1–N18, где N2 и N10 закрыты и зачёркнуты), `REFACTOR.md`
— 59 `R`-адресов; 34 ссылки на них, неразрешённых нет.

## Предложено
- Дальше по нарезке — M21: `PLAN.md` §6–§9. Там ждут два уже найденных адреса: §8.2 всё ещё планирует переезд
  как `0.1.0` (метка, которой не было), а §8.4/§8.5 описывают непубликованный пакет и семь шагов CI.
- §11 обещает, что метрика `gzip` «удаляется», тогда как реестр её держит и пакет поставляет (§4.4, D4) —
  адрес передан в M22.
