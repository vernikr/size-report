# M23 подплана markdown: голова `CHANGELOG.md`

**Запрошено:** пройти первый кусок журнала выпусков — рамку «что значит изменились числа» и выпуски
2.4.0 … 2.0.1; перепроверить утверждения по коду, сжать, перевести; сторож `test/changelog.test.js`
держит русские слова документа — править его тем же коммитом и доказать опытом.

**Сделано.** `CHANGELOG.md` 184 добавлено / 208 удалено = **392 изменённые строки**, сторож
`test/changelog.test.js` 7 / 7 = **14**, всего **406 изменённых строк в двух файлах** при бюджете 600.
Файл 690 → 666 строк; кириллицы 452 → **260 строк**, из них 13 — строки таблиц с именем файла фикстуры
`заметки.md` (данные, остаются); **в диапазоне 1–261 прозы по-русски не осталось**. Пройдены: рамка,
2.4.0, 2.3.0, 2.2.0, 2.1.0, 2.0.2, 2.0.1 — до строки 261, дальше `## 2.0.0`. `verify:fast` зелёный
(5 шагов, 72 проверки из 177). Выпуска нет: документ и проверка в тарболл не входят.

**Сторож держал не два русских слова, а три.** Кроме заголовка `### Что изменится в числах` (assert) и
ячейки шапки `Файл` (строка шапки иначе уходит в разбор чисел) он сверял **имя итоговой строки
таблицы — `ИТОГО`**: `concat(['ИТОГО'])` в `test/changelog.test.js`. Все три заменены (`What changes in
the numbers`, `File`, `TOTAL`), вместе с шапкой таблиц (`min со strip` → `min with strip`). Ни одна
работающая проверка не ослаблена: разбор строк и сверка с живым прогоном те же.

**Доказано опытом, а не словом.** Сторож с прежним русским заголовком на переведённом документе
краснеет ровно на разборе раздела: `not ok 2` — «в CHANGELOG нет раздела «What changes in the numbers»»;
после правки сторожа 2 из 2 зелёные.

**Структуру сверил замером, а не глазами.** Заголовки `## <версия> — <дата>`, набор `###`, число строк
таблиц и подписи строк до правки и после совпадают (разошлись только переведённые заголовки), поэтому
ни один внешний адрес сдвинуться не мог; 13 строк данных фикстуры не менялись ни в одном выпуске.

**Проверено по коду:** `bin/postinstall.js` ставит хук через `autoInstall` (`src/hook.js`) и сам называет
платформенную разницу (pnpm 10, yarn berry); путь отчёта по умолчанию — `docs/size-report.html`
(`src/project.js:94`), каталог создаёт `src/artifact.js:21` (`mkdirSync recursive`); README §6 называет
`install-hook`/`uninstall-hook` и «Ignored build scripts» с `pnpm.onlyBuiltDependencies`; шаблон
`templates/README.md` говорит про первый отчёт. Числа таблиц (2511 / 1597 / 1483 / 540) теперь держит
сторож против живого прогона на фикстуре — они не переписывались.

**Остаток:** M24 — 2.0.0 … 1.3.0 (строки 262–422), M25 — 1.2.0 … 1.0.0 (423–666).
