# M25 подплана markdown: хвост `CHANGELOG.md`, файл закрыт

**Запрошено:** пройти 1.1.1, 1.1.0 и 1.0.0 (442–661), тем самым закрыть файл целиком; каждое число, имя
файла, поле и обещание перепроверить по коду и живой истории; три заголовка вне поля зрения сторожа
(«Что вошло», «Исправлено», «Не входит в 1.0.0») перевести как текст документа; строки таблиц с именем файла
фикстуры не трогать; замер адресов до и после.

**Сделано.** 136 добавлено / 157 удалено = **293 изменённые строки в одном файле** при бюджете 600. Кириллицы
в `CHANGELOG.md` было **161 строка**, стало **15**: 13 — строки данных фикстуры (`заметки.md`) и 2 — **названные
цитаты печатаемого вывода** (отказ «в истории нет коммита «HEAD»» и имя причины «нет git»), больше ничего.
Заголовков выпусков и строк таблиц в диффе нет (0 и 0), поэтому ни один адрес и ни одно число не сдвинулись.
Поскольку `CHANGELOG.md` — один из шести документов, которые читают сторожа (`DOCS` в `tools/docs-facts.js`),
прогнан **полный профиль, а не только быстрый: 8 шагов, 74,1 с, зелёный целиком**; `verify:fast` зелёный
(5 шагов, 72 из 177), сторож ссылок 4 из 4, сторож журнала 2 из 2. Выпуска нет.

**Три заголовка вне поля зрения сторожа переведены и никем снаружи не называются** — замер: `Что вошло`,
`Исправлено`, `Не входит в 1.0.0` встречаются только в самом файле и в подплане. Сторож журнала (`### What
changes in the numbers`, `File`, `TOTAL`) не тронут: раздел о числах и данные те же.

**Три пункта списка «Не входит в 1.0.0» с тех пор сдвинулись, и теперь сказано куда.** Разделение набора
проверок на быстрый и полный **пришло** 2026-09-14 (`REFACTOR.md` R-5.7: `pnpm test` — 72 проверки из 177,
полный профиль — весь набор), а кэша и профиля на большой истории **нет до сих пор** (`PLAN.md` §9, «still
open»). Лицензионный файл вошёл в 1.1.1 — это сказано в самом 1.1.1. И самое заметное: **в npm пакет тогда не
публиковался**, а сегодня он там (`npm view @vernikr/size-report version` → **2.4.0**) и выпускается тегом из
CI, при этом потребитель по-прежнему берёт его git-ссылкой — замер по его манифесту:
`"size-report": "github:vernikr/size-report#v1.2.0"`. `dist/app.js` так и не поставляется (его нет среди
`files` манифеста).

**Числа перемерены живьём, а не перенесены.** Токены: клон фикстуры со словарём даёт `tok` **540** (точно,
код 0), без словаря — **583** (оценка по длине, 1 токен ≈ 3 знака, код 4), то есть оба числа записи 1.0.0
подтверждены прогоном. Артефакт остался **22 112 Б**, `pnpm run check:standards` переснимает оба эталона
сегодня: фикстура 4 из 4 побайтово (16 коммитов), паритет 3 из 3, бандл живой истории 149 коммитов. Пакет из
тарболла — страница **66 277 Б** (`pack:check` в полном профиле), то есть то самое число, которым в прошлом
проходе заменено устаревшее 59 376 Б. Дельты `esbuild` из таблицы (1597 → 1483, `code.js` 276 → 185,
`style.css` 55 → 43) сторожа держат против живого прогона. `sha256 863ce3e9…` назван тем, чем он был — замером
того прохода (`REFACTOR.md` R-4.19), с оговоркой, что с тех пор таблица потребителя пересобирается его
собственными коммитами. Заодно подтверждено замером, что метки `0.1.0`–`0.5.0` не существовали: тегов `v0.x`
нет вообще.

**Проверено по коду:** `explain` зовёт `resolveCommit` (`git rev-parse`), а вне истории отчёта — отдельная
причина (`src/explain.js`); `tools/refusals.js` и имя причины `нет git` (`src/refusal.js:44`) есть; проверка
настроек требует, чтобы путь колонки был непустой строкой (`src/config.js:139-142`); `--init` без знакомых
расширений не печатает «✗» (`src/init.js:47`); `SIZE_REPORT_NO_OPTIONAL` и её шов (`src/optional.js:14`);
`tool: {name, version}` в данных и ключ памяти страницы строится из паспорта (`src/page/state.js:75`);
`gzip` в реестре есть, а в черновике настроек его нет (`src/metrics.js:101`, `templates/size-report.config.json`);
`LICENSE` в дереве и в `files`; версии необязательных зависимостей — `esbuild 0.28.2`, `gpt-tokenizer 4.0.0`.

**Адреса замерены:** ключей в `BLOCKERS.md` **21** (3 блокера + 18 заметок), `R`-адресов в `REFACTOR.md` —
**59**; ни один не потерян и не добавлен (этот проход документов не касался).

**Остаток подплана:** M26 — критерий готовности; `CHANGELOG.md` из его списка выпал: файл пройден целиком.
