# 0121. PLAN.md: голова и §1–§3 переведены и сверены (M17 подплана markdown)

## Просили
Продолжить подплан `docs/plans/2026-09-15-docs-rework/markdown.md`: M17 — `PLAN.md` §1–§3 (111 русских
строк по нарезке) со сверкой каждого утверждения по коду, настройке и живому прогону, сжатием и
переводом; адреса §8.4/§8.5, которые надо перемерить, а не перенести, перенести в подплан, если они не
попадут в кусок; замер входящих ссылок до и после; сторожа править тем же коммитом, если перевод его
сломает и не ослабляя.

## Сделано
- Пройдены **голова файла (1–20, её не брала ни одна часть нарезки) и §1–§3 (21–175)**: 145 добавлено /
  134 удалено = **279 изменённых строк в одном файле** при бюджете 600. Кириллицы в файле 910 строк →
  **788**; в пройденном диапазоне не осталось **ни одной** (первая кириллическая строка теперь 176 —
  это §4, следующий проход). Файл 1218 → 1228 строк.
- `pnpm run verify:fast` зелёный (5 шагов, 72 проверки из 177), гейт-файлов в коммите нет, трейлер не
  нужен. Правок кода логики нет, дифф — один файл. Выпуска нет: `PLAN.md` в тарболл не входит.

## Что оказалось неверно и чем проверено
- **§1.1–§1.3 сверены по истории проекта-потребителя** (`git show 58cd786^:…`): 1145 / 472 строки,
  27 колонок, 161 строка артефакта — все четыре числа верны. Пять флагов старого инструмента в его
  исходнике есть, `--help` тогда не было (R-0.3). А вот список «ключей, которые надо перенести» называл
  `minify` и `tokens`, которых тогда **не существовало**: это ключи, добавленные самим планом (шаги 3 и
  4). Переписано на одиннадцать ключей того дня (замер) со ссылкой на §4.6.
- **§1.4: колонка обещания заменена замером** по живому проекту-потребителю: `sizes`/`test:sizes` — уже
  `size --write`/`size`; в `tests/harness.js` один шаг вместо двух; `tests/doc-sync.js` счётчиков размера
  не читает; `docs/TESTING.md` — один раздел о пакете; `AGENTS.md` говорит «инструмент поставлен пакетом»;
  `ci.yml` сохранил `fetch-depth: 0`; **в `.gitignore` про отчёт ничего не появилось** — обещанной строки
  нет, отчёт так и лежит в git, и это дело проекта (`--init` не правит `.gitignore`, R-4.8); из трёх
  файлов удалены два, настройки остались под старым именем.
- **§2: имя файла настроек названо неверно.** «Остаётся `size-report.config.json`» — нет: имя по
  умолчанию `size-table.config.json` (`CONFIG_NAME` в `src/config.js`), и файл потребителя его сохранил.
  Исправлено замером. Строка «блок для ИИ-агентов» больше не уезжает — блока нет и он намеренно не
  делался (R-4.11); строка `.size-report/` в `.gitignore` заменена тем, что действительно остаётся:
  путь отчёта и решение, коммитить ли его.
- **§3 (14 инвариантов) сверены по коду:** п.3 — контроля «ни одного `git show` за проход» сегодня нет,
  правило держит `test/git-pins.test.js` (ни одного зова git мимо общего списка закреплений); п.4 — ключ
  памяти не «sha + датчик + версия алгоритма», а sha блоба + датчик (`measurer` в `src/history.js`), а
  версионный кэш — план §4.4 (`REFACTOR.md` 185: он не сделан); п.5 — механизм назван (`showSha` в
  `src/data.js`); п.10 — гард назван точнее (один рабочий поток на прогон, R-5.4); п.11 — прочитан
  `assertMatchesDisk`; п.13 — прочитан `GIT_PINS` (четыре закрепления + локаль); **п.14 ссылался на
  `test/contract.test.js`, которого нет** — состав полей стережёт `test/contract-data.test.js`. В §1.6
  все ссылки на требования и проект (§7.3, §8.2, §3.1, §4.2, §6.1, §7.1, §7.2, §12) ведут в
  существующие разделы, а число «12,2 с» заменено тем, что эту слабость закрыло (R-5.7, R-5.8).
- **Сторож поймал мой дефект:** `PLAN.md: §4.9 → requirements.md` — безымянная ссылка `§4.9` на строке,
  где ближайшим именем документа был `requirements.md`, разрешилась в требования. Исправлено в
  документе (теперь `PLAN.md` §4.9), сторож не ослаблялся.
- **Замер адресов:** ключей 69 до и 69 после, ни один не потерян и не добавлен; 112 ссылок, разрешаемых
  сторожем, ни одна не ведёт в `PLAN.md` мимо существующего раздела.

## Найдено вне этого куска
Перемер всех строк комментариев с кириллицей по дереву: **22 строки русской прозы** остались в пяти
файлах, которые подплан `comments.md` объявил пройденными — `src/parse.js` 39, 40, 42;
`tools/docs-facts.js` 28–30, 35; `tools/parity-freeze.js` 91–92; `tools/refusals.js` 204–205, 230–231,
278–279, 289, 299, 303–304, 326–327; `test/minify.test.js` 268. Это не данные (печатаемый вывод цитируют
только две из них и лишь внутри фразы), а проза, то есть работа того подплана: цена — один проход на
пять файлов. В `comments.md` записан остаток с адресами, правка не делалась в этом проходе.

## Предложено
- Взять остаток по комментариям (22 строки, 5 файлов) отдельным проходом, пока он мал.
