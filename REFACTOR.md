# REFACTOR.md — чистка size-report по слоям

Вход — аудит 2026-09-14 (SPEC 8, DESIGN 4, CORRECTNESS 7, QUALITY 3). Здесь **только
чистка**: объём кода и скорость. Что делаем по шагам выпуска — `PLAN.md` §5,
известные дефекты и обходы — `BLOCKERS.md`, номера пунктов ниже ссылаются на них и
не пересказывают.

## 0. Границы

- **Поведение и форма вывода не меняются.** Ни числа, ни порядок строк, ни клетки
  артефакта, ни состав полей контракта.
- **Побайтовый паритет обязателен после каждого пункта** — иначе пункт не закрыт.
- **Никаких новых возможностей внутри рефакторинга.** Захотелось новое — в
  `PLAN.md`, не сюда (список — §7).
- **Один пункт — один коммит**, откат = снять коммит. Пункт, требующий смены
  поведения, выносится из плана, а не «заодно поправляется».
- Комментарий описывает код, а не правку (как в проекте-потребителе).

### Чем доказывается, что ничего не сломалось (три прогона, одни для всего плана)

| Прогон | Ожидание |
|---|---|
| `pnpm test` | 27/27 (число проверок **не может уменьшиться**) |
| сборка артефакта на фикстуре | sha256 `5c0c73e4f2e9…` побайтово |
| `pnpm run parity:live` | 95 строк × 27 колонок, артефакт 225 673 Б, sha256 `1bdb27e14bac…`, две среды |

Что именно сократилось — записывается числом в `WORKLOG.md` (`wc -l` до/после), а
не ощущением.

## 1. Порядок и почему такой

| Волна | Слой | Почему здесь |
|---|---|---|
| **0** | Тупики CLI (`fix:`, не рефакторинг) | 30 минут, закрывает единственный найденный дефект сразу; тексты подсказок правятся один раз, до того как их тронет пункт 1.4 |
| **1** | Движок и CLI | Самый большой объём и все три дублирования; волна 2 и разбиение по файлам зависят от единого слоя, поэтому объединение идёт первым |
| **2** | Страница | Зависит от 1.1: две трети дублирования страницы исчезают вместе с общим слоем, отдельно их чистить значит делать работу дважды |
| **3** | Тесты | Дешёвая страховка для волн 1–2 вводится по ходу, а общий харнесс — после того как файлы движка перестанут переезжать |
| **4** | Документация | Идёт следом за фактом; правило «правка кода тянет строку в доке» действует с пункта 1.1, а не с начала волны |
| **5** | Производительность (движок и тесты) | По прямому требованию: сначала объём, потом скорость. Замеры тестов осмысленны только после того, как прогоны перестанут дублироваться (3.2) |

## 2. Волна 0 — тупики CLI (правка поведения, вне рефакторинга)

| # | Пункт | Приёмка |
|---|---|---|
| R-0.1 | Подсказка починки ведёт к работающей команде (сейчас `--init` советует команду, которая завершается успехом и ничего не создаёт; `BLOCKERS.md` §N2) | Прогон: `node bin/size.js` без конфига → текст называет команду, её выполнение создаёт конфиг |
| R-0.2 | `--page` создаёт недостающий каталог (сегодня `ENOENT` стеком на `--page .size-report/…`) | `node bin/size.js --page .size-report/report.html` в свежем клоне → файл, код 0 |
| R-0.3 | У каждой ветки отказа — справка и код выхода вместо стека (`--help`, отсутствующий конфиг) | Тест на каждый путь; сверка с таблицей кодов `PLAN.md` §4.1 |

Проверки на эти три пути добавляются здесь же: сегодня у `--init`/`--force`
покрытие ровно нулевое.

## 3. Волна 1 — движок и CLI

| # | Пункт | Что сокращается | Приёмка |
|---|---|---|---|
| R-1.1 | **Один слой отчёта на артефакт и страницу.** Сегодня одно и то же выражено дважды: `cell`/`cellNow`/`nowCells`/`commitCell`/`noteText` + `CSS` (≈200 строк) и `APP_MATH`/`APP_DOM`/`APP_CSS` (≈207 строк); совпадают все смыслы. Остаётся один вычислительный модуль и два представления | netto **−150…−250** строк в `src/size-table.js` | три прогона из §0; сверх того — артефакт обязан остаться байт в байт, включая липкую шапку и колонку (расходятся — откат и разбор, **не** «поправить эталон») |
| R-1.2 | **Линтер и формат.** В пакете нет ни одного правила (`package.json` знает только `test`/`parity:live`/`fixture`), из-за этого и проскочила склейка двух операторов в одну строку при переносе (`commitCell`, 1062). Взять набор проекта-потребителя, начать с `indent` (в `PLAN.md` §8.1 линтер уже стоит строкой скрипта — здесь он доводится до правила) | 0 строк (правка качества) | `pnpm run lint` — 0 ошибок; мутация: искусственно сдвинутый отступ валит проверку; артефакт не изменился |
| R-1.3 | **Разбиение движка по файлам** — существует, `PLAN.md` пункт **D1** и схема §4.2. Идёт после 1.1, чтобы файлы разложить один раз | файл 1669 строк → модули ≤250 строк каждый | три прогона из §0; публичный API (`exports`) не теряет ни одного имени — проверка по списку |
| R-1.4 | **Устаревшие тексты и тонкий legacy-путь** — существует частично, `PLAN.md` пункт **D5**: шапка движка предлагает `node tools/size-table.js`, `initMode` печатает советы про `tests/size-table.js` в проекте-потребителе, `check`/`checkMode` сверяют артефакт с историей — смысл, который держится только пока отчёт лежит в git | −40…−70 строк (текст и печать) | `pnpm test`; вывод `--init` проверяется тестом по строкам |
| R-1.5 | **Якорь фикстуры** (`fixtures/legacy/size-table.cjs`, 1145 строк) — на нём стоят 5 из 27 проверок (трио «движок пакета / замороженная копия» и две про ревизию эталона). Пока нужен: им различается «сломался движок» и «съехал эталон». Демотируется до одной проверки, удаляется из дерева на 1.0.0 (байты остаются в истории на `c939def`) | −1145 строк на 1.0.0 | `pnpm test` 27/27 без него: проверки, ссылающиеся на копию, обязаны быть явно вычеркнуты, а не пропущены молча |

## 4. Волна 2 — страница

| # | Пункт | Что сокращается | Приёмка |
|---|---|---|---|
| R-2.1 | **Программа страницы уезжает из строковой константы в модуль** (решение уже принято, `PLAN.md` §4.1: собирается на публикации пакета). Сегодня 184 строки JS живут текстом внутри движка и не линтуются принципиально | −(180…200) строк из движка, столько же в модуль | страница, собранная из модуля, даёт тот же DOM: те же 96 строк на живой истории, те же клетки; `pnpm test` |
| R-2.2 | **Один набор стилей.** Сейчас два: `CSS` и `APP_CSS`, 11 из 23 деклараций страницы дословно совпадают, а липкая шапка есть только у артефакта | −40…−60 строк | артефакт байт в байт (липкая шапка не теряется); страница в jsdom без ошибок |
| R-2.3 | **Одна функция разрядов.** Тот же регексп дважды (`group` и `appGroup`, во втором случае с двойным экранированием) | −5…−10 строк | `pnpm test`: числа артефакта и страницы остаются строками без изменений |

## 5. Волна 3 — тесты

| # | Пункт | Что сокращается | Приёмка |
|---|---|---|---|
| R-3.1 | **Общий харнесс.** `test/parity.test.js` и `test/contract.test.js` держат две копии `ROOT`/`SYNTH`/`BUNDLE`/`CONFIG`/`PACKAGE_BIN`/`MAX_BUF`/`cloneFixture`/`runCli`, и сигнатуры уже разошлись (`runCli` в паритете принимает `env`, в контракте — нет) | −30…−40 строк, один модуль вместо двух копий | `pnpm test` 27/27; одна копия каждой вспомогательной функции (проверяется `grep -c`) |
| R-3.2 | **Не мерить одно и то же несколько раз.** Сейчас один и тот же прогон по фикстуре запускается отдельно под `--json`, `--write` и контрольный режим, и то же трио — для замороженной копии | время, не строки | `pnpm test` 27/27; число проверок прежнее, длительность — числом в `WORKLOG.md` |
| R-3.3 | **Один клон фикстуры на среду** вместо клона под каждую проверку | время | то же |
| R-3.4 | **Тройка замороженной копии демотируется** (её предмет — «эталон снят с той ревизии», а не поведение движка) — идёт вместе с R-1.5 | −60…−80 строк тестов | `pnpm test` 27/27 |

## 6. Волна 4 — документация

| # | Пункт | Что делается | Приёмка |
|---|---|---|---|
| R-4.1 | **Правило, а не пункт:** правка кода тянет за собой строку в `README.md`/`PLAN.md` в том же коммите (шапка движка, список файлов, числа прогонов, `fixtures/`) | — | `grep` по путям из документации: каждый существует |
| R-4.2 | **Инструкция «как подключить инструмент к новому проекту» — место закреплено и не теряется:** `README.md`, раздел подключения; три сценария установки — `PLAN.md` §8.4; исполнение — `size init` в шаге 5 и `templates/` (§4.2); блок для файла агентов проекта — требование §13 | внутри рефакторинга правится только ссылка на неё | ссылка ведёт на существующий раздел; в `PLAN.md` шаг 5 назван как место реализации |
| R-4.3 | Числа приёмки в доках приводятся к факту после каждой волны (сегодня в `README.md` — 27 проверок, четыре среды) | — | совпадает с выводом `pnpm test` и `parity:live` |
| R-4.4 | `REFACTOR.md` этот — в таблицу файлов `README.md` | — | ссылка существует |

## 7. Волна 5 — производительность (движок, затем тесты)

Движок на живой истории уже приемлем: `--page` 107 КБ за **2,9 с**, `--data` 203 КБ,
`--check` зелёный. Поэтому здесь только то, что не меняет поведения, а настоящая
цель — тесты.

| # | Пункт | Приёмка |
|---|---|---|
| R-5.1 | **Один проход по истории на вызов.** Сейчас `--write`, `--page` и `--data` строят историю каждая; внутри одного вызова повторных проходов быть не должно | время каждого режима не выросло; `--page`/`--data` совпадают побайтово с эталоном |
| R-5.2 | **Скорость тестов.** Замер на сегодня: 27 проверок, 14,8 с суммы длительностей; дороже всех «вывод не зависит от настроек и локали» 2899 мс, «выкладка с CRLF» 2015 мс, «правка только на диске» 1521 мс, «контракт воспроизводим» 1294 мс, тройка замороженной копии 2926 мс. `parity:live` — 25 с из двух клонов проекта | цель: `pnpm test` ≤ 7 с, `parity:live` ≤ 15 с при том же числе проверок; числа до/после — в `WORKLOG.md` |
| R-5.3 | **Среды живой сверки — на одном клоне** (различаются переменные, а не содержимое дерева) | `parity:live` зелёный, 95 × 27, тот же sha256, клонов — один |

Кэш «sha + датчик + версия» (`PLAN.md` §4.4) сюда **не** входит: это возможность,
а не чистка.

## 8. Не в этом плане

Следующая возможность отчёта — чтобы её не смешали с чисткой: дерево файлов по
папкам, запоминание выбора в браузере, внешний вид и темы, категории как часть
панели, пре-собранный `dist/app.js` как артефакт публикации, новые команды
(`init/measure/render/check/doctor/explain`) и их семантика, автообновление по
коммиту, шаблон CI, блок для файлов агентов, `esbuild`, токены, удаление метрики
`gzip` (`PLAN.md` пункт **D4**), кэш замеров — всё это `PLAN.md` §5, шаги 2–6.

Отдельно, как мелкие правки поведения (не чистка, но и не возможность) — идут
сразу после волны 0 или вместе с ней:

- вырожденное состояние страницы: при снятых всех метриках шапка получает
  `colspan="0"` (невалидный HTML) и таблица молча остаётся без чисел — нужен
  либо запрет, либо подпись;
- категории и их источник в контракте (`categoryBy`) — уже есть, но правила
  категорий пока живут в движке; переносить их в настройки — шаг 2.

## 9. Что уже установлено и куда отнесено

| Установлено аудитом | Пункт |
|---|---|
| Пять обязанностей в одном файле (1669 строк) | R-1.3 |
| Два рендерера одной таблицы | R-1.1 |
| Два набора стилей одной таблицы | R-2.2 |
| Программа страницы текстом в шаблонной строке (не линтуется) | R-2.1 |
| Дублированная тестовая обвязка с разошедшимися сигнатурами | R-3.1 |
| Нет линтера; отсюда склейка двух операторов в строку (1062) | R-1.2 |
| Поля реестра метрик под `gzip`, которого в v1 не должно быть | §8 (`PLAN.md` D4) |
| Случайная правка рендера, сделанная «заодно» при переносе | R-1.1 (снимается) + R-1.2 (стережёт) |
| Дублирование прогонов в тестах | R-3.2, R-3.3 |
| Якорь фикстуры на 1145 строк | R-1.5 |
