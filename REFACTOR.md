# REFACTOR.md — cleaning size-report by layers

The input is the audit of 2026-09-14 (SPEC 8, DESIGN 4, CORRECTNESS 7, QUALITY 3). What stands here is
**cleaning only**: volume of code and speed. What is done per release step lives in `PLAN.md` §5, known
defects and workarounds in `BLOCKERS.md`; the items below cite them rather than restate them.

## 0. Boundaries

- **Behaviour and the shape of the output do not change.** Not the numbers, not the order of rows, not the
  cells of the artifact, not the fields of the contract.
- **Byte parity is obligatory after every item** — otherwise the item is not closed.
- **No new features inside the refactoring.** Wanted something new → `PLAN.md`, not here (the list is §7).
- **One item, one commit**; a rollback is a removed commit. An item that would need a behaviour change
  leaves the plan instead of being “fixed along the way”.
- A comment describes the code rather than the edit (as in the consumer project).

### What proves that nothing broke (three runs, one set for the whole plan)

| Run | Expectation |
|---|---|
| `pnpm test` (fast) and `pnpm test:all` (full) | all green, and the number of checks **cannot shrink** — while the number itself lives where it is checked: in the table of runs in `README.md` (how many checks each takes), read by `test/docs-numbers.test.js`, with the completeness of the split read by `test/suites.test.js` (§R-5.7). No run has a time target at all (§R-5.8). A second copy of those numbers is absent here on purpose: stated once it ages together with the table, stated twice it ages in silence |
| building the artifact on the fixture | sha256 `5c0c73e4f2e9…`, 22 112 B, byte for byte (untouched by the pass about compression: the `strip` engine is the default) |
| `pnpm run parity:live` | 95 rows × 27 columns, artifact 225 673 B, sha256 `1bdb27e14bac…`, two environments |
| `pnpm run lint:strict` | zero findings over the package's real code (introduced in R-1.2) |
| `pnpm run pack:check` | the built tarball works: the sources arrived, and the numbers, the artifact and the page are as from the repository (introduced in R-1.3) |
| `pnpm run check:standards` | both standards reproduce: our files byte for byte, the history bundle by content (branches, tip, commit count) plus a live `HEAD` of the live bundle; the re-take goes into a temporary directory and is compared with what is committed (introduced in R-4.9, rewritten in R-4.10) |
| `pnpm run verify` and the CI file (`.github/workflows/ci.yml`) | the checks see the same thing: one job runs the whole list above with the same commands on every push and pull request (step 6 of the plan) |
| the wiring instruction, step by step by hand (once per documentation pass) | in a fresh repository the steps of “Wiring it into your project” pass with exactly the commands written there — install, `--write`, `--init`, the project's `sizes` and `test:sizes`, `check`, `doctor`, `install-hook`; every divergence is an item in wave 4 |

What exactly shrank is written down as a number in the journal (`worklog/**`, `wc -l` before and after)
rather than felt.

## 1. The order, and why it is such

| Wave | Layer | Why here |
|---|---|---|
| **0** | Dead ends of the CLI (`fix:`, not a refactoring) — ✅ **done 2026-09-14** | Thirty minutes, and it closes the only defect found at once; the advice texts are edited once, before item 1.4 touches them |
| **1** | Engine and CLI (1.1 ✅, 1.2 ✅, 1.3 ✅) | The largest volume and all three duplications; wave 2 and the split into files depend on a single layer, so the merging goes first |
| **2** | The page | Depends on 1.1: two thirds of the page's duplication go away with the shared layer, and cleaning them separately means doing the work twice |
| **3** | Tests (3.1 ✅, 3.2 ✅, 3.3 ✅) | Cheap insurance for waves 1–2 comes along the way, while the shared harness comes after the engine's files stop moving |
| **4** | Documentation | Follows the fact; the rule “an edit to the code drags a line in the doc” holds from item 1.1 rather than from the start of the wave |
| **5** | Performance (the engine) | By direct requirement: volume first, speed after. The speed of the **tests** was closed by wave 3 (5.2, 5.3, with the measurements there); the engine is what wave 5 has left |

Each item's state is in its own row: the marks in this table are the waves as they stood when the table was
written, while §2–§7 carry the dates and the evidence. Rows stand in the order the items were written, so an
address may sit under a younger one (`R-0.6` under `R-0.7`), while no address is repeated: the two rows that
were both numbered `R-1.4` are now `R-1.4` (the git boundary, which `BLOCKERS.md` §N10 cites) and `R-1.8`.

What the rows quote follows one rule: a **cause name, a printed refusal and a printed report line stay
verbatim Russian**, because they are data the tool ships rather than prose of this document, and a row citing
them is citing the code. A failure message of a check is retold in English: it is evidence about the check,
not the tool's words to a person.

## 2. Wave 0 — dead ends (behaviour fixes, outside the refactoring) — ✅ done 2026-09-14

| # | Item | Acceptance |
|---|---|---|
| R-0.1 | The fix advice leads to a command that works (before: the missing config advised a command that finished successfully and created nothing; `BLOCKERS.md` §N2) | Run: `node bin/size.js` without a config → the text names the command, running it creates the config, and `--write` works with it at once (re-measured 2026-09-16 on a fresh repository: code 1 with the advice, then `--init` — the file appears — then `--write` — code 0) |
| R-0.2 | The report's missing directory is created by itself (before: `--page .size-report/…` ended in an `ENOENT` stack) | Re-measured 2026-09-16: the `--page` mode no longer exists at all (code 2, “незнакомый ключ” — the tool prints in Russian; the modes today are `--init`, `--write`, `--data`, `--json`), the report's path is given to `--write [file]`, and the directory is created by itself (measured: `--write .report/deep/out.html` → code 0, the file lies there) |
| R-0.3 | Every refusal branch has help and an exit code instead of a stack (before: `--help` did not exist at all and a refusal printed a stack) | A check per path; compared with the table of codes in `PLAN.md` §4.1 (`--help` measured 2026-09-16: code 0 and the printed usage) |
| R-0.4 | _(found along the way)_ The draft of the settings does not make the table's own file a column: otherwise the advised command leads into a new dead end, its own settings check rejecting the draft | Check: `--init` on a project whose table is already in history, then `--json` over the draft → code 0 |
| R-0.5 | _(closed right here, `PLAN.md` D5)_ Stale texts: the engine's header and the `--init` advice name real commands instead of `tools/size-table.js` and `tests/size-table.js` of a foreign project | `grep` over the engine finds not one mention of paths the package does not have (re-measured 2026-09-16: `rg 'size-table\.js' src/` finds nothing) |
| R-0.6 | _(came from the tokens pass, `BLOCKERS.md` §B3)_ A column whose file lived in history and was deleted before HEAD brought the run down with **code 1** and “no file instead of no file” — that is, a history with deletions had no report at all. A divergence of the two sides of the comparison became the refusal (a lost creation, a change, a deletion), and the text names both the tree's side and the state's side | Two checks in `test/disk.test.js`: a history with an addition, an edit, a deletion, a return and a second deletion builds, and its numbers agree with the blob size from git (red on the old code with B3's text); a file that appeared only in a merge brings the run down when the reading of history is mutated, with both sides in the text |
| R-0.7 | _(a continuation of R-0.6, `BLOCKERS.md` §N8)_ The path for a state was chosen by the order of the settings rather than by what the commit holds: with rename detection off, git hands over both paths of a renamed file in one commit, the engine took the vanished one, and the comparison refused on a lawful case. Now the plan collects every alias of the column the commit touched and takes the first one git gave a blob for | The earlier numbers did not move: the engine's output before and after the fix matched byte for byte on the fixture with `diff.renames` in both values, both frozen standards reproduce byte for byte, the live report is the same 95 × 27, and the run over the live history takes 1.56–1.58 s. The check “a rename inside a column's aliases does not bring the run down” is red before the fix (clone `237bbdd`: 5 of 6 green); a lost edit, a lost creation and a lost deletion remain three witnesses of the mutation |

Checks on these paths were added right here: before the wave the coverage of `--init`/`--force` and of the
refusal outputs was exactly zero. All ten new checks are red on the engine before the fix.

## 3. Wave 1 — engine and CLI

| # | Item | What shrinks | Acceptance |
|---|---|---|---|
| R-1.1 | **One reporting layer for the artifact and the page** — ✅ **done 2026-09-14**, though the minus was **not reached: the file grew by 25 lines** (1757 → 1782), of which code −9 (1325 → 1316) and comments +25 (308 → 333). The duplicated calculation is gone (there were two independent computational parts: 12 functions, ≈120 lines of code), but the single version carries both cases and their documentation, so it is nearly as long as one of the copies. What gives the promised −150…−250 in this file is not the merging but **R-2.1** (the page's program leaves the string constant: −200 from the file) and **R-2.2** (−45 of styles) — the estimate in this row was wrong. What came instead of a minus: there is no second calculation at all (the page runs the engine's code), the artifact and the page's markup matched the old output byte for byte, and the second calculation got a check (contract checks 12 → 13) | — | the minus for the file is in R-2.1 and R-2.2 |
| R-1.2 | **Linter and format** — ✅ **done 2026-09-14**. `eslint.config.js` (flat, ESLint `^9.18.0` — the consumer's version): the rules are taken from the consumer — `no-unused-vars`, `no-undef`, `block-scoped-var`, `eqeqeq` as **warnings** — plus two of our own as **errors**: `indent` (`error`, 2 spaces) and `no-multi-spaces` (`error`, `ignoreEOLComments`), which is the very rule that catches the gluing this item started from (re-read 2026-09-16: the config says `indent` is the only warning-turned-error apart from `no-multi-spaces`, and the severities are as named here). What is consciously left out so as not to pass noise off as a signal: `max-statements-per-line` — 39 findings on a clean tree and blind to the gluing itself (the statement on that line was alone); `brace-style` with `allowSingleLine: false` — 78 (the code accepts the one-liner `if (...) return;`); the reasons stand in the config's header. Scripts `lint` (advice, as in the consumer) and `lint:strict` (fails). `fixtures/` is ignored: that is data (history bundles) and the frozen copy of the old implementation. The linter also found real litter: `test/cli.test.js` held unused `os` and `PACKAGE_BIN` — removed. Agreement with the consumer lives **in the rules rather than in a shared config**: the module is ESM, while the consumer's config declares `sourceType: "script"` and fails on the module's sources with “'import' and 'export' may appear only with 'sourceType: module'” (checked). The consumer is untouched; to lint the module's sources it needs one line with `sourceType: 'module'` for those paths. What remains: lint deliberately stays out of `pnpm test` (the suite is about behaviour) and got its place as a separate step of the profile (`pnpm run verify`), which is what CI runs | 0 lines (a quality edit) | `pnpm run lint:strict` — 12 files, 0 findings; mutation: the historical line `} else {      body = …` returned to `commitCell` — the run gives **exactly one finding, on that very line** (1106), the rest clean; `pnpm test` 38/38 in two environments, the artifact and the live parity without a re-take |
| R-1.4 | **One boundary for calling git, for the engine and the checks** (found by the documentation guard, recorded as `BLOCKERS.md` §N10 — the same class as B1, only in the harness) — ✅ **done 2026-09-14**. Before: the engine pinned its settings on its own boundary (`src/git.js`), while the checks and the tools called git directly — with the machine's settings. On a machine where `core.quotePath` is on by default (`GIT_CONFIG_GLOBAL=/dev/null`), `git log --name-only` hands over non-English paths quoted, and `docs/заметки.md` from `BLOCKERS.md` became “nonexistent”: the documentation guard was green on this machine and red on another. Now: the list of pins and the locale come from `src/git.js` (`gitArgv`/`gitEnv`) — one list for the package rather than a second one beside it; the checks and tools go through the harness (`gitIn`/`gitTry`); unpinned reading stayed exactly where it is the subject — in the check measuring inherited settings — and it is named (`gitBare`, the list of allowed places kept by a person). Witnesses: `test/git-pins.test.js` — the sources (a direct call without the shared list is named by file and line) and the behaviour (the same reading without the pin returns a quoted path, with it — as it is) | −9 direct calls of git in the checks and tools (13 lines), +2 checks (`test/git-pins.test.js`, 88 lines), +13 lines in `tools/harness.js` (`gitIn` with the pins, `gitTry`, `gitBare`), 0 lines in the engine — it was pinned already | ✅ red before the fix: the same guard on clone `d8ba03f` names **eleven** places (`tools/check-standards.js:110,119,121`, `tools/make-fixture.js:60,292`, `tools/pack-check.js:52`, `tools/parity-freeze.js:50,55`, `tools/parity-live.js:162,163`, `test/cli-paths.test.js:28`, and further down the list) — the working list is kept by the check rather than by a comment. Mutations: an unpinned call returned to `test/cli-paths.test.js` brings the first check down with that same address; removing `core.quotePath=false` from the list does the same to the second, but only in an environment without the machine's settings (which is the whole point of two environments: on the author's machine the pin is invisible). The standards were not re-taken and **did not move**: `check:standards` — 4 of 4 + 3 of 3 byte for byte (the pin touched the tools that take the standards as well), `pack:check` — artifact 22 112 B and page 59 358 B from the tarball, `parity:live` — 95 × 27 in two environments; 99/99 in two environments, `lint:strict` — 0 |
| R-1.3 | **Splitting the engine into files** — ✅ **done 2026-09-14** (also `PLAN.md` item **D1**). There is one seam and it follows the boundaries of data: above sits what reads git and the file system, below what works on values already collected, while the settings, the texts and the refusal stand at the edges — everyone knows them and they know no one. That gave 15 modules (plus `derived.js` and `page/app.js`, which existed): `cli` 245, `strip` 207, `git` 194, `render` 168, `history` 161, `config` 103, `data` 90, `size-table` (the entry point, re-exports only) 83, `page/build` 82, `locales` 73, `journal` 66, `metrics` 62, `refusal` 56, `tool` 13. Along the way a real link showed up that is invisible otherwise: the data contract called `build` from the module of modes — the assembly moved into `history.js`, and the cycle `data ↔ cli` did not appear (the module count and the names are as of that day; the tree has been split further since, so today's layout is the tree's own business) | the file 1525 → at most **245**; `src` in total **+78 lines raw / +41 of code** (1812/1327 → 1890/1368) — module headers and imports, not one line of code rewritten | `pnpm test` **39/39** (38 + an API insurance) in two environments; `pnpm run lint:strict` — 0; the artifact and **the page byte for byte** with the engine's output before the split, the live parity in two environments without a re-take; **work from the built package** — from then on by `pnpm run pack:check` (`npm pack` → unpack into a foreign directory → clone the fixture): all 16 sources and `page/` arrived, and `--json`, the artifact and the page are byte for byte as from the repository; the check itself is proven by a mutation (take `src` out of `files` — it fails and explains what to look at); the public API — 58 names, none lost and none added (`test/api.test.js`), and the mutation “remove one name from a re-export” brings exactly that check down |
| R-1.8 | **Stale texts and the thin legacy path** (once the second row numbered R-1.4; renumbered 2026-09-16 — nothing outside cited that number, while `R-1.4` is cited by `BLOCKERS.md` §N10 for the git boundary) — ✅ **closed 2026-09-14**, `PLAN.md` item **D5**. The stale texts went with R-0.5 and R-4.8: the engine's header no longer offers `node tools/size-table.js`, `initMode` no longer advises `tests/size-table.js` for a consumer project (measured 2026-09-16: `rg 'size-table\.js' src/` finds nothing), and the advice names the path inside the project (R-4.21). The path the item called legacy is not one any more: `check` stayed as a command with an answer of its own (completeness and the reason a commit was skipped, R-4.12), and comparing the artifact with the history remained its job | −40…−70 lines were promised; what actually shrank is in R-0.5 and R-4.8 | `pnpm test`; the output of `--init` is checked line by line by a check |
| R-1.6 | **One owner of the command line's grammar** (audit: “two modes at once” survived from R-4.13 as the same class — a command answers zero having done nothing) — ✅ **done 2026-09-14**. Before: three helpers (`strayArg` — a line of refusal, `noArgs` — printing at once, `plainWords` — words without parsing) and the choice of mode made by the **order of `if`s** in `main`; the rule “this is not allowed” lived in the author's prose rather than in a value, so `--write --data` answered zero, having written the table and given no data (`--json --write`, `--data --page`, `--page --write` — the same). Now: one parser before the project is read decides everything at once (which mode, whether it goes with the command and with the other keys, whether every key got a value, whether there are stray words), refuses in **one way** rather than by printing and returning from four places, and hands over a ready plan that `main` merely executes. The rules that did not exist: two modes at once (all six combinations enumerated), `--json` beside a mode and beside a command without a JSON answer, `--config` beside `--init`, a key named twice. **What turned out about the old order:** it never touched lawful calls — not one working call depended on the order of the branches; but in mistaken ones the mode was chosen by a place in the list rather than by a person: `--data --page` printed data in silence and `--page --write` wrote the table in silence. Such a call is now refused, naming both modes. The parser has lived in `src/args.js` since it was split out of `src/cli.js`; there is no separate `checkArgs` — the refusals are raised by the parser itself (the file of the time was `src/cli.js` 418 → 450, and today's split is the tree's own business) | **no minus — +32 raw / +26 of code** in the file of the time (`src/cli.js` 418 → 450; 320 → 346; later split into `src/cli.js` and `src/args.js` with the shared `refuseCause`): the parsing grew longer because it reads values and notices a repeat for the first time, and the six rules are new; but no cut was expected here — the audit's promised −55…−65 belonged to the test helpers, while the checks grew, because they are the proof. Plus 3 lines in `src/refusal.js` (the rule is named in the help), `test/cli.test.js` 160 → 216 | ✅ the new checks are red before the fix: the same set on clone `c526399` — **0 of 1** needed (“two modes: --init --write: expected code 2, got 0”). The edges are checked by enumeration rather than by an example: all six combinations of modes, `--json` beside `--write`, a command with a mode, `--json` for `install-hook`, `--config` with `--init`, a key named twice (`--write --write`, `--config a --config b`), lone `-` and `--`, a stray word before and after a command — each with its own code and text, and every one with a ready command in the refusal. Lawful calls were re-checked as well (`--init draft.json --force`, `--page out.html --config …`, `explain <sha> --config …`, `--config x check`, `doctor --json`, `--json`/`--data`/`--write` with `--config`) and stayed green, while not one call was found that rested on the order of the branches. **120/120 in three environments** (23.32 s; 23.18 without the machine's settings; 22.79 with `CI=1`), `lint:strict` — 0, `pack:check` — artifact 22 112 B and page 59 358 B from the tarball byte for byte, `parity:live` — 95 × 27 in two environments with the same sha256 `1bdb27e1…`, `check:standards` — 4/4 + 3/3, and **not one standard re-taken** (the parsing touches neither the numbers nor the format of the report) |
| R-1.7 | **The `--json` rule — one and named** (audit: “`--json` has two meanings, and that is visible only by reading”) — ✅ **done 2026-09-14**. The decision: `--json` is **a form of the answer** rather than a mode, and an answer belongs to exactly four invocations: without a command or a mode it is the former form of the data (frozen by the parity standard — it cannot be removed), while `check`, `explain` and `doctor` have answers of their own. Hence beside a command without an answer and beside a mode it is a refusal, and each case has its own cause (`нет ответа в JSON`, `два ответа сразу` — both names stand in the registry of causes, read 2026-09-16) rather than the half-measure of “ignore it in silence”. In the code that is **one value**, `ANSWER_COMMANDS` (today in `src/args.js`, read 2026-09-16: three commands plus the data form = four invocations with an answer), and both refusals look at it; the help names the rule in words, and README does so in the section for an agent. The behaviour did not change with the fix (both refusals existed before it): what changed is that the rule became one and named. The new checks are by enumeration: all four invocations with an answer (their stdout parses as JSON) and all four modes with three commands without one, each with its own code and text, plus “a refused call wrote no page”. **Honestly about “red before the fix”: that check is green at the former revision** — what changed here was the wording rather than the behaviour, so what was red before the fix was the absence of the check rather than its failure | `src/cli.js` +8 raw (the parsing has since moved to `src/args.js`), nowhere else; `test/cli.test.js` +43 | ✅ 122/122 in two environments (23.16 / 24.04 s; 23.23 without the machine's settings), `lint:strict` — 0, the artifact and the page from the tarball the same (22 112 B / 59 358 B), `parity:live` 95 × 27 with the same sha256, `check:standards` 4 of 4 + 3 of 3, no standard re-taken |
| R-1.5 | **The fixture's anchor** (`fixtures/legacy/size-table.cjs`, 1145 lines) — the checks about the origin of the standard rest on it (“the engine broke” against “the standard drifted”). Revision 1.0.0 arrived, and with it the item's condition — ✅ **done 2026-09-15**. The copy left the tree: it is rarely needed history rather than a working copy of the package, and history is where such a past belongs. One point takes the bytes (`legacyTool()` in `tools/harness.js`, read 2026-09-16): the commit that **added** the copy (by `git log --diff-filter=A` rather than by a sha written by hand — that way the material survives history moving), with the hash compared against the record of the standard's origin — so the “frozen copy” cannot silently stop being one, as before. Both re-takes (`pnpm run fixture`, `pnpm run parity`) and the suite's check take it from there; the records of origin kept the path the copy lay under, because a record tells the standard's story rather than today's tree | **−1 113 lines out of the tree** (the repository 36 993 → 35 880, −3.0 %), plus −2 checks (R-3.4); by file (added/removed): `fixtures/legacy/size-table.cjs` −1145, `tools/harness.js` +38/−6 (materialisation and the frozen target), `tools/parity-freeze.js` +6/−8, `tools/make-fixture.js` +5/−5 (the path in the record of origin), `test/frozen.test.js` +24/−29, `tools/docs-facts.js` +3, `README.md` +6/−6; in the measurement — not one line | ✅ **a byte-for-byte re-take with no copy in the tree:** `pnpm run check:standards` takes both standards anew on the side and compares with what is committed — “fixture: 4 of 4 files byte for byte, the manifest by fields, the bundle carries 16 commits” and “parity: 3 of 3 files matched byte for byte”. Found and fixed along the way: taking the fixture wrote **the path of a temporary file** into the manifest (now the path the copy lay under; without that the manifest diverged from the committed one and `check:standards` was red). The distinguisher is alive: `test/frozen.test.js` takes the copy from history, compares its hash with the manifest and demands the same numbers of the standard (under `LC_ALL=C` as well) — 4 checks green. The numbers and the standards are untouched: 131/131 in two environments, artifact 22 112 B and page 59 358 B byte for byte, `parity:live` — 95 × 27, `lint:strict` — 0, `pack:check` green |

## 4. Wave 2 — the page

| # | Item | What shrinks | Acceptance |
|---|---|---|---|
| R-2.1 | **The page's program leaves the string constant for a module** (the decision was already taken, `PLAN.md` §4.1: it is assembled when the package is published) — ✅ **done 2026-09-14**. Before: 184 lines of JS as text inside the engine (`APP_DOM`) plus the text of the shared calculation, taken off functions by `toString()`. Now the page's program and the shared calculation are ordinary modules — and the assembly chapter reads their sources, stripping the module syntax line by line (`stripModules` → `pageSource` → `pageScript`, today in `src/page/build.js`, read 2026-09-16); `APP_DOM` is gone (measured: no such name in `src/`). The minus was promised as −(180…200) out of the engine and the same into a module — by file it was even more: `src/size-table.js` 1782 → **1525** (raw −257, code −197: 1316 → 1119). But it is **a move rather than a cut of the package**: 287 raw lines / 208 of code in the modules, that is 1782 → 1812 in total (**+30 raw, +11 of code** — file headers and `import`s). What came instead of volume: the page's program became ordinary code (the linter sees it, R-1.2) and the engine/page boundary became a file boundary. The page has one chapter per concern since (`app`, `build`, `dom`, `panel`, `state`, `table` — measured 2026-09-16), which is the tree's own business rather than this item's. What is left of the item — a pre-built `dist/app.js` as an artifact of publication — is in §8 and is not done here | −257 raw / −197 code out of the engine; +287/+208 in the modules; the package +30 raw in total | `pnpm test` **38/38** in both environments; the artifact byte for byte (225 673 B, sha256 `1bdb27e1…`), `parity:live` green; the page on the fixture is the same DOM as the engine's before the item: 15 rows, 330 cells, and the markup of the panel, the table, the note and the heading matched byte for byte, including after a metric and a file are switched off; on the live history 96 rows / 5376 cells / 32 switches, the page 104 355 B, three `<script`, no external references and no `type="module"`, no errors in the DOM |
| R-2.2 | **One set of styles, and the page's look** — ✅ **done 2026-09-14**. There are no two sets any more: `table.css` is **the shared part of the table** — the same text that lived inside the engine — so it serves the artifact and the page at once and carries the sticky header and the sticky commit column that only the artifact had, while the page's look sits **on top of the table** in `src/page/app.css`; `src/css.js` reads both from disk relative to its own place. The artifact kept no layer of its own — re-read 2026-09-16: `src/css.js` reads two files (`src/table.css` 34 lines, `src/page/app.css` 234) and the package has exactly two files of styling, so the `src/artifact.css` of that day was folded into the shared one (the 8 lines went with it), and no guard asks for a third. The convention about the colour of a delta is **one, taken from the artifact**: growth green `#1e8449`, fall red `#c0392b` — two lines in `table.css`, and there is no other colour in the package's styling (measured: two hex colours in both files together, and the check watches that). The page gave up its own convention (growth brown `#b04a00`, fall green `#1a7f37`): the artifact is frozen by bytes, so its colours cannot change without re-taking the standard, and two reports of one history cannot show growth in different colours. Changing the convention is two lines in `table.css` plus re-taking the artifact's standard — there is no separate home for a colour. The page's look was reworked: typography and rhythm, a panel with a legend, the table in its own frame with its own scroll, sticky header and commit column, adaptation to a narrow window (300px per column → 190), empty states in words (all metrics or all files switched off), label-switches with focus that survives a rebuilt panel | styles in code were **67 lines and in files 199** — **no minus**: the shared `table.css` came back with the artifact's own lines folded into it, the page's new look takes 99 lines, and reading from disk 34. The promised −40…−60 belonged to merging the two sets rather than to reworking the look | `pnpm test` **42/42** in two environments; the artifact **byte for byte** (22 112 B, sha256 `5c0c73e4…` on the fixture; 225 673 B on the live history), `parity:live` in two environments without a re-take; `lint:strict` — 0; `pack:check` — the page from the tarball byte for byte as from the repository. **The page in a real Chrome**: not one external resource (no `link`, no `img`, no loads) and an empty console; it fits at 1500px and at 620px, where before the fix it ran 1518px wide in a 620px window; after scrolling 400px the header stays put and the commit column stays at the left edge; switching every metric off gives a caption instead of the grid, and every file off gives the total in 2 columns with the caption; `Tab` + `Space` switch a metric and recompute the table (22 columns → 11), the focus staying on the same switch; five mutations show each new check guarding its own thing — a second colour of deltas in `app.css`, a repeat of a shared rule in `app.css`, silence about emptiness, silence about switched-off files, a lost focus |
| R-2.4 | **The tree of files in the panel** (a feature of step 2 of the plan, not a cleaning — written here because it edits the same file of the page) — ✅ **done 2026-09-14**. Instead of a flat list by category the panel shows a tree of the same paths the caption of a file carries: a folder has three states (`checked`/`indeterminate`/off) and a switch for its whole subtree, with the number of files beside it; the category buttons stayed as a row above the tree. The state is still held by files alone (`appView.files`, read 2026-09-16 — `src/page/state.js`), a folder and a category have none of their own, so there is nothing to drift. A list longer than `min(30vh, 320px)` scrolls inside the panel (measured 2026-09-16 in `src/page/app.css`): without that the 25 files of the live history pushed the table off the screen. The tree itself lives in the panel's chapter since the page was split (`src/page/panel.js` — measured: the three states are set there) | 0 lines of economy (a feature): `src/page/app.js` 230 → 288, `src/page/app.css` 132 → 143, `src/page/build.js` 89 → 91, 4 lines of the dictionary in `src/locales.js` | `pnpm test` **54/54** in two environments (the new check red on the code before the fix: no folders in the markup, and the envelope's closed list of names does not know the new ones); the artifact **byte for byte** (22 112 B on the fixture, sha256 `5c0c73e4…`; 225 673 B on the live history), `parity:live` 95 × 27 in two environments without a re-take; `lint:strict` — 0; `pack:check` — the page from the tarball byte for byte as from the repository (40 603 B). In a real Chrome on the live history: 9 folders up to three levels, 25 files, the panel 429 px in a 1080 window, the network is the file alone and the console empty; `docs/6` turns a folder into the third state after one of its files is switched off, and switching a whole folder off takes 52 columns to 40 |
| R-2.5 | **The reader's memory of the choice** (a feature of step 2 of the plan, editing the same file of the page) — ✅ **done 2026-09-14**. The rule by which a choice is applied: a record is tied to the **report's passport** (the tool's name, the data schema, the artifact's path, the title and the column labels in the report's order; the fingerprint is the record's key, 32 bits), while inside the record the choice lies **by names** — a file by its path (the path at HEAD, or the last one from the settings when the file is gone), a metric by its key — and only what is switched off is stored. Everything else follows: someone else's report's choice is not picked up (in Chrome every `file://` page shares one storage — checked: two reports hold two keys); a name the report does not have (a column redirected to another path, a metric removed from the settings) means nothing, while a new one stays switched on; a record of another format version, of a foreign passport or a corrupted one is as good as absent; “switched everything back on” removes the record rather than leaving a trace. **The tip of the history and the package's version are absent from the passport on purpose:** a grown history and an updated tool are the same report, and a choice must survive the next build; the price is that one field can be added to the passport if wanted (a tip needs a field in the contract, and there is none today). Restoring happens **before the first draw** (`src/page/state.js`, read 2026-09-16), so the first reader gets the default markup unchanged — a reader's state does not touch the numbers or the default | 0 lines of economy (a feature): `src/page/app.js` 288 → 381, and the checks counted then in `test/contract.test.js` 527 → 651 — that file has since been split, and the memory of the choice is watched by `test/page-choice.test.js` today (measured 2026-09-16: 6 checks there) |
| R-2.6 | **Exchanging a choice by a link** (`PLAN.md` §4.8, item 6 — `location.hash` as the fallback path is there too) — ✅ **done 2026-09-14**. The address **is** the link: the anchor carries **the same record** as the memory (`#size-report=…`, the same JSON, percent-encoded, because raw quotes and non-ASCII paths have no place in an address), so the sender copies the address rather than using a button of its own. The order is decided by the same code as for the memory: one's own link is applied (and replaces the whole view — it carries the whole choice rather than a difference), a foreign or unreadable one is not applied and is explained by a line above the table, and a name the report does not have is skipped with a counter. Three circumstances visible only in a browser, which is why they went into the decision: (1) a sent address is not rewritten before the reader's first action (otherwise one opens a link and loses it); (2) a sent choice does not go into the reader's memory — the memory changes only through his own action; (3) a change of the anchor does not reload the document, so a choice from a new address is applied on `hashchange` — without that a link would work only in a new tab (found by a browser check rather than by the checks). The price: the address is long (it carries only what is switched off) and is updated through `replaceState` on every change of the selection, which does not clutter the browser's history | 0 lines of economy (a feature): `src/page/app.js` 381 → 515, `src/page/app.css` 143 → 156, `src/locales.js` 99 → 111, `src/page/build.js` 91 → 95, and the file then counted (`test/contract.test.js` 651 → 800; its witnesses are `test/page-choice.test.js` today); not one new line in the contract, the numbers or the artifact | `pnpm test` **59/59** (and 56/56 in the window before the link was finished) in two environments; the new checks red on the code before the fix (clone `658f0ad`: “the choice did not reach the address” — `-1 !== 0`; no `#notice` in the markup; clone `8e551b9`: `0 !== 1` — no record; `"undefined" is not valid JSON` — nothing to read), the envelope's list of names extended on purpose by six, and the claim about self-sufficiency — red on the old code too, but because of the word `import(` in a comment: the check is literal, so the comment was rewritten in words (`worklog/archive/WORKLOG.md` §25.5); `lint:strict` — 0; the artifact **byte for byte** (22 112 B on the fixture, 225 673 B on the live history, sha256 `1bdb27e1…`), `parity:live` 95 × 27 in two environments without a re-take; `pack:check` — the page from the tarball byte for byte (54 772 B). In a real Chrome from disk: a recipient without memory sees the same 25 columns and the total 994 335 by the link; a foreign address leaves 52 columns and 1 133 362 and is explained; on an open page the link changes the view from 52 to 25 columns; a re-entry returns the same choice; the console is empty and there are no network requests beyond the file itself |
| R-2.7 | **A number's exactness by cell rather than by column alone** (`PLAN.md` step 4, cut 2 — **a feature rather than a cleaning**: it closes what step 3 left). Before: `accuracy` was declared for a metric as a whole, and one format without a minifier made the whole column approximate while honestly counted cells lay beside it. Now one rule serves two answers: `pointExact` (`src/metrics.js`, read 2026-09-16 — exported there and used by `src/history.js`) says whether a particular file's number is exact, and both the metric's caption and the marks of the cells come from it. A metric's caption is **the worst in the column** and is now counted by cells rather than by the name of the method (a report of JSON alone under stripping is declared exact, because it is), while a cell comes to the contract as a mark rather than being derived by the page from paths: the shape is `approx: { min: { rows: "…", now: "…" } }` — a mark per row cell and one per the “now” row — and a metric with no mark at all does not appear in the report (`src/data.js`, read 2026-09-16). Hence no manual rules on the page (the list of formats lives in the engine alone) and full agreement of the two answers; the dotted style of a cell is `td.approx` in **`src/table.css`** (corrected 2026-09-16: that is where the marks of the table live, not in `src/page/app.css`), and each metric's method is visible as text under the switches rather than only in the tooltip. **The second remainder of step 4 decided:** there will be no switch of the tokenizer family on the page — the page gets finished numbers and has no other dictionary, and precomputing every family means paying time for numbers nobody may ask about (`PLAN.md` §4.8.4 cancelled there). The choice lives in the settings of the run, and the page names it — which is what follows for the report's texts | **No minus, and none promised — this is a feature.** `src` in total **3110 → 3220 raw** (+110, of which +57 code and +53 comments; counted as non-empty lines other than comments across `src/**/*.js` and `src/**/*.css`). By file: `src/data.js` 84 → 104 (the row of marks and its rule in the header), `src/history.js` 179 → 187 (the mark is set beside the measurement and does not go into the cache of contents — it is a property of the path rather than of the blob), `src/metrics.js` 253 → 268 (the rule and its description), `src/locales.js` 111 → 124, `src/page/app.js` 515 → 560, `src/page/build.js` 95 → 102, `src/page/app.css` 156 → 165, while `src/tokens.js` **83 → 76** (`binaryFormats` became `isBinary`, read 2026-09-16: the list of formats is no longer assembled on the metric's side). Checks: `test/contract.test.js` 800 → 906 and `test/minify.test.js` 208 → 239 (the contract's file has been split since; the marks are watched by `test/page-view.test.js` and `test/minify.test.js` today) | ✅ **80/80** in two environments (11.16 / 10.75 / 11.07 s; 11.30 without the machine's settings), `lint:strict` — 0, `pack:check` — artifact 22 112 B and page 59 358 B from the tarball byte for byte, `parity:live` 95 × 27 and 225 673 B with the same sha256 in two environments, `check:standards` 4 of 4 + 3 of 3 — **not one standard re-taken**: `--json` and the artifact are untouched (the artifact never printed a metric's accuracy). Three new checks red before the fix (clone `e463b70` with the new suites: `Cannot read properties of undefined (reading 'raw')` — no marks in the contract; the same in `minify.test.js` under real compression; “an approximate cell is not marked” in the DOM). The change of the rule shows exactly where the behaviour changed: in the check of the caption it is the new claim about a report of JSON alone under stripping that turned red (on the old code `'approximate'` stood where by cells it is `'exact'`). The fixture was checked on a **mixed** report rather than a uniform one: `package.json` (JSON) exact, `.js` and `.md` not; under real compression the other way round — `.js` exact, `.md/.toml/.txt` not. In a real Chrome from disk: on the live history 26 cells of 27 are marked for `min` (the only exact one is `manifest.json`), none for `raw`, and the total carries the mark of “the worst of those switched on”; with a single exact file left no marks remain in the table, a cell carries the method's caption, the panel names the method of both metrics as text, and the legend says what the dotted line means; no network requests |
| R-2.3 | **One function for digit grouping** — ✅ **done 2026-09-14 together with R-1.1**: `appGroup` with its double escaping is gone (measured 2026-09-16: no such name in `src/`), and one `group` of the shared calculation part sets the digits (`src/derived.js`) | −5…−10 lines | `pnpm test`; the artifact byte for byte, the page in jsdom — the same numbers |

## 5. Wave 3 — tests

| # | Item | What shrinks | Acceptance |
|---|---|---|---|
| R-3.1 | **A shared harness** — ✅ **done 2026-09-14**. `tools/harness.js` is the single owner of the harness: paths, clones (plain, CRLF, one shared per environment), running the tool, parsing refusals, hashes, the first difference. No suite keeps a copy of its own any more, and a `grep` for the helpers' names in `test/` finds none (re-measured 2026-09-16: nothing in `test/` declares `runCli`/`firstDiff`/`sha256`/`MAX_BUF`) — the copies took the divergent signature of `runCli` with them (`env` was in the parity alone). Copies stayed in `tools/parity-live.js` for a while: that is a separate command with an output format of its own rather than a suite of checks, and what remains there is measured below (R-3.5) | the harness 73 → 194 lines, `test/` 933 → 929 (code −25: 662 → 637). Together **+117 raw / +52 of code** — no minus: the harness grew by the cache of runs, the shared clone, the CRLF clone and the parsing of refusals, while the five new files grew by their headers. The item's gain is not volume but the absence of copies | `pnpm test` 39/39 in two environments; the `grep` for the helpers' names in `test/` — empty (one copy per package rather than per suite) |
| R-3.2 | **Not measuring the same thing several times** — ✅ **done 2026-09-14**. Runs that only read go through `readRun` (the key: the tool, the directory, the keys, the environment — `tools/harness.js`, read 2026-09-16), so the same command in one environment is counted once per suite. Assembling runs (`--write` and the control one) are deliberately not cached: they depend on the state of the tree, and a cache for them would be a lie. The number of checks stayed 39, with the names compared line by line: 35 matched verbatim and 4 were renamed (the trio of the frozen copy and the check of its revision), not one lost and not one claim rewritten | time, not lines | the sum of the run's durations **15 074 → 5 328 ms** with the same number and composition of checks |
| R-3.3 | **One clone of the fixture per environment** instead of a clone under every check — ✅ **done 2026-09-14**, though not literally. A clone is held one **per suite and environment** (`sharedClone('plain')`, `sharedClone('crlf')` — both called by the checks, read 2026-09-16), while suites are separate processes, so a run keeps five clones rather than one: a single one for all suites would need inter-process synchronisation for an economy worth nothing. Checks that edit files (an edit on disk, the settings, a truncated history) take a clone of their own — otherwise a neighbour would get a foreign edit | time | clones per run, counted by the calls of `git clone` (a wrapper in `PATH`): **24 → 21**. The saving in clones is small, and that is worth saying plainly: the time came not from them but from the heavy file falling into five that went as a wave across the cores — the longest part fell from 14 436 to 3 357 ms and the parity suite from 14 436 to 2 916 |
| R-3.4 | **The trio of the frozen copy demoted** (its subject is “the standard was taken from that revision” rather than the engine's behaviour) — ✅ **done 2026-09-15** together with R-1.5, and the row itself was mended 2026-09-16: it had been broken across two lines, so its tail rendered as a paragraph. Of three checks **one** remained, and the file holds four today (measured: `test/frozen.test.js` takes 4 checks in the run): it demands the same numbers of the standard from the copy out of history and checks them again under `LC_ALL=C` (so the pin of the taking is not lost — take the standard without the pin and the numbers would diverge here). The separate “`--write` builds the same artifact and passes the control” and “the numbers do not depend on the locale” are gone: the first is done wholly and byte for byte by `pnpm run check:standards`, which takes both standards anew, and the second is part of the check that remained. What went is **five** lines (+24/−29) rather than the promised −60…−80: the checks were short, and the minus here is in the run's time (2.4–2.9 s → 2.2 s) rather than in the text | `test/frozen.test.js` (6 checks → 4) | ✅ the remaining check is red without the copy: remove the materialisation and it fails with “the history has no `fixtures/legacy/size-table.cjs`: there is nowhere to take the copy the standard was taken from”; substitute the historical blob and the comparison with the record of origin fails. The re-take of both standards stays proven: `check:standards` green (4 of 4 and 3 of 3). 131/131 in two environments |
| R-3.5 | **The tools' helpers from the shared harness** — ✅ **done 2026-09-14 in part** (R-4.9): `tools/parity-freeze.js`, `tools/make-fixture.js` and `tools/pack-check.js` take from `tools/harness.js` the paths (`ROOT`, `SYNTH`, `BUNDLE`, `CONFIG`, `LEGACY`), `MAX_BUF`, `sha256` and the pinned environment — three copies of `MAX_BUF` and two of `sha256` went. The same pass moved the gluing of a process's output (`collectOutput`) there as well: it collected the output as a string and broke on a character split between chunks (`worklog/archive/WORKLOG.md` §21). **What is left in `tools/parity-live.js` today, measured 2026-09-16:** `parseArgs`, `runCli` and `firstDiff` — `MAX_BUF` and `sha256` are gone from it (only `tools/harness.js` defines `sha256`, and `parity-live`'s `runCli` already leans on the harness's `collectOutput`). Bringing the three remaining ones together is an item of its own rather than a cleaning of tests | −25…−40 lines in the tools (already taken: 3 copies of `MAX_BUF`, 2 of `sha256`, `collectOutput` — 8 lines out of `parity-live.js`; the later consolidation of `MAX_BUF`/`sha256` is not counted here) | `pnpm test` 53/53, `parity:live` green; a `grep` for `on('data')` across the package finds `tools/harness.js` alone (re-measured 2026-09-16) |

Wave 3's acceptance is by measurement rather than by a word; the machine, the method and both environments stand
in `worklog/archive/WORKLOG.md` §14. **What the wave established:** `pnpm test` 15.7 → **5.6 s** (median of three),
`pnpm run parity:live` 23.8 → **13.3 s**, and the sum of `node --test` durations 15 074 → 5 328 ms — with the
number of checks unchanged (39): a cheaper run must not be a run that lost checks.

**The time budget was retaken more than once, and then abolished.** The target “`pnpm test` ≤ 7 s” was taken on
39 checks, and by the 51st the suite stood at 6.9 s — a margin of 0.1 s, so the very next check would have made it
red in silence. The target was therefore retaken with room for the future check rather than for today's number,
and each retake carried its own table of windows; the numbers of different windows are incomparable (the same
revision ran 15.5 s and 20.0 s in a row on a busy machine), which is why those tables measured one thing each
time — the cost of one new file, taken by a paired run with and without it.

**Time targets were abolished on 2026-09-15 (R-5.8), and that is the state today:** there is no gate on seconds
in the suite or in CI (measured 2026-09-16: the fast run prints “цель по времени не объявляется” — no target is
declared), and a run prints each file's duration for reference. The tables that stood here were history rather
than obligations, and they are gone with the targets; of the method what remains is the machine and the way the
numbers were taken — `macOS 15.7.9`, `Node 22.23.2`, `/usr/bin/time -p`, one run at a time
(`worklog/archive/WORKLOG.md` §20) — and the rule that a number of one window says nothing about another.

| `pnpm test` (107 проверок, `PLAN.md` §5 — `doctor`) | **13,3–13,5 с** (13,46 / 13,49 / 13,24; 13,25 без настроек машины) — восемь новых проверок стоят **0,9–1,0 с**: та же ревизия без `test/doctor.test.js` идёт **12,4 с** (12,44 / 12,36) при 99 проверках. Цель не двигалась, хотя числа ниже прошлых: разница — машина (та же ревизия шла 19,0–24,4 с подряд), вклад файла измерен отдельно | **≤ 18 с** | 4,5 с |
| `pnpm test` (117 проверок, `PLAN.md` §5 — хук) | **24,3–25,7 с** (24,43 / 24,26 / 24,74 / 24,28 / 25,68; 24,87 и 24,57 в среде без настроек машины) — десять проверок хука стоят **+8,5 с**: та же ревизия без `test/hook.test.js` идёт **15,8–16,6 с** при 107 проверках, а сам файл — **17,9–18,5 с** отдельным прогоном (это интеграционные прогоны: клон, коммиты, слияние, отказы; он стал самым долгим файлом набора). Цель поднята один раз и по этой причине, а не под сегодняшнее число | **≤ 28 с** | 2,3 с |
What stands in the log of these measurements and is reusable: a run costs what its most expensive file costs
rather than the sum (the pool runs the files in parallel), the load at the **entry** of a run says more about the
window than a number taken at the end (the pool is itself a noticeable part of the load), and a new file's cost is
taken by a paired run rather than derived from the scatter. The convergence of files inside one run was checked
and rejected: no gain, and on a small runner the processes only take time from each other.

## 6. Wave 4 — documentation

**One coda serves every row below, and the rows do not repeat it:** the item was closed with the full run green in
two environments, the artifact and the page built from the tarball byte for byte, `parity:live` 95 × 27 with the
same sha256, `check:standards` 4 of 4 + 3 of 3, `lint:strict` — 0, and not one standard re-taken. What a row names
beyond that is what differed: the mutations that prove a new check, the paired run that prices one, the live run
in a real browser, the divergence the guard found first.

| # | Item | What is done | Acceptance |
|---|---|---|---|
| R-4.1 | **The documentation's promises have a guard** — ✅ **done 2026-09-14**, and on the same day **split by promise** (six promises in one file had mixed, with the pin read among strangers; the split is R-4.15). The guard checks what is machinable and only that: paths from code spans exist in the git tree, in the fixture's history or in the list of foreign ones (kept by a person, where new planned paths are entered); README's table of files agrees with the tree **both ways** (a file missing from it is a gap nobody noticed); the commands and keys of the instructions (`README.md` without the sections about a foreign project and about a missing copy, `templates/README.md`) exist in `USAGE`, both lists taken from the help itself rather than rewritten into the check; the count of checks in `README.md` equals the number of `test(` declarations in the suites (a declaration inside a loop is forbidden — the count would then be a derivation from the code, and the file's first check watches that); and a `§…` link carrying a document's name leads to a section that exists. **What the guard does not take, said rather than hidden:** wordings and meaning, promises about the future, the truth of a described file's role, and whether the count of declarations matches what the runner prints — that stays with a person, and the check's header says so. Corrected 2026-09-16: the guard no longer reads a time budget out of this file — targets were abolished (R-5.8) and `publishedRuns` reads README's table alone, so the sentence about “the last line of the budget” described a state that no longer exists. What the guard does read from the **engine's printed** output is its Russian (the sections of the help, the causes of code 2), and that coupling is written down in R-4.15 rather than discovered later | +1 file of checks (the guard: **295** lines in one file; after the split — four files plus `tools/docs-facts.js`, 57 of those lines parsing the addresses of sections), 0 lines in the engine and the tools | ✅ the guard was introduced red and found **4 divergences**, all fixed factually rather than by a change of meaning: (1) the table of files named neither `fixtures/live/README.md` nor `test/runner.test.js` nor the guard's own file; (2) `README.md` promised 92 checks against 97 — the number and the budget retaken honestly; (3) two `PLAN.md` links led to sections the named documents do not have — one to a subsection about manual control in a document that has no such subsection (the requirements have one, and it is exactly about what the user switched on), the other to a subsection of §8 of the requirements, which has none (corrected to §8 itself). A mutation on a live document brings down exactly the right claim (a number → the check of numbers, an unknown key → the check of commands, a foreign path → the check of paths, a broken address → the check of links), and on an empty run the guard stays silent |
| R-4.14 | **The causes of a refusal are named — in the code and in the documentation** (audit: “the table of codes says less than happens”) — ✅ **done 2026-09-14**. Before: code 2 meant two dozen different causes, each a literal at the place of refusal, while README's table of codes named six. Now the causes stand in **one list** (`CONFIG_CAUSES` in `src/refusal.js` — five groups: the command line, settings and the project, history, the hook, measurement; read 2026-09-16), the help prints them from it, and `refuseCause` lets no code-2 refusal through without a named cause (an undeclared one throws `причина отказа не объявлена` — the tool prints in Russian — because that is a defect of the tool rather than a human's dead end). All 25 former refusals moved onto it **without a change of text**: for the user the behaviour is the same. README's table of codes names the same list, and that is a check rather than a promise: `test/docs-commands.test.js` compares three sides (the registry in the code, the help, README's table) and demands that every cause is issued by someone and that no bare `refuse(EXIT.CONFIG` is left in the engine (read 2026-09-16: the file holds four checks — commands and keys, calls that work without an installed package, the causes of refusals, the addresses of sections) | `src/refusal.js` 86 → **124** raw, +12 in the other modules (moving onto `refuseCause`), `src` 4346 → **4396**; `README.md` +8 lines of the table of codes; not one line in the measurement | ✅ **red before the fix**: without it the check fails with “README's table of codes names the wrong causes” and “causes are declared but nobody issues them: нет ответа в JSON, два ответа сразу, нет коммита” — the second found a real hole at once: a cause hidden behind a ternary is not read by the check, and those two refusals were split into calls of their own, so the mechanism turned out alive rather than decorative. Mutations: remove a cause from README's line and the table's comparison fails; add one to the registry and the comparison with the help fails; return `refuse(EXIT.CONFIG` to `src/cli.js` and the bypass check fails; `refuseCause('выдуманная причина', …)` throws |
| R-4.15 | **The documentation guard split by promises** (audit: six different promises in one file, with the pin read among strangers) — ✅ **done 2026-09-14**. Four files, one promise each: `test/docs-paths.test.js` (paths exist in the tree, the table of files agrees both ways), `test/docs-commands.test.js` (calls and keys from the help, the causes of refusals, the addresses of sections), `test/docs-numbers.test.js` (counts of checks, and time targets while they existed), `test/docs-pin.test.js` (the install pin against the help of the pinned revision) — and one reader of facts, `tools/docs-facts.js`, so the files do not drift apart in copies (parsing a call of the tool and reading the help out of history lived in two places and were about to diverge). Not one check lost or weakened: the six former ones moved as they were, and the seventh is new (the causes of refusals, R-4.14). **The reader's coupling is named here rather than found by accident:** three places read the engine's **printed** output in Russian — the `Команды:` section of USAGE, the `Команды:`/`Режимы:` split when reading a pinned revision's help, and `Причины отказа кодом 2` in the table's check — while the documents cite that output as data; translating documentation cannot touch them, and only a change of the tool's printed language would | the guard was one file of **357** lines and became four of **326** plus a reader of **178** = **504** raw (**+147**): the split costs headers, imports and the shared reader rather than saving, and that is the price of one home per promise — named plainly rather than rounded | ✅ the same six checks green after the move, and the move found a real incompleteness at once — README's table of files named neither the guard's new files nor `tools/docs-facts.js` (the completeness check worked on a live change rather than on a mutation). The split added no time: the suite took 23.2 s both before and after (the four files run in parallel with the other suites) |
| R-4.16 | **Release `1.0.0`** (`PLAN.md` §5, step 6: the version, `CHANGELOG.md`, a tag) — ✅ **done 2026-09-14**; not a cleaning but the making of a release, and it touched not one line in the measurement. Before: the manifest said `0.0.0` (“there has been no release of its own”), `CHANGELOG.md` stood in the list of what is not here yet, and the promise “a project with the former settings will not see its numbers move” lived in prose alone. The release: version `1.0.0` with `schema: 1`, declared frozen, and `CHANGELOG.md` with a section **“what changes in the numbers”** — measured on the fixture (10 columns, 4 cuts): `raw` depends on nothing but the git object; `min` with `strip` gives the same bytes as the former tool (that is the parity), while with `esbuild` it is smaller — the total **1597 → 1483** (−7 %), by `code.js` **276 → 185**; `tok` is a new number (540), and without a dictionary it is an estimate (583) with code 4, hence marked; `gzip` is not part of that first set (`PLAN.md` D4 — re-read 2026-09-16: the registry holds it and the package ships it, but the first release's default set does not). The tag `v1.0.0` and the install pin in `README.md` leading to it, because forty characters would have to be looked up by eye while a tag is constant — the pin has moved on with later releases, and the guard ties it to `installSpec` (R-4.13). The release guard `test/changelog.test.js` holds both sides of the promise: the version in `CHANGELOG.md` is the manifest's version, and the table of numbers is a run of the tool on the fixture rather than a retelling, with the total row equal to the sum of the rows (read 2026-09-16: two checks, the section found by its heading and the table's header row skipped by the cell `Файл` — that cell and the heading `### Что изменится в числах` are the two Russian words the guard holds, so translating `CHANGELOG.md` must touch the guard in the same commit) | `CHANGELOG.md` — a new file (the measurement, the causes, what 1.0.0 leaves out); `test/changelog.test.js` — a new file of checks (2); `package.json` — the version and the list of what ships; `README.md` — the release status, the pin to the tag, a row of the table of files, the guard's fifth home; not one line in the measurement | ✅ “red before the fix” in the form it takes for a new check: without `CHANGELOG.md` the guard fails with `ENOENT` (nothing to check), and every mutation brings down its own check — version `1.0.1` instead of `1.0.0`, the number 186 instead of 185 for `code.js`, a substituted total, a removed section about the numbers. The table's numbers were taken by running the tool twice on the fixture with `--data` rather than derived from a scatter |
| R-4.13 | **The pin in the install example and the silent word in the command line** (found by an end-to-end run; the same class as B1–B3 and N10 — the numbers are right while the path leads into emptiness; the record with the reproduction stands as `BLOCKERS.md` §N13) — ✅ **done 2026-09-14**. Before: `README.md` installed the package at revision `d63468d`, older than every subcommand the text teaches, and an extra word there was silently skipped rather than refused — the help knew flags alone, so a subcommand in such an installation answered zero having done nothing; worse than a refusal exactly in this: an instruction under which the tool says “all is well” to what it cannot do. The fix has four parts: (1) an unknown key, a key without a value (`--config`), `--force` without `--init` and a word after a mode that takes a value are refused with code 2, the culprit named and the fix given, with the arguments parsed **before the project is read** (a refusal must name what a person typed rather than what the project lacks; re-measured 2026-09-16: `--wite` → code 2 with the key named, `--config` → code 2 with the ready command); (2) the install example leads to a revision where the named commands exist **and an extra word is already refused** — the revision with the fix rather than the one at hand, or the text would contradict itself; (3) the guard checks exactly that: the pin leads to a revision of **this** repository and its help knows the commands the text names, the help read out of history (`git show <pin>:src/refusal.js`) rather than from the tree, where it is our own (read 2026-09-16: `test/docs-pin.test.js`); (4) the consumer project's pin was raised to the same revision — that project is outside this tree, so this row keeps it as the record's word rather than as a check of ours. A fifth came when the pin was moved by hand: **pnpm resolves a short sha only through visible refs** (`git ls-remote` hands over branch tips alone), so while the revision is a tip a short pin resolves and on the next commit the install fails with `Could not resolve … to a commit` — checked by trial; the example therefore carries forty characters, or a branch/tag name, and the guard demands exactly that (read 2026-09-16: forty characters or a resolvable name, and the pin equals `installSpec()`). Incidentally the guard caught a fifth divergence of the same class: **this file claimed “77 checks today” against 119** — the number was removed from the second place, because a live number lives where it is checked (`README.md`) | `src/cli.js` **+26** lines (parsing and refusals), `test/cli.test.js` **+45**, the documentation guard **+35**, `README.md` +9 lines about the pin; not one line in the measurement | ✅ the new checks red before the fix (**0 of 2** on clone `4b8dc8d` with the new suites: `--wite`, `--config` without a value, `--force` without `--init` and a mode with a stray word all answered zero). Mutations: the old pin (`d63468d`) — “the revision has no help with the `Команды` section” (the tool prints in Russian); a pin to a revision that exists but does not know the hook (`bb08bba`) — “does not know the commands: install-hook, uninstall-hook”; an invented command in the text — “the documentation calls what the tool does not know: frobnicate”. On the pinned revision the promise holds: the check, `doctor` and `explain` answer zero, `install-hook` installs both files and `uninstall-hook` removes them (`worklog/archive/WORKLOG.md` §35) |
| R-4.17 | **`explain` understands revision names rather than a sha alone** (found by a live check of the release: `size explain HEAD` answered code 2 with “the history has no commit «HEAD»” — the commit exists, and a false cause sent a person to look for the problem in the history; the same class as B1–B3, N8 and R-4.13) — ✅ **done 2026-09-14**. Before: the argument was compared only against the start of a sha over the report's list of commits, so any revision name — `HEAD`, a branch, a tag, `HEAD~1` — looked like a commit that does not exist. Now the name is resolved **by git, at the boundary** (`resolveCommit` in `src/git.js`: `rev-parse --verify --quiet '<name>^{commit}'`), and only when the name is not found does the former search by the start of a sha remain — so an ambiguous prefix keeps its own refusal with the list of matching commits rather than git's message. The rules of names are not rewritten here: `^{commit}` cuts off names leading to something other than a commit (a tag on a blob, a path in the tree), and a name with a leading dash never reaches git as a name — in `rev-parse` it would become a key. Three consequences: (1) `HEAD`, a branch, a tag, `HEAD~1` and a sha give **one and the same answer**; (2) a name of a commit outside the report's history (another branch) gets the **new cause “коммит вне истории”** with the sha named rather than “no such commit” — the commit exists, and that is what a person needs to hear; (3) an invented name and a typo get the true cause — “not a revision name and not the start of a sha”. The tool has no other place accepting a commit (checked: the argument is parsed for `explain` alone), so the pass is bounded by it | `src/git.js` **+14** (the new `resolveCommit` at the boundary), `src/explain.js` +9/−5, `src/cli.js` — the text of the refusal, `src/refusal.js` — the cause in the registry and a line of help, `README.md` — a row of the table of codes, four places about `explain` and a paragraph on how to name a commit | ✅ **red before the fix: 3 of 11** in `test/check.test.js` on the release clone `19c3dd2` — two new checks and a tightened former one (it now demands the real text of the refusal); on the new code all 11 green. The new checks compare the whole answer (`deepEqual`) rather than fields, and tell a non-existent name from a commit outside the history by different causes, making a branch of an empty commit in a clone of its own and returning `main` to its place so that the difference is real. **The live project** (not only the fixture): `explain HEAD` → “only the report itself was touched”, `explain HEAD~1` → “the row is there: the 95th of 95”, a branch name → row 13, a tag → its own answer, a commit past the history of main → code 2 and “this is commit a873960, but it is not in the report's history”, a typo → code 2 and “not a revision name and not the start of a sha”. The suite 127 → 129 |
| R-4.18 | **Every refusal tells the truth, and that is a guard rather than a hope** (audit: a false cause in a text was found by a random live run — B1, B3, the parsing of arguments, `explain HEAD`, four times in a row; the subject here is the whole class rather than the fifth find) — ✅ **done 2026-09-14**. **All** the refusals were walked: the five groups of the registry and every code the tool gives (`1` — a divergence with the tree and the table's check, `2` — the parsing of a call, the settings, history, the hook, measurement, `3` — a truncated history, `4` — approximation, `5` — an internal error). Six places were found where the text lied or was silent about the fix, and **two of them are code rather than wording**: (1) one cause `не git-репозиторий` (“not a git repository, or git is unavailable”) named two different dead ends and neither of them — now the split follows **what git itself said**: `ENOENT` (the program is not on `PATH`) gives the new cause `нет git`, every other failure of `git` gives `не git-репозиторий`, and the text names the directory the tool looks into; (2) the settings check let a column with a non-string path through (`paths: [123]`): the column matched nothing while the report gave zeroes **as success** — now that is a refusal naming the column and a sample of what arrived (one rule: a column's path is a non-empty string). Texts: (3) `нечем звать инструмент` promised “the hook will stay silent” although there is no hook yet; (4) `файл не JavaScript` named the output rather than the evidence — now it says that the **source** text parses neither as a script nor as a module, which clears the stripper; (5) “внутренняя ошибка” did not say this is a defect of the tool rather than of the project; (6) `--init` with no extensions found printed “✗” and returned code 0, **having already written the draft** — the mark and the code said different things. The second and larger half is the **guard**: `tools/refusals.js` holds a line per refusal (the code, the phrases that must be there, and `truth` in words), while the maps `SITES` (throws) and `PRINTED` (printed “✗”) hold **counts of places**, so a new place of refusal changes the count and brings the run down: **a refusal cannot appear without a check**. Two checks share the halves of that promise: `test/refusals.test.js` **calls** the 33 cases carrying a scenario (and, through them, the advice each refusal offers) and compares the codes and phrases, while `test/refusals-catalog.test.js` reads the sources and runs nothing. What stays with a person is named rather than hidden: wordings outside `must`, meaning (`truth` is deliberately not machine-checked — a substring cannot tell a true explanation from a plausible one) and the mark “!”, which is a note rather than a refusal | `tools/refusals.js` — new (measured 2026-09-16: **38** catalogue lines — 33 called by a run, 4 given to other checks, 1 uncheckable; the place maps hold 32: **27** throws and **5** printed), `test/refusals.test.js` — new (1 declaration, grouped by cause inside), `test/refusals-catalog.test.js` — new (2 declarations); `src/config.js` +11 (the difference of causes and the rule for a column's paths), `src/refusal.js` — one cause in the registry, `src/hook.js`, `src/strip.js`, `src/cli.js` — 2–4 lines of text each; `README.md` — a paragraph about the guard, a row in the table of codes, three rows of the table of files; not one line in the measurement | ✅ **red before the fix** (clone `6e00277` with the new files): the catalogue — “the number of refusals with the mark «✗» diverged from the map PRINTED … `src/cli.js`: 5 against 4” (that extra “✗” at `--init`), the refusals — “settings and project / wrong settings: expected code 2, got 1” (a column with a numeric path went into the report as zeroes); not one of the new phrases is in the old code (checked by search). **Mutations** in a copy of the tree, each bringing down its own check: a corrupted phrase in the catalogue — “the refusal has no «git innit»”; a substituted count of places — “the count of places diverges”; a new place of refusal in `src/metrics.js` — “wrong settings (1 in the catalogue, 2 in the sources)”; a phrase removed from a check the catalogue gives a refusal to — “`test/module.test.js` has no claim…”; a new “✗” in `src/check.js` — “diverged from the map PRINTED”; a renamed printed refusal (the count unchanged) — caught by the other half: “the refusal has no «покрытие:»”; the `ENOENT` difference removed — “the refusal has no «git не запустился: его нет в PATH»” (the guard quotes the printed text verbatim, which is Russian); a catalogue case whose name diverged from the cause's name — “the place of refusal has neither a case in the catalogue nor a named reason why it is not caught” (that comparison closed a hole of the guard itself: the count of places catches a new place but not a place without a check). **A live run** (repeated 2026-09-16 in `/private/tmp/notarepo3`): an environment without git in `PATH` → code 2 and, verbatim, “✗ git не запустился: его нет в PATH (таблица собирается по его истории, а смотрю я в /private/tmp/notarepo3).” — while the same directory with git on `PATH` gives the other cause, “git не видит здесь репозитория … (сейчас смотрю в /private/tmp/notarepo3)”. The suite 129 → **132** (the fast one 56 → 58) |
| R-4.19 | **Release `1.1.0` and the consumer on a release** (`PLAN.md` §8.4/§10) — ✅ **done 2026-09-15**, and not a cleaning: the making of a release and bringing a foreign project to what is now true (the repository became public — `worklog/archive/WORKLOG.md` §44). The release gathers what live runs found after 1.0.0, which all lies **outside the measurement**: `explain` understands revision names, the refusals have their catalogue with a guard and a new cause `нет git`, a column with a non-string path is refused instead of being reported as zeroes, and `--init` no longer prints “✗” at code 0. The consumer moved from a revision from the middle of the work (`6530237`, seven commits behind the release) to the tag `v1.1.0`, and the step with a read-only deploy key was removed from both of its jobs — together with the texts that promised it (`AGENTS.md`, `README.md`, `docs/ROADMAP.md`). Why a new release rather than 1.0.0: the tag `v1.0.0` (`19c3dd2`) is older than the fix of revision names (`6e00277`), that is, `explain HEAD` refused in it — the question was put by the widget and the answer was to release 1.1.0 | `package.json` — the version; `CHANGELOG.md` — the section of the release with a table of numbers of its own; `README.md` — the status, the pin of the example, the name of the tarball, a row of the table of files; not one line in the measurement | ✅ **the numbers did not move, and that is proven twice:** the release's table matches the table of 1.0.0 to the last cell (the release guard compares it with a live run), and the consumer's committed table (211 KB) was rebuilt by the 1.1.0 release into the same bytes — `sha256 863ce3e9…`; after the commits of that pass the table was rebuilt by **commits of its own** (97 rows, 215 KB; then 98 rows, 216 KB after the roadmap was edited — live text named the old release `#v1.0.0` as the working way to install) under the project's own rule. The consumer was checked by its own means: an install from scratch without credentials takes 1.4 s, `test:all` — 7 steps green, `test:sizes` — 98 × 25 agrees, `lint:strict` — 0, `explain HEAD` — code 0 (in 1.0.0 code 2), `doctor` and `check` answer as before, and `ci.yml` holds no `secrets.` at all (there were two) |
| R-4.20 | **Обещания про действия пользователя сверены с фактом** (найдено прошлой записью, проверено здесь: поручение «удалить секрет `SIZE_REPORT_KEY` в настройках потребителя» было **вымышленным** — вход с правами ADMIN показал ноль секретов в трёх семьях, ноль ключей доступа, ноль окружений и вебхуков, а шаг с ключом не выполнялся ни разу: коммит, который его добавил, не отправлялся) — ✅ **сделано 2026-09-15**, и это проход по фактам, а не чистка: ни числа, ни измерение не тронуты. Проверено **всё**, где документ или журнал говорит, что остаётся пользователю или требует его действия, и каждое утверждение — против прогона или настройки, а не против памяти. **Пять мест оказались неверными и починены:** `README.md` §8 п. 5 «Дать доступ к пакету в CI перед установкой» (доступа не требуется ни локально, ни в CI — пункт убран, нумерация сдвинута, рядом сказано почему); `README.md` §7 «постоянной проверки доков в пакете пока нет» (сторож документации работает с R-4.1 — переписано на то, что есть); `PLAN.md` §8.4 (пример установки вёл на `#v1.0.0`, а выпуск — `v1.1.0`: тег стал нейтральным, действующий назван в `README.md` под сторожем); `PLAN.md` §8.5 (список шагов CI называл `pnpm test`, тогда как CI гоняет `pnpm test:all` — набор разделён R-5.7); `README.md` §«Статус» («ставит пакет из git по коммиту» — пин там тег выпуска). **Что подтвердилось:** «публикация в npm — отдельное решение» (манифест `private: true`, в реестре пакета нет), «CI пакета — семь шагов» (в `ci.yml` ровно семь именованных плюс три служебные установки), «секретов не требует», «файла лицензии нет». **Находка с ценой:** имя `size-report` в npm занято чужим пакетом (2017 год, три версии) — публикация под ним невозможна, а `npx size-report` без установленного пакета разрешается в чужой; сказано в `PLAN.md` §8.4. **Не взято осознанно:** таблица открытых вопросов `PLAN.md` §10 — это решения автора, а не действия пользователя, и часть её строк не проверена; сторож всё ещё читает только пример установки в `README.md`, поэтому второй пин может пройти мимо него | `README.md` — 4 места (пункт списка убран, абзац §7 переписан, строка про npm и слово «коммит»), `PLAN.md` — 2 места (пин и шаг CI) плюс цена имени в §8.4, `WORKLOG.md` — §46 | ✅ каждое «остаётся за пользователем» названо со способом проверки: `gh api` настройки репозитория (ADMIN, приватный) — секреты, ключи, окружения и вебхуки по нулям; `git branch -r --contains` — коммиты с шагом ключа не отправлялись; `rg` по всему дереву вместе с игнорируемыми файлами — одна ссылка, и та журнальная; `npm view size-report` — имя занято; счёт шагов `ci.yml`; `ls LICENSE*`. Починено ровно то, что не подтвердилось, ни одно верное утверждение не переписано, новых обещаний не добавлено. 132/132 в двух средах, линтер — 0, `pack:check`, `parity:live` (95 × 27, `sha256 1bdb27e14bac`) и `check:standards` (4 из 4 + 3 из 3) зелёные, **эталоны не переснимались** |
| R-4.21 | **Совет называет путь внутри проекта, а не имя пакета** (нашлось справкой по именам, R-4.20: имя `size-report` в npm занято чужим пакетом 2017 года, поэтому совет `npx size-report` в проекте без установленного пакета — не отказ, а запуск **чужого** кода: текст, который должен выручать, вредит) — ✅ **сделано 2026-09-15**. Советов было четыре места, и все четыре правились: умолчание `DEFAULT_CONFIG.fixCommand` (его цитирует подпись отчёта у конфига, написанного руками без `fixCommand`), черновик `--init`, подсказка и справка (`invocation()` в `src/refusal.js`) и шаблоны (`size-report.config.json`, `ci.yml`, `README.md`). Форма теперь одна — **путь внутри проекта** (`node node_modules/<имя>/bin/size.js`): при установленном пакете он делает обещанное, без пакета отказывает **на месте** (`Cannot find module …`, код 1) и в сеть не идёт вовсе, потому что менеджер пакетов в зове не участвует. Решение `invocation()` принимает **по дереву, а не по своему пути**: `fs.existsSync` от текущего каталога вместо прежней проверки `node_modules` в `__filename` — та отвечала про то, откуда движок, а не про то, куда смотрит читатель. Обе половины обещания сторожатся не числом, а зовом: `test/module.test.js` выполняет **ту строку, что напечатал инструмент**, в проекте с пакетом и в проекте без него (реестр направлен в никуда — если зов пойдёт в сеть, это видно), `test/templates.test.js` держит форму `fixCommand` в черновике и в шагах `ci.yml`, `test/docs-commands.test.js` — четвёртое обещание документации: ни один зов в `README.md` и `templates/README.md` не идёт по имени пакета | `src/refusal.js` — `invocation()` решает по дереву (5 строк против 3), `src/config.js` и `src/cli.js` — по строке совета, `templates/README.md`, `templates/ci.yml`, `templates/size-report.config.json` — по строке-две (имя осталось только там, где объясняется ловушка, и в виде `<имя>`); `test/module.test.js` +45, `test/templates.test.js` +9, `test/docs-commands.test.js` +20, `tools/docs-facts.js` +5; в измерении — ни строки | ✅ **красное до правки** (копия дерева с исходниками HEAD и новыми проверками): `test/module.test.js` — **4/5**, «совет ведёт не путём внутри проекта: `npx size-report --init`»; `test/templates.test.js` — **2/3**, «черновик советует не путь внутри проекта: `npx size-report --write`»; `test/docs-commands.test.js` — **3/4**, «`templates/README.md`: «npx size-report --write»». Обе половины совета показаны живым прогоном: в проекте с пакетом зов из подсказки создаёт черновик (код 0), в проекте без пакета тот же зов — код 1 и `Cannot find module '…/node_modules/size-report/bin/size.js'`; для контраста `npx size-report` в том же каталоге отвечает `FetchError: request to http://127.0.0.1:9/size-report failed`, то есть по имени зов действительно уходит в реестр. **Числа не поехали:** оба замороженных эталона и закоммиченный отчёт потребителя называют команду **своими настройками** (`fixtures/synthetic/config.json`, `fixtures/parity/config.json` и `size-table.config.json` потребителя несут `fixCommand` явно), поэтому умолчание в них не попадает; артефакт 22 112 Б и страница 59 358 Б побайтово, `parity:live` — 95 × 27 и `sha256 1bdb27e14bac`, `check:standards` 4 из 4 + 3 из 3, набор **132 → 133** (быстрый 58 → 59), 133/133 в двух средах, `lint:strict` — 0, `pack:check`, эталоны не переснимались |
| R-4.23 | **Проверка называет другой счёт, а вердикт остаётся за расхождением** (найдено прогулкой по README с пакетом из реестра, `WORKLOG.md` §54) — ✅ **сделано 2026-09-15**. Было: отчёт собран с настоящим минификатором, а прогон идёт без него (`SIZE_REPORT_NO_OPTIONAL`, платформа без `esbuild`) — числа честно расходятся, и `size` отвечал **кодом 1** «таблица размеров расходится с историей», называя первой разошедшуюся строку подписи, а о другом счёте **молчал вовсе**; сборка (`--write`) в том же окружении отвечает кодом 4 и обе заметки печатает. Стало: заметка о датчике печатается **всегда** — и в размерной проверке, и в `size check` (покрытие), — а вердикт остаётся первым по важности. **Правило записано:** нарушение старше приближения (тот же порядок у `size check` и у `doctor`) — код 4 утверждал бы, что расхождение объясняется датчиком, а этого никто не проверял: расхождение может быть и настоящей правкой мимо отчёта. Место правки оказалось не тем, что называл разбор: причина была не в `check.js`, а в том, что режимы **не сводили вердикт с заметками** — теперь это один помощник `verdict(code, gaps)` в `src/cli.js`, через который идят оба режима, и второй такой связки в пакете нет | `src/cli.js` — помощник `verdict` (8 строк) и по строке в двух режимах вместо развилки в каждом; `test/minify.test.js` — одна проверка (три зова: сборка с минификатором, проверка без него, покрытие без него); `README` — §7 и таблица кодов | ✅ **красное до правки:** новая проверка на коде HEAD падает на «другой счёт не назван, и расхождение осталось без причины», показывая ровно тот текст, что видел пользователь; на новом коде — **9/9**. Числа не поехали: артефакт 22 112 Б, страница 59 376 Б, `parity:live` 95 × 27, `check:standards` 4 из 4 + 3 из 3, набор **134 → 135**, `lint:strict` — 0 |
| R-4.22 | **Совет отказа проверяется исполнением** (нашлось прошлым проходом, R-4.21: подсказка звала по имени пакета из реестра — текст, который должен выручать, вредил; предмет здесь — весь класс «совет неисполним», а не следующая находка) — ✅ **сделано 2026-09-15**. Было: отказ обязан назвать причину, и это сторожилось (R-4.18), а **что он советует** — нет. Стало: у каждого случая каталога объявлено, что он советует (`advice`), и совет **выполняется** в том состоянии, которое его напечатало. Починено пять мест, и все — по замеру. (1) Совет **повторяет настройки, которые человек назвал** (`src/cli.js`): без этого в проекте с другим именем файла настроек отказы `--wite`, `check --write`, `--page a b` звали команду, которая ищет умолчательное имя, не находит его и уводит во **второй** отказ — про файл, которого у человека нет; путь берётся в кавычки, если в нём пробел (`advicePath` в `src/refusal.js`). (2) `loadConfig(file, root)` — совет «создайте его: `--init`» называет **тот файл, который искали**, и молчит об имени ровно тогда, когда оно и так умолчательное. (3) «конфиг уже есть» советует `--init <файл> --force` тем же файлом, о котором шла речь, а не умолчательным. (4) Из совета минификатора убрана половина «считайте прежним способом (`engine: strip`)»: **измерено** — на `.js` она передаёт тот же файл гарду `minify.guard` (второй отказ, код 2), на `.ts` молча уходит в построчную подрезку; остался выход, который этой причине отвечает, и он же проверен прогоном (`minify.ext` для `.js` — код 0, точность `approximate`). (5) «перенос состояния пропустил правку» больше не советует пересборку: **измерено** — команда из настроек выполняется, а отказ после неё остаётся, потому что расхождение не в таблице; вместо неё назван разбор (`git show HEAD:<путь>`). Совет хука «нечем звать инструмент» называет установку из манифеста (`installSpec()`, новое поле `repository`), а не `add -D <имя>`: имя в реестре занято, и это то же обещание, что в `README.md` — обе стороны сверяет `test/docs-pin.test.js`. Механизм держит класс сам: маркеры совета (`починка:`, `создайте его:`, `соберите её:`, `локально:`, `в CI:`) вынимают совет **из вывода**, поэтому новый совет в существующем отказе виден проверке, а не глазу; у случая без совета — `advice: []`, и это тоже обещание. | `tools/refusals.js` **228 → 340** (+112: объявления советов и разбор маркеров), `test/refusals.test.js` **199 → 338** (+139: исполнитель), `test/refusals-catalog.test.js` 92 → 118, `test/disk.test.js` 274 → 321, `test/module.test.js` 261 → 289, `test/minify.test.js` 253 → 258, `test/docs-pin.test.js` +10; `src/cli.js` +21/−12, `src/config.js` +8/−3, `src/tool.js` +11 (`installSpec`), `src/hook.js` +7/−2, `src/minify.js` +4/−2, `src/history.js` +5/−1, `src/refusal.js` +6 (`advicePath`), `src/doctor.js` 1/−1 (`loadConfig` с корнем), `package.json` +4 (поле `repository`); в измерении — ни строки | ✅ **красное до правки** (копия дерева: исходники HEAD, проверки из рабочего дерева): `test/refusals.test.js` — ««командная строка / два режима сразу»: отказ напечатал совет, которого нет в каталоге: node …/bin/size.js --write»; `test/minify.test.js` — проверка 6 из 8 (в тексте остался снятый совет `"engine": "strip"`); `test/disk.test.js` — проверка 2 из 14 (совет пересборки вместо разбора, и то, что пересборка отказа не убирает). На новом коде **133/133** в двух средах (26,2–28,6 с: четыре прогона одного дерева в разных окнах, загрузка машины гуляла 15…75 из-за чужой сборки). **Исполнено 23 команды совета** (21 объявленная плюс две проверяемые половины «не-команд»), из них **5 с проверкой «отказ ушёл»**: `--init @missing`, `git init`, `git fetch --unshallow`, `@fixNotable`, `@fixDrift` — после каждой тот же зов, что отказывал, отвечает другим. Не выполняется то, что выполнить нечем, и это названо: **12** советов — действие человека (правка настроек, коммит, установка зависимостей и git, выбор из подходящих коммитов), у каждого сказано почему; **4** — форма с подстановкой (`<файл>`, `<коммит>`), где имена команд и ключей сверяются со справкой; **6** отданы другой проверке с названным файлом и строкой. Числа не поехали: артефакт 22 112 Б и страница 59 358 Б побайтово, `parity:live` — 95 × 27 и `sha256 1bdb27e14bac`, `check:standards` 4 из 4 + 3 из 3, `lint:strict` — 0, `pack:check`, эталоны не переснимались |
| R-4.24 | **Выпуск из CI по тегу — без секрета и без одноразового кода** (завершение шага 6: публикация не должна зависеть от человека с аутентификатором) — ✅ **сделано 2026-09-15**. Выпуск 1.1.1 стоил ручных шагов: вход в браузере, вторая ступень по security key, подтверждение по одноразовой ссылке — и так каждый выпуск, а токен в настройках репозитория пришлось бы заводить и ротировать. Стало: `.github/workflows/release.yml` — `push` тега `v*` прогоняет тот же полный набор, сверяет версию манифеста с тегом, проверяет работу из собранного пакета и отправляет его в реестр по **удостоверению GitHub Actions** (trusted publishing: `id-token: write`, ни одного `secrets.`, ни `--otp`); prerelease уходит в `next`, а не в `latest`. Кнопка тоже есть, но по умолчанию она **черновая** (`workflow_dispatch` с `dry_run: true`): собирает и печатает, что уехало бы, ничего не публикуя (и она же показала, что настроен ли издатель — так не проверяется: см. ниже). **Одна настройка остаётся человеку и делается один раз:** на npmjs.com в настройках пакета указать издателя — владельца, репозиторий и файл рабочего процесса. Сторожит обещание `test/release.test.js` (3 проверки), и он **разбирает** описание, а не ищет в нём подстроки: выпуск начинается тегом, версия берётся из манифеста, секрета и кода нет, права ограничены, черновой режим не публикует, перед публикацией идёт полный набор, а подсказка называет **этот же** файл — иначе настройка на npmjs.com отправила бы владельца в другой рабочий процесс. Разборщик подмножества YAML вынесен из сторожа шаблонов в `tools/yaml.js` — один разборщик на два описания (шаблон для чужого проекта и выпуск), и в нём новое правило: двоеточие с пробелом в незакавыченном значении — ошибка. Правило появилось по факту, а не из вкуса: первая редакция описания несла `? … : …` в команде публикации, YAML этого не разбирает вовсе, а подстрока находилась и проверка была зелёной | 3 новых файла: `.github/workflows/release.yml` **85** строк (из них 14 — шаги, остальное — почему так), `test/release.test.js` **117** и `tools/yaml.js` **109** (разборщик, вынесенный из `test/templates.test.js`: 243 → **166**, копия не разошлась); `README.md` — абзац о выпуске и три строки таблицы файлов, `tools/suites.js` — строка в `SLOW` с причиной; набор проверок **131 → 134** (три прежних те же — изменилось то, как они читают файл) | ✅ 134/134 в двух средах, `lint:strict` — 0, `pack:check`, `check:standards` (4 из 4 + 3 из 3) и `parity:live` (95 × 27) — зелёные, числа отчёта и оба эталона не тронуты. **Первая же отправка нашла настоящий дефект, и это лучшая часть приёмки:** раннер упал на новом файле через ноль секунд («workflow file issue»), тогда как у себя прогон был зелёным: подстрока в описании находилась, а YAML файл не разбирался вовсе. Причина — `? … : …` в незакавыченной команде публикации; команда переписана (метка выпуска — из `contains(github.ref_name, '-')`), а проверка научена разбирать описание тем же разборщиком, что и шаблон. **Мутация:** тот же `? … : …` возвращён на место → `test/release.test.js` даёт **0/3** с точной причиной («строка 81: двоеточие с пробелом в незакавыченном значении — YAML прочитает это как конец значения»), а `actionlint` (внешний к проекту, локальный, в CI не входит) называет ту же строку и то же место — две независимые проверки сходятся. **Черновой прогон на раннере сделан — и дважды поучительно.** Первый (`34919603067`, 37 с) прошёл полный набор и работу из тарболла, а упал на «cannot publish over the previously published versions: 1.1.1»: черновой прогон на выпущенной версии красный всегда, поэтому версия в нём теперь черновая надстройка (1.1.1 → **1.1.2-draft.0**, `--no-git-tag-version`, только в рабочем каталоге раннера), и это требует сторож. Главное же он показал про сам инструмент: `npm publish --dry-run` **не проверяет учётные данные** (в пустом каталоге — код 0 без токена), значит настроен ли издатель, черновой прогон не показывает, и README это теперь говорит прямо вместо прежнего обещания. Второй прогон (`34919783952`, **27 с**) зелёный целиком, собрал `@vernikr/size-report@1.1.2-draft.0` (38 файлов) и **ничего не отправил** — реестр после него по-прежнему только `1.1.1`. **Настоящий тег пройден, и этим закрыт последний предел: выпуск `1.2.0` ушёл из CI по удостоверению.** Run `34920712770`, 44 с: `+ @vernikr/size-report@1.2.0`, provenance подписан и записан в журнал прозрачности, реестр отдаёт `latest: 1.2.0`, `npm access get status` — `public` — без токена, без `--otp`, без единого `secrets.`. Он же нашёл **две** причины, по которым черновая попытка ничего не говорит: (1) издателя у пакета не было вовсе — он заведён командой `npm trust github @vernikr/size-report --file release.yml --repo vernikr/size-report --allow-publish` (id `1787f6da…`, права `publish` и `stage publish`), то есть одноразовая настройка делается не только кнопкой; (2) `actions/setup-node` с `registry-url` пишет в `.npmrc` строку `_authToken=${NODE_AUTH_TOKEN}`, и npm, видя заполненное поле учётных данных, за удостоверением OIDC не идёт — публикация падает 404 при верно заведённом издателе. `registry-url` из шага убран (реестр и так по умолчанию тот же, а явный адрес живёт в `publishConfig`), сторож `test/release.test.js` требует, чтобы шаг его не получал; **мутация:** возвращённый `registry-url` роняет именно эту проверку (3 → **2 pass / 1 fail**), после возврата файла — 3/3. Кнопка с выключенным `dry run` так и не нажималась: её место занял настоящий тег |
| R-4.2 | **Инструкция «как подключить инструмент к новому проекту»** — ✅ **сделано 2026-09-14**, раздел `README.md` «Wiring it into your project» (место закреплено, `PLAN.md` §8.4 ссылается сюда). Написана не из планов, а из прогона: шесть шагов (установка → черновик настроек → правка конфига → скрипты и первый отчёт → проверка в CI → коды выхода), таблица ключей конфига, только проверенные коды выхода и отдельный раздел «Ловушки, найденные этой же инструкцией». Эта же инструкция — рецепт для шага 5 | +95 строк README (раздел целиком), из них 4 пункта — дефекты, найденные прогоном (R-4.5…R-4.8) | ✅ пройдена покомандно в свежем репозитории (три коммита, ESM в `src/`, `pnpm`); протокол — `WORKLOG.md` §16 |
| R-4.3 | Числа приёмки в доках приводятся к факту после каждой волны — ✅ **сделано 2026-09-14**, обновляется каждым проходом: `README.md` говорит о 50 проверках и двух средах, как и вывод прогонов; устаревшее «27 проверок, четыре среды» ушло ещё в волнах 1–3 | — | цифры README совпадают с `pnpm test` (50/50) и `parity:live` (2 среды) |
| R-4.4 | `REFACTOR.md` этот — в таблицу файлов `README.md` — ✅ **сделано** | — | ссылка существует |
| R-4.5 | **Найдено прогоном: подкоманд нет.** Инструкция обещала `size init`/`size update`, а CLI знает только флаги: `size init` в свежем проекте падает кодом 2 («нет файла настроек») — текст расходился с реальностью | текст README исправлен на `size --init`; **поведение не менялось** | — | реализация подкоманд — шаг 5 (`PLAN.md` §5), там же `doctor`/`explain` |
| R-4.6 | **Найдено прогоном: модуль в расширении `.js` роняет метрику `min`** — ✅ **сделано 2026-09-14**. Причина: гард компиляции пробовал только скрипт (`vm.Script`), а проект с бандлером пишет `export` в `.js`; прогон вылетал кодом 5 со стеком «стриппер испортил src/greet.js: Unexpected token 'export'» — то есть обвинял стриппер в том, что произошло до него. Починка: **форма определяется по тексту** (`^import|export` и `.mjs`), результат принимается, если разбирается **хотя бы одним** способом — скриптом или модулем; модуль разбирает сам Node через `node --check` по временному файлу `.mjs`. От этого гард не слабеет: настоящая поломка не разберётся ни тем, ни другим. Второй случай, который был тем же дефектом, теперь отказ с командой: если **исходный** текст тоже не разбирается, значит в графе не JavaScript (разметка или типы в `.js`) — код 2, названы причина и `minify.guard`/`minify.ext` | +38 строк в `src/strip.js` (гард вырос на второй разбор и различие «своя поломка / чужая графа»), −2 в `metrics.js`/`refusal.js` | ✅ `test/module.test.js` (5 проверок): оба варианта ESM-проекта (с `type: module` и без) собираются конфигом прямо из `--init` без правок, код 0; мутация «стриппер перестал понимать одинарные кавычки» роняет прогон честным текстом; JSX в `.js` — код 2 с причиной и командой; все пять проверок красные на коде до правки (`git worktree` на `b283122`) |
| R-4.7 | **Найдено прогоном: подсказки цитируют внутренний путь** — ✅ **сделано 2026-09-14**. Было: отказ предлагал `node node_modules/.pnpm/size-report@git+https+++…/node_modules/size-report/bin/size.js --init`, а `--help` печатал `node bin/size.js`. Стало: `invocation()` в `src/refusal.js` различает репозиторий пакета и установку — в чужом проекте подсказка и справка говорят `npx size-report --init` (npx запускает уже установленный пакет и в сеть не идёт) | 0 строк (переписаны 6) | ✅ проверено в наборе `test/module.test.js` (движок, положенный на место установки) и руками: `npx size-report --init` из подсказки создаёт настройки, `--write` по ним собирает таблицу |
| R-4.8 | **Найдено прогоном: мелочи первого запуска.** ✅ **сделано 2026-09-14** в R-4.9: подсказка `--init` больше не обещает «проверки едут вместе с пакетом» (их там нет — есть команда), а `DEFAULT_CONFIG.fixCommand` называет существующее имя пакета (`npx size-report --write`) вместо старого `npx size-table --write`, которое попадало в подпись отчёта у конфига, написанного руками без `fixCommand`. Осталось непринципиальное: `--init` не правит `.gitignore` (этого он и не обещает), а отчёт в потребителе решили оставить в git | подсказки — 0 строк (переписаны 2), `DEFAULT_CONFIG` — 0 строк (переписано значение) | текст отказа совпадает с фактом; `pnpm test` — те же 51 проверка |
| R-4.12 | **Полнота покрытия и объяснение пропущенной строки** (`PLAN.md` шаг 5, требования §4.2) — ✅ **сделано 2026-09-14**. Два ответа, которых не было: «всё ли в истории попало в отчёт» и «почему у этого коммита нет строки». Стало два модуля: `src/check.js` (полнота: объединение изменённых путей всех коммитов делится на три части — отслеживаемые колонками, исключённые (`skip` и файл отчёта) и **незнакомые**; незнакомые — код 1, с путём, коммитом-первопричиной и готовой починкой; тем же ответом идёт сводка по выпавшим коммитам с причинами и их sha) и `src/explain.js` (причина пропуска берётся у того же прохода, что и отчёты, улики — из списка изменённых путей коммита; различает четыре вида: слияние скрыто `rows.merges`, тронут только отчёт, ни одного файла колонок и «числа не сдвинулись»). Команды — словами (`size check`, `size explain <sha|префикс>`, у каждой `--json`), читаются любым аргументом, который не ключ и не его значение; неизвестное слово — код 2, команда вместе с режимом (`check --write`) — отказ. `skip` остаётся одним списком: он же и объявление исключений полноты (второй ключ не заводился). Внутри движка причина пропуска стала ключом — **строки контракта не тронуты** (`--json`, `--data` и артефакт побайтово те же). Проверки — `test/check.test.js` на настоящих коммитах фикстуры | Это возможность, а не чистка, и минуса здесь не обещалось. Счёт — непустые строки всех `src/**/*.js` и `src/**/*.css`: **2988 → 3311** (+323), из них кода **2180 → 2427** (+247), остальное — комментарии. По файлам (строк всего): `src/check.js` 0 → **142**, `src/explain.js` 0 → **98**, `src/cli.js` 265 → 348, `src/history.js` 187 → 202 (ключи причин и `skipLine`), `src/refusal.js` 68 → 75 (справка), `src/data.js` 104 → 105 (строки те же, собираются из ключей), `test/check.test.js` 0 → **168** | ✅ **83 → 92 проверки**: красное до правки доказано клоном `00eee02` с новым файлом — **0 из 9**, все падают на неизвестной команде (код 2); зелёное — 92/92 в двух средах. Случаи — из фикстуры, а не выдуманы: непокрытый путь `README.md` с коммитом `3294a69`, «только отчёт» `cd78fd9`, «числа не сдвинулись» `9fee206` (назван файл колонки `src/code.js`), «мимо колонок» `9dfe679` (журнал колонкой не отслеживается), слияние `9326134` при `rows.merges: false`; починка настроек (путь колонкой или в `skip`) закрывает нарушение и **не двигает числа отчёта** (14 строк в обоих вариантах), а коммит со строкой отвечает номером строки и не выдумывает непокрытых путей. На живой истории потребителя ответ говорит правду о его настройках: `check` — **20 путей мимо колонок** (`icons/*.svg`, `.gitignore`, `CHANGELOG.md`, `tests/golden/…`, сам `size-table.config.json`), 149 коммитов, 95 строк, выпало 54 (49 «только таблица» + 5 «без изменения объёма»), код 1, **1,5 с**. Эталоны не пересняты: `parity:live` — те же 95 × 27, 225 673 Б и sha256 `1bdb27e14bac`; `--json` и артефакт побайтово те же; `lint:strict` — 0, `pack:check`, `check:standards` — зелёные |
| R-4.11 | **Шаблоны для подключаемого проекта** (`PLAN.md` шаг 5) — ✅ **сделано 2026-09-14**. Три файла в `templates/` (10 104 Б): `size-report.config.json` — черновик настроек с колонками-примером (`README.md` и `package.json` есть почти в любом проекте, поэтому первый отчёт собирается сразу), тремя метриками и `fixCommand: npx size-report --write`; `ci.yml` — описание проверки (сборка таблицы и сверка с файлом на диске, два снимка чисел — обычный и в среде без настроек git — и их сравнение, секретов не требует, шаг с deploy key для приватного пакета закомментирован примером); `templates/README.md` — куда что кладётся, что менять и чего в шаблонах нет намеренно. **Блок для `AGENTS.md` не сделан сознательно** — требования такого файла не просят (в разделе 6.3 требуется машиночитаемый вывод, а не файл для агента), выдумывать формат чужого репозитория инструмент не должен; это сказано в `templates/README.md`, а в `README.md` — в «What is not here yet». Шаблон под проверкой (`test/templates.test.js`): черновик проходит ту же проверку настроек, которой его встретит первый запуск, и собирает настоящий отчёт; описание проверки разбирается разборщиком подмножества YAML и зовёт только те команды и ключи, что есть в справке; записка обязана называть свои файлы и то, чего в шаблонах нет. Файлы едут в поставке (`files` в `package.json`) — `pack:check` сравнивает каталог шаблонов наравне с `src`: состав и байты каждого файла | +3 файла шаблонов (10 104 Б), +1 файл проверок (`test/templates.test.js`, 14 КБ, из них разборщик YAML ~55 строк); `package.json` — одна строка (`templates` в `files`), `tools/pack-check.js` +14 строк (цикл по двум каталогам и побайтовая сверка шаблонов) | ✅ **приёмка прогоном в свежем репозитории**, а не на словах: `/tmp/fresh-tpl` — `git init`, два коммита, `pnpm add -D ./size-report-0.0.0.tgz`, `cp` обоих шаблонов как есть и ни одной ручной правки: `pnpm exec size --write` → таблица на 3 строки × 2 файла (8 КБ), `pnpm exec size` → **код 0** и сверка с историей, два снимка `--data` (3 314 Б) в обычной среде и с `GIT_CONFIG_GLOBAL=/dev/null` — `diff` пуст, а `npx size-report --write` из `fixCommand` работает без сети (код 0). Три новые проверки красные до правки (клон `9b0d592` с новым файлом: **0/3**, все три падают на отсутствующих шаблонах). Стерегут результат: `pack:check` — список `files` называет только существующее, в тарболле 23 исходника и **3 шаблона**, каждый шаблон в пакете побайтово тот же; `pnpm test` — **83/83** в двух средах; `lint:strict` — 0; артефакт 22 112 Б и страница 59 358 Б, `parity:live` 95 × 27 и 225 673 Б со `sha256 1bdb27e14bac` в двух средах, `check:standards` 4 из 4 + 3 из 3 — **эталоны не переснимались, числа отчёта не менялись** |
| R-4.10 | **Найдено CI: проверка эталонов зависела от версии git, а бандл живой истории — от его догадки** — ✅ **сделано 2026-09-14**. Второй прогон CI (первый закрыт в §9, R-3.5) упал на шаге воспроизводимости эталонов дважды. (1) `check-standards` сравнил `history.bundle` **побайтово**: упаковку пишет git, её байты зависят от версии, и проверка была зелёной на macOS и красной на ubuntu (`README` фикстуры это же и утверждает). Стало: побайтово сверяется только написанное нами (`README.md`, `config.json`, `golden.json`, `artifact.sha256`), у бандла — содержимое (`git bundle list-heads` плюс число коммитов из клона), манифест — по полям, потому что в нём записан хеш бандла. (2) `fixtures/live/history.bundle` лежал без `HEAD`: клон сам выбирал, какую ветку выложить, и на другой версии git печатал `hint: Using 'master' …`, а первая строка этого замечания становилась текстом нашего отказа — настоящая причина оставалась за ней. Стало: бандл пересобран с `HEAD` на ревизии эталона (803 581 Б вместо 791 182 Б, ревизия та же — `bd6ef9d`, 149 коммитов, 95 × 27), `check:standards` требует `HEAD` и ветку `main` у бандла, `execFileSync` в `parity-freeze` больше не дублирует stderr git в наш собственный, а отказ печатается целиком (до 12 строк) | `check-standards.js` 79 → 181 строка (+102), `parity-freeze.js` 205 → 210, `fixtures/live/README.md` 28 → 32, бандл 791 182 → 803 581 Б | ✅ мутации: правка конфига фикстуры роняет 4 утверждения с названной строкой; бандл без `HEAD` — «`history.bundle` больше не заменяет проект»; бандл с другой ревизией — 2 утверждения и настоящий текст причины. Локально весь список CI в том же порядке: `lint:strict` 0, `pnpm test` **53/53** в двух средах, `pack:check` (20 исходников, артефакт 22 112 Б и страница 37 193 Б), `parity:live` 95 × 27 в двух средах, `check:standards` 4 из 4 + 3 из 3 + ветка `main` на `bd6ef9d` |
| R-4.9 | **Обещания пакета сведены к факту** — ✅ **сделано 2026-09-14**. Список поставки называл четыре пути, которых в репозитории нет (`dist/`, `templates/`, `CHANGELOG.md`, `LICENSE`) — убраны, а `pnpm run pack:check` проверяет его с двух сторон: в списке нет несуществующего (мутация «вернуть `dist` в `files`» роняет ровно это утверждение) и в тарболл не попадает необещанное (при этом автоматические включения npm — манифест, README, лицензии — лишними не считаются: иначе проверка ругалась бы на обещанный планом `LICENSE`). Снятие обоих эталонов оживлено: инструментом стала встроенная копия (`fixtures/legacy/size-table.cjs`) вместо удалённого шагом 5 файла в потребителе, ревизия проекта берётся из манифеста, окружение снятия закреплено (`core.quotePath=false` — без него фикстура теряет строку, и эталон молча укорачивается) | — | пересъём побайтово: `data.json`, `config.json`, `artifact.sha256`, `golden.json`, `history.bundle` — те же; в манифестах изменилось только имя инструмента, а в README фикстуры — строка о встроенном инструменте и восстановленная из генератора CRLF-заметка (её правили руками после снятия — расхождение с манифестом теперь стережёт `test/frozen.test.js`) |

## 7. Волна 5 — производительность (движок, затем тесты)

Движок на живой истории уже приемлем: `--page` 107 КБ за **2,9 с**, `--data` 203 КБ,
`--check` зелёный. Поэтому здесь только то, что не меняет поведения, а настоящая
цель — тесты.

| # | Пункт | Приёмка |
|---|---|---|
| R-5.1 | **Один проход по истории на вызов.** Сейчас `--write`, `--page` и `--data` строят историю каждая; внутри одного вызова повторных проходов быть не должно | время каждого режима не выросло; `--page`/`--data` совпадают побайтово с эталоном |
| R-5.2 | **Скорость тестов — ✅ сделано волной 3** (R-3.1–R-3.3, замеры там же): `pnpm test` 15,7 → **5,6 с**, сумма длительностей 15 074 → 5 328 мс, `parity:live` 23,8 → **13,3 с**. ⚠️ **Бюджет переснят 2026-09-14 (R-4.9):** цель ≤ 7 с бралась на 39 проверках, к 51-й набор стоит 6,9 с (запас 0,1 с), поэтому цель — `≤ 10 с` с запасом на следующую проверку; таблица целей — в конце §3 | — |
| R-5.3 | **Среды живой сверки — на одном клоне — снят по факту** (R-3.3): обе среды собирают артефакт (`--write`) в своём дереве, и на общем клоне это была бы гонка за один и тот же файл. Диагноз пункта был неверен, и время взято не экономией клона, а тем, что среды идут вперемешку: 23,8 → 13,3 с при двух клонах, как и было | — |

| R-5.4 | **Цена разбора модуля** (появился с R-4.6) — ✅ **сделано 2026-09-14**. Было: гард разбирал модуль запуском `node --check` на каждую клетку — **97 мс на запуск**, а на синтетической истории с модулями (31 коммит, 3 файла, файл меняется каждым коммитом) `--write` **5,8 с против 0,46 с** у такой же истории со скриптами. Стало: модуль разбирает **один рабочий поток на прогон** (`src/parse.js` + `src/parse-worker.js`): `vm.SourceTextModule` под `execArgv: ['--experimental-vm-modules', '--no-warnings']`, обмен синхронный (`postMessage` + `Atomics.wait` + `receiveMessageOnPort`), поток поднимается лениво — при первом модуле. Замер по 5 прогонов на каждой стороне: модульная история **5,59 → 0,47 с**, скриптовая **0,39 → 0,39 с** (за поток не платит вовсе), проект с одной изменённой клеткой 0,44 → 0,40 с. Цена не убрана, а перенесена: **старт потока ≈ 54 мс раз на прогон** вместо 86 мс на клетку, и сам обмен стоит ~0,085 мс. Три отступления назад к `node --check` (нет файла потока в упаковке, поток не ответил за 2 с, в Node нет модулей vm) | законность ускорения доказывает `test/guard.test.js`: разбор идёт потоком, а не тихим отступлением; оба пути дают **тот же вердикт** на восьми образцах; отступление без файла потока отвечает тем же вердиктом; 300 разборов дешевле одного запуска. Гард жив **после** ускорения: мутация стриппера (перестал понимать одинарные кавычки) роняет прогон на модуле честным текстом «стриппер испортил src/greet.js на 70168e3: SyntaxError: Invalid or unexpected token», после откатa мутации — 0. Артефакт, страница и числа — те же: 50/50 в двух средах, `parity:live` 95 × 27 без пересъёмки, `pack:check` — 20 исходников и новое утверждение «разбор модуля в пакете идёт потоком» |

| R-5.5 | **Настоящее сжатие** (`PLAN.md` шаг 3, срез 1 — **возможность, не чистка**; записана здесь, потому что правит тот же слой: реестр метрик и границу измерения). `min` перестал быть только «снятием балласта»: способ выбирает настройка `minify.engine` — `strip` (умолчание: под ним сняты оба замороженных эталона, и числа потребителя не могли поехать молча) или `esbuild` (настоящее сжатие JS/TS/CSS **необязательной зависимостью**, загружается лениво и синхронно через `createRequire`, потому что замер — синхронный проход; отсутствие зависимости возвращается ответом, а не исключением). Новый слой `src/minify.js` (92 строки) держит таблицу форматов как единственный источник правды и для замера, и для подписи метрики — подпись не может разойтись с тем, что происходит. Описание метрики для читателя появилось в одном месте (`metricView`): у `min` оно зависит от способа, и оба потребителя (`--data` и примечание артефакта) берут его оттуда. Три решения с ценой: (1) `accuracy` говорит про худшее в колонке — один формат без минификатора делает метрику приближённой целиком, а способ называет, где именно; (2) без минификатора прогон **не падает**: числа те же, что у `strip` (побайтово с эталоном), способ говорит «приближение», а код выхода — **4**, иначе приближение уехало бы в CI как успех; (3) файл, который минификатор не разобрал, — отказ кодом 2 с причиной от него самого и двумя выходами (`minify.ext` или `engine: strip`), а не молчаливое упрощение. Черновик `--init` ведёт новые проекты на сжатие. **Цена названа числами:** на фикстуре сжатие меньше упрощения в 44 клетках и ни разу не больше (`src/code.js` 276 → 185 Б, `src/style.css` 55 → 43, по фикстуре −1 372 Б), на живой истории (95 × 27) прогон **1,48 → 1,71 с** — запуск минификатора и разбор тех файлов, которые он берёт; JSON минифицируется разбором и остаётся точным, HTML, JSX/TSX и прочие форматы — упрощение с названной пометкой | **Минуса нет и не обещается — это возможность.** `src` **+233 raw / +144 кода** (2486/1713 → 2719/1857), комментариев +1 строка. По файлам: `src/minify.js` **0 → 86** (новый, 49 кода), `src/metrics.js` 66 → 187 (51 → 135 кода: второй способ и одно место описания), `src/cli.js` 245 → 262, `src/config.js` 103 → 108, `src/strip.js` 223 → 230 (список точных стратегий), `src/render.js` 128 → 131, а `src/data.js` **90 → 84** (сборка дескриптора метрики стала одной строкой); тесты: `test/minify.test.js` **0 → 207** (новый), `test/module.test.js` 166 → 204 (прогон мутации в копии движка и два способа в отказе); `package.json` — `optionalDependencies.esbuild: 0.28.2` (точная версия: от неё зависит число) и строка в `pnpm-lock.yaml` | ✅ 7 новых проверок; **шесть из семи красные на коде до правки** (клон `5680462` с положенным рядом новым модулем, чтобы отличие было в поведении, а не в отсутствующем файле: «минификатор не сократил ни одной ревизии «code.js»»; способ — прежний; ожидался код 4, получен 0; ожидался код 2, получен 0; в черновике нет `minify`; ожидался код 2, получен 0 — седьмая проверяет сам новый модуль, на старом коде его нет вовсе). Отступление проверяется тем же окружением, которым пользуется установка без необязательных зависимостей (`SIZE_REPORT_NO_OPTIONAL`), и числа при нём **побайтово равны эталону**. Артефакт и страница — те же (22 112 Б и 54 772 Б), `parity:live` 95 × 27 в двух средах без пересъёмки, `check:standards` 4 из 4 + 3 из 3, `lint:strict` — 0, `pack:check` — все исходники в тарболле |

| R-5.6 | **Токены** (`PLAN.md` шаг 4, срез 1 — **возможность, не чистка**; записана здесь, потому что правит тот же слой: реестр метрик, необязательные зависимости и черновик настроек). Третье измерение отчёта: `tok` — вес файла для языковой модели. Устройство необязательных зависимостей стало общим (`src/optional.js`, 31 строка): ленивая **синхронная** загрузка через `createRequire` (замер — синхронный проход), версия берётся у самого пакета (в подписи метрики она часть способа, а не подробность `node_modules`), недоступность возвращается ответом, а не исключением, шов отсутствия — окружение `SIZE_REPORT_NO_OPTIONAL`. Из этого `src/minify.js` стал **короче на 22 строки** (86 → 64): загрузчик был отдельным куском того же устройства. Новый `src/tokens.js` (83 строки) держит семейства и кодировки, счёт словарём и оценку по длине, список форматов без текста; в реестре метрик у `tok` свой слот и своё описание для читателя (`tokView`) — способ называет словарь и кодировку, а `accuracy` объявляет число точным или приближённым. Три решения с ценой: (1) **семейство одно — `openai`**: у остальных нет словаря, который можно было бы назвать их собственным, а считать чужим словарём и называть это семейством значило бы обещать то, чего нет (в `PLAN.md` шаг 4 записано три семейства — решение изменено здесь и отмечено там); (2) **кодировка — часть числа**, а не подробность: один и тот же файл даёт 168 токенов в `o200k_base` и 196 в `cl100k_base`, поэтому она выбирается в настройках и называется в подписи, а отказ настроек перечисляет кодировки семейства; (3) **приближение — свойство колонки целиком**: формат без текста (картинка, шрифт, архив) делает метрику приближённой и называет причину, а отчёт без словаря считает оценкой по длине с названным коэффициентом и отдаёт **код 4** — иначе приближение уехало бы в CI как успех. Числа при отступлении те же, что у прежнего отчёта без токенов. **Цена названа числами:** байт на токен отличается по файлам фикстуры в **2,5 раза** (2,56 у `package.json` … 6,30 у `crlf.txt`) — токены не отношения;в браузере страница с `tok` несёт **41 колонку** вместо 31, а выключение метрики
переключателем возвращает 31 и меняет адрес; на живой истории (95 × 27, 1,23 МБ
текста) тот же отчёт идёт **1,56 → 6,32 с** (три прогона: 1,56/1,56/1,55 и
6,42/6,28/6,26), а в «сейчас» **303 705 токенов** — 4,05 Б на токен; словарь читается
**0,3 с на процесс**, из-за чего `pnpm test` **7,3–7,9 → 10,4 с** при 66 → 73
проверках (бюджет переснят в §3) | **Минуса нет и не обещается — это возможность.** `src` **2719 → 2893 raw (+174)**, `test/` **1914 → 2112 (+198)**. По файлам: `src/metrics.js` 187 → 253, `src/optional.js` 0 → 31, `src/tokens.js` 0 → 83, `src/config.js` 108 → 121, `src/cli.js` 262 → 265, `src/minify.js` 86 → **64** (загрузчик ушёл в общий модуль), `test/tokens.test.js` 0 → 195, `test/module.test.js` 204 → 206, `test/minify.test.js` 207 → 208; `package.json` — `optionalDependencies.gpt-tokenizer: 4.0.0` (точная версия: от неё зависит число) и строка в `pnpm-lock.yaml` | ✅ 7 новых проверок; **до правки они невозможны, и это названо, а не спрятано**: на ревизии до прохода движок отвечает отказом кодом 2 («неизвестная метрика «tok» (есть: raw, min, gzip)»), а файл проверок не загружается — в нём нет `src/tokens.js`. Числа: `code.js` фикстуры — 735 Б `raw`, 276 упрощением, 185 сжатием, **168** токенов (196 в `cl100k_base`); набор **73/73** в двух средах; артефакт 22 112 Б и `parity:live` 95 × 27 со `sha256 1bdb27e14bac` — те же, эталоны не переснимались; `lint:strict` — 0, `pack:check` — исходники и страница из тарболла побайтово, `check:standards` 4 из 4 + 3 из 3. Отступление без словаря проверяется окружением `SIZE_REPORT_NO_OPTIONAL` (код 4, подпись называет оценку и её коэффициент) |

| R-5.7 | **Набор разделён на быстрый и полный прогон** (`PLAN.md` §5, остаток шага 6) — ✅ **сделано 2026-09-14**, и это не чистка, а разделение: ни одна проверка не теряется и не ослабляется, полный прогон запускает весь набор. Было: один прогон на всё, и его время держалось только в спокойные часы — 23,4 с в тишине и до 29,9 с под нагрузкой при цели 28 с; правку проверять этим же прогоном значило платить минуту ожидания за пять секунд нужного. Стало: **`pnpm test` — быстрый** (14 файлов, 56 из 127 проверок: паритет на фикстуре, контракт данных и страница, сторож документации и выпуска, сторож разделения) и **`pnpm test:all` — полный** (все 25 файлов, 127 проверок; CI зовёт его оба раза — обычной средой и без настроек машины). Разделение — по цене, а не по алфавиту: цена файла здесь не объём, а сколько раз он запускает инструмент и git, поэтому в быстром остались те, кому хватает прочитанного (исходники, дерево, справка, клоны общей фикстуры), а в полном — те, что гоняют инструмент по многу раз на своих клонах, коммитят и ставят хуки; причина для каждого дорогого файла названа построчно (`tools/suites.js`), а умолчание — полный прогон, поэтому новое дорогое не может тихо уехать в быстрый файл, которого в объявлении нет вовсе. Стерегут разделение двое, и это их роли: `test/suites.test.js` — объявление (полнота классификации, причина и потолок стоимости у быстрого файла, что быстрый прогон остаётся частью набора), а сам прогон (`tools/run-tests.js`) — измерение (стоимость каждого файла своим запуском рядом с объявленной, сумма чисел проверок против объявления, отказ при выходе за цель вдвое) | **Минуса не обещалось и нет — это новое устройство.** `tools/suites.js` **0 → 101** (53 кода), `tools/run-tests.js` **0 → 220** (159 кода), `test/suites.test.js` **0 → 100** (58 кода); `test/docs-numbers.test.js` 51 → **76** (второй прогон и его цель), `tools/docs-facts.js` 178 → **195** (чтение таблицы прогонов `README.md` как утверждения о числах), `package.json` — три строки скриптов, `.github/workflows/ci.yml` — шаг проверок зовёт `pnpm test:all` вместо `pnpm test`. Набор проверок стал **127**, а не 124: три проверки сторожа разделения. В измерении — ни строки | ✅ **цель по времени названа замером** (по три раза на каждый прогон, загрузка машины на входе в строке): быстрый **5,35 / 5,61 / 4,97 с** при загрузке 19,5–22,6 (**4,80–5,61 с** по всем окнам) — цель ≤ 10 с с запасом; полный **22,47 / 22,54 / 23,16 с** при загрузке 21,7–44,7 и **27,90 / 28,89 / 29,71 с** в занятых окнах — цель ≤ 28 с, то есть **достигнута в спокойных окнах и не достигнута в занятых, и это сказано прямо**: прогон печатает «ЦЕЛЬ НЕ ДОСТИГНУТА», а не зелёную галку при 29,7 с. Красным за занятую машину прогон не становится намеренно (отказ — только при двукратном выходе), иначе CI и правка падали бы от чужой нагрузки. **Красное до правки:** на клоне выпуска новые проверки не загружаются вовсе (`tools/suites.js` нет — `not ok test/suites.test.js`), а с положенным рядом объявлением падает проверка чисел: «README не называет быстрый прогон: `pnpm test` — сколько проверок из скольких и цель». Мутации (клон `19c3dd2` с новым объявлением): дорогой файл отнесён к быстрым как есть → «test/hook.test.js объявлен быстрым со стоимостью 22.13 с при потолке 4.5 с»; быстрый файл без причины → «быстрый файл без причины»; файл полного прогона без причины → «в полном прогоне файл без названной причины»; **стоимость подделана (дорогой файл с ценой 0,5 с) → объявление проходит, и его ловит прогон**: «test/hook.test.js объявлен за 0,50 с, а идёт 15,97 с — его место в полном прогоне», код выхода 1. То есть у маскировки сторож не декларация, а замер, и это не пробел, а разные роли: снимок стоимости нельзя проверить без измерения. 127/127 в трёх средах — **23,90 / 23,38 / 23,63 с** (обычная, без настроек машины, `CI=1`; загрузка на входе 6,7–28,3), в самом занятом окне 26,25 с, `lint:strict` — 0, `pack:check`, `parity:live` (95 × 27, `sha256 1bdb27e14bac`) и `check:standards` (4 из 4 + 3 из 3) — зелёные, **эталоны не переснимались**. ⏤ **цели и гейты по времени сняты 2026-09-15 (R-5.8): ниже о них — история, а не действующее устройство** |

| R-5.8 | **Цели и гейты по времени сняты** — ✅ **сделано 2026-09-15** по прямому требованию владельца: машина бывает под очень разной нагрузкой, и любые числа секунд из-за этого то держатся, то нет. Было: у каждого прогона объявленная цель (`BUDGET`), у быстрого файла — потолок стоимости (`LIMIT`) и снимок стоимости рядом с каждой записью, прогон валился при выходе за цель вдвое и при расхождении замера со снимком, а сторож документации сверял строки бюджета `REFACTOR` и столбец «Цель» `README`. Стало: ни цели, ни потолка, ни снимка — длительность каждого файла и прогона печатается замером (вместе с загрузкой окна, чтобы числа было чем объяснить), а разделение держится признаком файла; из сторожа документации ушли бюджетные строки, из сторожа разделения — потолок и снимок, из `README` и `REFACTOR` — столбец «Цель» и упоминания бюджета. Ни одна проверка не потеряна (134 остаются) и ни одна не ослаблена: классификация по-прежнему полная, у каждого файла есть причина. **Что править в будущем:** если понадобится замечать «дорогой файл уехал в быстрый», вернуть это можно только замером прогона и только как предупреждение — отказ за время противоречит этому решению | **Минус в коде, а не в комментариях:** `tools/suites.js` **109 → 94** raw (`LIMIT`, `BUDGET`, `MEASURED` и поле `cost` у 28 записей), `tools/run-tests.js` **220 → 185** (две проверки-гейта и их сообщения), `test/suites.test.js` **100 → 79** (три проверки сняты, две остались), `test/docs-numbers.test.js` **76 → 53** (разбор бюджета и разбор строк REFACTOR), `tools/docs-facts.js` 209 → 208 (`publishedRuns` больше не читает секунды) — итого **−95 строк**; в `README` и `REFACTOR` текст стал короче, но этот минус не обещался и не считается | ✅ красное было настоящим и не про нагрузку: перед правкой прогон на занятой машине (загрузка 38–64) падал на сторожe документации — «последняя строка бюджета REFACTOR про полный прогон — про 131 проверок, а в наборе их 134», то есть объявление отстало на три проверки. После правки те же 134/134 в двух средах при той же загрузке, `lint:strict` — 0, `pack:check`, `check:standards` и `parity:live` зелёные; числа отчёта, артефакт и оба эталона не тронуты |

Кэш «sha + датчик + версия» (`PLAN.md` §4.4) сюда **не** входит: это возможность,
а не чистка.

Возможная чистка, названная, но не сделанная: прогон держит пул и счётчики сам
(`tools/run-tests.js`, 159 строк кода), хотя часть этого умеет `node --test` на списке
файлов. Свести своё к отчёту поверх чужого пула — отдельный пункт, и его цена —
потеря стоимости файла отдельным числом (её пришлось бы собирать из `duration_ms`
вложенных проверок, а это не время файла).

## 8. Не в этом плане

Следующая возможность отчёта — чтобы её не смешали с чисткой: темы, категории как часть
панели, пре-собранный `dist/app.js` как артефакт публикации, новые команды
(`init/measure/render/check/doctor/explain`) и их семантика, автообновление по
коммиту, шаблон CI для потребителя и `templates/` (свой CI сделан — шаг 6), блок для файлов агентов, остаток шага 4 (семейства токенов кроме `openai`, переключатель семейства на странице), удаление метрики
`gzip` (`PLAN.md` пункт **D4**), кэш замеров — всё это `PLAN.md` §5, шаги 2–6.

Отдельно, как мелкие правки поведения (не чистка, но и не возможность) — идут
сразу после волны 0 или вместе с ней:

- ✅ **Дерево файлов по папкам** (R-2.4), **память выбора** (R-2.5) и **обмен
  выбором ссылкой** (R-2.6) — сделано 2026-09-14;
- ✅ **Соглашение о цвете дельт** (R-2.2): принято по артефакту — рост зелёный,
  спад красный, двумя строками в `src/table.css`. Разбор решения и цена смены —
  в строке R-2.2;
- ✅ **Вырожденное состояние страницы** (R-2.2): снятые все метрики и снятые все
  файлы объясняются словами, сетка без метрик не строится вовсе;
- категории и их источник в контракте (`categoryBy`) — уже есть, но правила
  категорий пока живут в движке; переносить их в настройки — шаг 2.

## 9. Что уже установлено и куда отнесено

| Установлено аудитом | Пункт |
|---|---|
| Пять обязанностей в одном файле (1669 строк) | R-1.3 ✅ |
| Два рендерера одной таблицы | R-1.1 ✅ |
| Два набора стилей одной таблицы | R-2.2 ✅ |
| Программа страницы текстом в шаблонной строке (не линтуется) | R-2.1 ✅ (осталось `dist/app.js` — §8) |
| Дублированная тестовая обвязка с разошедшимися сигнатурами | R-3.1 ✅ |
| Нет линтера; отсюда склейка двух операторов в строку (1062) | R-1.2 ✅ |
| Поля реестра метрик под `gzip`, которого в v1 не должно быть | §8 (`PLAN.md` D4) |
| Случайная правка рендера, сделанная «заодно» при переносе | R-1.1 (снимается) + R-1.2 (стережёт) |
| Соглашение о цвете дельт расходилось у артефакта и страницы | R-2.2 ✅ |
| Вырожденное состояние страницы (`colspan="0"`, пустая сетка) | R-2.2 ✅ |
| Склейка вывода процессов как строки — живой паритет падал не на расхождении, а на разорванном символе (нашёл CI шага 6) | R-3.5 ✅ |
| Проверка эталонов сравнила байты бандла истории, которые пишет git: красная на чужой версии git (нашёл CI шага 6) | R-4.10 ✅ |
| Бандл живой истории без `HEAD`: клон выбирает ветку догадкой, и замечание git хоронило настоящую причину отказа (нашёл CI шага 6) | R-4.10 ✅ |
| Дублирование прогонов в тестах | R-3.2, R-3.3 ✅ |
| Плоский список файлов вместо дерева (требование §8.2 `module-design.md`) | R-2.4 ✅ |
| Выбор читателя терялся при перезаходе (`module-design.md` §8.4) | R-2.5 ✅ |
| Колонка, чей файл удалён до HEAD, роняет прогон отказом с бессмысленным текстом (нашёл проход по токенам) | `BLOCKERS.md` §B3 ✅ |
| Выбор пути для состояния по порядку настроек, а не по тому, что есть в коммите (граница B3, `diff.renames=false` + псевдонимы) | R-0.7 ✅ (`BLOCKERS.md` §N8 закрыт) |
| Выбор нельзя передать коллеге, а без памяти браузера его нет вовсе (`PLAN.md` §4.8, пункт 6) | R-2.6 ✅ |
| Якорь фикстуры на 1145 строк в дереве | R-1.5 ✅ (байты живут в истории, берутся оттуда по требованию) |
| Мутация живого дерева в наборе: проверка гарда ломала корневой `src/strip.js`, а соседние наборы идут параллельно — падали чужие проверки (нашлось проходом о сжатии, когда рядом появился ещё один набор) | R-5.5 ✅ (ломается копия движка) |
| Ложная причина в тексте отказа находилась случайным живым прогоном, четыре раза подряд (B1, B3, разбор аргументов, `explain HEAD`) | R-4.18 ✅ (все отказы под сторожем) |
| Два разных тупика («нет git» и «здесь нет репозитория») названы одним текстом с оговоркой «или» — то есть не названы | R-4.18 ✅ |
| Колонка с путём не-строкой давала отчёт нулями за успех вместо отказа | R-4.18 ✅ |
| `--init` без найденных расширений печатал «✗» и отдавал код 0, уже записав черновик | R-4.18 ✅ |
| Плата за приватность: пин потребителя на ревизию из середины работы и шаг с ключом в его CI | R-4.19 ✅ |
| Совет `npx size-report` ведёт в чужой пакет из реестра (нашлось справкой по именам) | R-4.21 ✅ (совет называет путь внутри проекта) |
| Совет отказа не проверялся на исполнимость: причина сторожилась, а выход из тупика — нет (нашлось прошлым проходом) | R-4.22 ✅ (совет выполняется в том состоянии, которое его напечатало) |
