# REFACTOR.md — чистка size-report по слоям

Вход — аудит 2026-09-14 (SPEC 8, DESIGN 4, CORRECTNESS 7, QUALITY 3). Здесь **только
чистка**: объём кода и скорость. Что делаем по шагам выпуска — `PLAN.md` §5,
известные дефекты и обходы — `BLOCKERS.md`, номера пунктов ниже ссылаются на них и
не пересказывают.

## 0. Границы

- **Поведение и форма вывода не меняются.** Ни числа, ни порядок строк, ни клетки
  артефакта, ни состав полей контракта.
- **Побайтовый паритет обязателен после каждого пункта** — иначе пункт не закрыт.
- **Никаких новых возможностей внутри рефакторинга.** Захотелось новое — в
  `PLAN.md`, не сюда (список — §7).
- **Один пункт — один коммит**, откат = снять коммит. Пункт, требующий смены
  поведения, выносится из плана, а не «заодно поправляется».
- Комментарий описывает код, а не правку (как в проекте-потребителе).

### Чем доказывается, что ничего не сломалось (три прогона, одни для всего плана)

| Прогон | Ожидание |
|---|---|
| `pnpm test` | все зелёные, число проверок **не может уменьшиться** — а само число живёт ровно в двух местах, и оба сверяются сторожем: `README.md` («при N проверках») и последняя строка таблицы целей в §3. Второй копии здесь нет намеренно: сказанное один раз стареет вместе с таблицей, сказанное дважды стареет молча |
| сборка артефакта на фикстуре | sha256 `5c0c73e4f2e9…` побайтово, 22 112 Б (не тронута проходом о сжатии: способ `strip` — умолчание) |
| `pnpm run parity:live` | 95 строк × 27 колонок, артефакт 225 673 Б, sha256 `1bdb27e14bac…`, две среды |
| `pnpm run lint:strict` | ноль замечаний на всём настоящем коде пакета (появился в R-1.2) |
| `pnpm run pack:check` | собранный тарболл работает: исходники доехали, числа, артефакт и страница — как из репозитория (появился в R-1.3) |
| `pnpm run check:standards` | оба эталона воспроизводятся: наши файлы — побайтово, бандл истории — по содержимому (ветки, верхушка, число коммитов), плюс `HEAD` у бандла живой истории; пересъём идёт во временный каталог и сверяется с закоммиченным (появился в R-4.9, переписан в R-4.10) |
| `pnpm run parity:live` и файл CI (`.github/workflows/ci.yml`) | то же самое видят сами проверки: один job запускает весь список выше теми же командами на каждый пуш и запрос правки (шаг 6 плана) |
| прогон инструкции подключения (вручную, раз за проход документации) | в свежем репозитории все шаги раздела «Как подключить» проходят ровно теми командами, что написаны: установка, `--init`, `--write`, `--page`, `test:sizes`; каждое расхождение — пункт в волне 4 |

Что именно сократилось — записывается числом в `WORKLOG.md` (`wc -l` до/после), а
не ощущением.

## 1. Порядок и почему такой

| Волна | Слой | Почему здесь |
|---|---|---|
| **0** | Тупики CLI (`fix:`, не рефакторинг) — ✅ **сделано 2026-09-14** | 30 минут, закрывает единственный найденный дефект сразу; тексты подсказок правятся один раз, до того как их тронет пункт 1.4 |
| **1** | Движок и CLI (1.1 ✅, 1.2 ✅, 1.3 ✅) | Самый большой объём и все три дублирования; волна 2 и разбиение по файлам зависят от единого слоя, поэтому объединение идёт первым |
| **2** | Страница | Зависит от 1.1: две трети дублирования страницы исчезают вместе с общим слоем, отдельно их чистить значит делать работу дважды |
| **3** | Тесты (3.1 ✅, 3.2 ✅, 3.3 ✅) | Дешёвая страховка для волн 1–2 вводится по ходу, а общий харнесс — после того как файлы движка перестанут переезжать |
| **4** | Документация | Идёт следом за фактом; правило «правка кода тянет строку в доке» действует с пункта 1.1, а не с начала волны |
| **5** | Производительность (движок) | По прямому требованию: сначала объём, потом скорость. Скорость **тестов** закрыта волной 3 (5.2, 5.3) — замеры там же; волне 5 остаётся движок |

## 2. Волна 0 — тупики (правка поведения, вне рефакторинга) — ✅ сделано 2026-09-14

| # | Пункт | Приёмка |
|---|---|---|
| R-0.1 | Подсказка починки ведёт к работающей команде (было: `--init` советовал команду, которая завершается успехом и ничего не создаёт; `BLOCKERS.md` §N2) | Прогон: `node bin/size.js` без конфига → текст называет команду, её выполнение создаёт конфиг, и по нему сразу работает `--write` |
| R-0.2 | `--page` создаёт недостающий каталог (было: `ENOENT` стеком на `--page .size-report/…`) | `node bin/size.js --page .size-report/report.html` в свежем клоне → файл, код 0 |
| R-0.3 | У каждой ветки отказа — справка и код выхода вместо стека (было: `--help` отсутствовал вовсе, отказ печатал стек) | Тест на каждый путь; сверка с таблицей кодов `PLAN.md` §4.1 |
| R-0.4 | _(найдено по ходу волны)_ Черновик настроек не делает колонкой сам файл таблицы: иначе команда из подсказки приводит в новый тупик — её же проверка настроек отвергает черновик | Тест: `--init` на проекте, где таблица уже в истории, затем `--json` по черновику → код 0 |
| R-0.5 | _(закрыто здесь же, `PLAN.md` D5)_ Устаревшие тексты: шапка движка и советы `--init` называют реальные команды вместо `tools/size-table.js` и `tests/size-table.js` в чужом проекте | `grep` по движку не находит ни одного упоминания путей, которых в пакете нет |
| R-0.7 | _(продолжение R-0.6, `BLOCKERS.md` §N8)_ Выбор пути для состояния шёл по порядку настроек, а не по тому, что в коммите есть: при выключенном распознавании переименований git отдаёт в одном коммите оба пути переименованного файла, движок брал исчезнувший — и сверка отказывала на законном случае. Теперь план собирает все псевдонимы колонки, которых коммит коснулся, а берётся первый, для которого git отдал блоб | Прежние числа не поехали: вывод движка до и после правки побайтово совпал на фикстуре при `diff.renames` в обоих значениях, оба замороженных эталона воспроизводятся байт в байт, живой отчёт — те же 95 × 27, прогон на живой истории 1,56–1,58 с. Проверка «переименование внутри псевдонимов колонки не роняет прогон» красная до правки (клон `237bbdd`: 5 из 6 зелёные); потерянные правка, создание и удаление — по-прежнему три свидетеля мутации |
| R-0.6 | _(пришёл из прохода по токенам, `BLOCKERS.md` §B3)_ Колонка, чей файл жил в истории и удалён до HEAD, роняла прогон **кодом 1** с текстом «файла нет вместо файла нет» — то есть на истории с удалениями отчёта не было вовсе. Отказом стало расхождение сторон сверки (потерянное создание, изменение, удаление), а текст называет и сторону дерева, и сторону состояния | Две проверки в `test/disk.test.js`: история с добавлением, правкой, удалением, возвратом и повторным удалением собирается и её числа сходятся с размером блоба из git (на старом коде красная текстом B3); файл, появившийся только в слиянии, при мутации чтения истории роняет прогон с обеими сторонами в тексте |

Проверки на эти пути добавлены здесь же: до волны у `--init`/`--force` и у
выводов об отказе покрытие было ровно нулевое. Все 10 новых проверок красные на
движке до правки.

## 3. Волна 1 — движок и CLI

| # | Пункт | Что сокращается | Приёмка |
|---|---|---|---|
| R-1.1 | **Один слой отчёта на артефакт и страницу** — ✅ **сделано 2026-09-14**, но минус **не достигнут: файл +25 строк** (1757 → 1782), из них кода −9 (1325 → 1316), комментариев +25 (308 → 333). Дублирование расчёта убрано (было две независимые вычислительные части: 12 функций, ≈120 строк кода), но единая версия несёт оба случая и документацию — она почти так же длинна, как одна из копий. Обещанные −150…−250 в этом файле даёт не объединение, а **R-2.1** (программа страницы уезжает из строковой константы: −200 из файла) и **R-2.2** (−45 стилей) — оценка в этой строке была неверной. Что получено вместо минуса: одного расчёта больше нет (страница исполняет код движка), артефакт и разметка страницы совпали побайтово со старым выводом, а второму расчёту поставлена проверка (12 → 13 проверок контракта) | — | минус по файлу — в R-2.1 и R-2.2 |
| R-1.2 | **Линтер и формат** — ✅ **сделано 2026-09-14**. `eslint.config.js` (плоский, ESLint `^9.18.0` — та же версия, что у потребителя): правила взяты у него же — `indent` (error, 2 пробела), `no-unused-vars`, `no-undef`, `block-scoped-var`, `eqeqeq` — плюс одно своё, `no-multi-spaces` (error, `ignoreEOLComments`): **именно оно и ловит ту самую склейку**, от которой пункт начался. Что осознанно не включено, чтобы не выдать шум за сигнал: `max-statements-per-line` — 39 замечаний на чистом дереве и саму склейку не видит (оператор на той строке был один); `brace-style` с `allowSingleLine: false` — 78 (в коде принят однострочник `if (...) return;`); причины записаны в шапке конфига. Скрипты `lint` (совет, как у потребителя) и `lint:strict` (падает). `fixtures/` — в ignored: там данные (бандлы истории) и замороженная копия старой реализации. Заодно линтер нашёл настоящий мусор: в `test/cli.test.js` лежали неиспользуемые `os` и `PACKAGE_BIN` — убраны. Согласование с потребителем — **в правилах, не в общем конфиге**: модуль — ESM, а конфиг потребителя объявлен `sourceType: "script"`, и на исходниках модуля он падает «'import' and 'export' may appear only with 'sourceType: module'» (проверено). Потребитель не тронут; чтобы линтовать исходники модуля у себя, ему нужна одна строка с `sourceType: 'module'` для этих путей. Остаток: линт сознательно не входит в `pnpm test` (набор — про поведение), а своё место он получил в CI шага 6 — отдельным шагом, и там же стоит `lint:strict` | 0 строк (правка качества) | `pnpm run lint:strict` — 12 файлов, 0 замечаний; мутация: историческая строка `} else {      body = …` возвращена в `commitCell` — прогон даёт **ровно одно замечание, на этой самой строке** (1106), остальное чисто; `pnpm test` 38/38 в двух средах, артефакт и живой паритет без пересъёмки |
| R-1.4 | **Одна граница вызова git на движок и проверки** (нашлось сторожем документации, записано `BLOCKERS.md` §N10 — тот же класс, что B1, только в обвязке) — ✅ **сделано 2026-09-14**. Было: движок закрепляет настройки на своей границе (`src/git.js`), а проверки и инструменты звали git напрямую — с настройками машины. На машине, где `core.quotePath` по умолчанию включён (`GIT_CONFIG_GLOBAL=/dev/null`), `git log --name-only` отдаёт не-английские пути закавыченными, и `docs/заметки.md` из `BLOCKERS.md` становился «несуществующим»: сторож документации был зелёным на этой машине и красным на другой. Стало: список закреплений и локаль берутся из `src/git.js` (`gitArgv`/`gitEnv`) — один список на пакет, а не второй рядом; проверки и инструменты ходят через `gitIn`/`gitTry` обвязки; незакреплённое чтение осталось ровно там, где оно и есть предмет — у проверки, измеряющей унаследованные настройки, и оно названо (`gitBare`, список разрешённых мест ведёт человек). Свидетели: `test/git-pins.test.js` — исходники (прямой вызов без общего списка назван файлом и строкой) и поведение (то же чтение без закрепления возвращает путь кавычками, с ним — как есть) | −9 прямых вызовов git в проверках и инструментах (13 строк), +2 проверки (`test/git-pins.test.js`, 88 строк), +13 строк в `tools/harness.js` (`gitIn` с закреплениями, `gitTry`, `gitBare`), 0 строк в движке — он уже был закреплён | ✅ красное до правки: тот же сторож на клоне `d8ba03f` называет **одиннадцать** мест (`tools/check-standards.js:110,119,121`, `tools/make-fixture.js:60,292`, `tools/pack-check.js:52`, `tools/parity-freeze.js:50,55`, `tools/parity-live.js:162,163`, `test/cli-paths.test.js:28` — и дальше по списку) — рабочий список ведёт проверка, а не комментарий. Мутации: возврат незакреплённого вызова в `test/cli-paths.test.js` роняет первую проверку с этим же адресом; снятие `core.quotePath=false` из списка — вторую, но только в среде без настроек машины (и в этом весь смысл двух сред: на машине автора закрепление невидимо). Эталоны не пересняты и **не поехали**: `check:standards` — 4 из 4 + 3 из 3 побайтово (закрепление коснулось и инструментов, снимающих эталон), `pack:check` — артефакт 22 112 Б и страница 59 358 Б из тарболла, `parity:live` — 95 × 27 в двух средах; 99/99 в двух средах, `lint:strict` — 0 |
| R-1.3 | **Разбиение движка по файлам** — ✅ **сделано 2026-09-14** (он же `PLAN.md` пункт **D1**). Шов один и он по границам данных: сверху то, что читает git и файловую систему, ниже то, что работает на уже собранных значениях, а настройки, тексты и отказ — по краям, потому что их знает любой и они не знают никого. Стало 15 модулей (плюс `derived.js` и `page/app.js`, которые уже были): `cli` 245, `strip` 207, `git` 194, `render` 168, `history` 161, `config` 103, `data` 90, `size-table` (точка входа, только реэкспорт) 83, `page/build` 82, `locales` 73, `journal` 66, `metrics` 62, `refusal` 56, `tool` 13. По ходу нашлась настоящая связь, которую иначе не увидеть: контракт данных звал `build` из модуля режимов — сборка переехала в `history.js`, и кольцо `data ↔ cli` не появилось | файл 1525 → максимум **245**; src в сумме **+78 строк raw / +41 кода** (1812/1327 → 1890/1368) — это заголовки модулей и импорты, ни одна строка кода не переписана | `pnpm test` **39/39** (38 + страховка API) в двух средах; `pnpm run lint:strict` — 0; артефакт и **страница байт в байт** с выводом движка до разбиения, паритет живой истории в двух средах без пересъёмки; **работа из собранного пакета** — теперь командой `pnpm run pack:check` (`npm pack` → распаковка в чужой каталог → клон фикстуры): все 16 исходников и `page/` доехали, `--json`, артефакт и страница — побайтово как из репозитория; сама проверка доказана мутацией (убрать `src` из `files` — падает и объясняет, что смотреть); публичный API — 58 имён, ни потеряно, ни добавлено (`test/api.test.js`), мутация «убрать одно имя из реэкспорта» роняет ровно эту проверку |
| R-1.4 | **Устаревшие тексты и тонкий legacy-путь** — существует частично, `PLAN.md` пункт **D5**: шапка движка предлагает `node tools/size-table.js`, `initMode` печатает советы про `tests/size-table.js` в проекте-потребителе, `check`/`checkMode` сверяют артефакт с историей — смысл, который держится только пока отчёт лежит в git | −40…−70 строк (текст и печать) | `pnpm test`; вывод `--init` проверяется тестом по строкам |
| R-1.6 | **Один владелец грамматики командной строки** (аудит: «два режима сразу» осталось от R-4.13 тем же классом — команда отвечает нулём, ничего не сделав) — ✅ **сделано 2026-09-14**. Было: три помощника (`strayArg` — строка отказа, `noArgs` — сразу печать, `plainWords` — слова без разбора) и выбор режима **порядком `if`-ов** в `main`; правило «так нельзя» жило в прозе автора, а не в значении, и потому `--write --data` отвечал нулём, записав таблицу и не отдав данные (`--json --write`, `--data --page`, `--page --write` — так же). Стало: один `parseArgs` + `checkArgs` до чтения проекта — он решает всё сразу (какой режим, совместим ли он с командой и с остальными ключами, все ли ключи получили значение, нет ли лишних слов), отвечает **одним способом** — `refuse` из трёх мест, а не печатью и `return` из четырёх, — и отдаёт готовый план, который `main` только исполняет. Правила, которых не было: два режима сразу (перебор всех шести сочетаний), `--json` рядом с режимом и у команды без JSON-ответа, `--config` рядом с `--init`, ключ, названный дважды. **Что выяснилось про прежний порядок:** законных зовов он не касался — ни один работающий вызов не зависел от порядка ветвлений; но в ошибочных выбор режима делал не человек, а место в списке `MODE_FLAGS`: `--data --page` молча печатал данные, `--page --write` молча записывал таблицу. Теперь такой зов отвергается, называя оба режима | **минуса нет — +32 raw / +26 кода в `src/cli.js`** (418 → 450; 320 → 346): разбор стал длиннее, потому что впервые читает значения и замечает повтор, а шесть правил — новые; но расчёта на это и не было — обещанное аудитом сокращение (−55…−65) относилось к тестовым помощникам, а проверки наоборот выросли, потому что они и есть доказательство. Ещё +3 строки в `src/refusal.js` (правило названо в справке), `test/cli.test.js` 160 → 216 | ✅ новые проверки красные до правки: тот же набор на клоне `c526399` — **0 из 1** нужной («два режима: --init --write: ожидался код 2, получен 0»). Края проверены перебором, а не примером: все шесть сочетаний режимов, `--json` рядом с `--write`, команда с режимом, `--json` у `install-hook`, `--config` с `--init`, дважды названный ключ (`--write --write`, `--config a --config b`), одинокие `-` и `--`, лишнее слово до и после команды — каждое своим кодом и текстом, и все с готовой командой в отказе. Заодно перепроверены законные зовы (`--init draft.json --force`, `--page out.html --config …`, `explain <sha> --config …`, `--config x check`, `doctor --json`, `--json`/`--data`/`--write` с `--config`) — остались зелёными, а зовов, державшихся на порядке ветвлений, не нашлось ни одного. **120/120 в трёх средах** (23,32 с; 23,18 без настроек машины; 22,79 с `CI=1`), `lint:strict` — 0, `pack:check` — артефакт 22 112 Б и страница 59 358 Б из тарболла побайтово, `parity:live` — 95 × 27 в двух средах с тем же sha256 `1bdb27e1…`, `check:standards` — 4/4 + 3/3, **ни один эталон не пересматривался** (числа и формат отчёта разбор аргументов не трогает) |
| R-1.7 | **Правило `--json` — одно и названное** (аудит: «у `--json` два смысла, и это видно только из чтения») — ✅ **сделано 2026-09-14**. Решение: `--json` — **форма ответа**, а не режим, и ответ бывает ровно у четырёх вызовов: без команды и режима это прежняя форма данных (заморожена эталоном паритета — убрать её нельзя), у `check`, `explain` и `doctor` — их ответ. Поэтому у команды без ответа и рядом с режимом он отказ, и причина у каждого случая своя (`нет ответа в JSON`, `два ответа сразу`), а не полумера «молча проигнорировать». В коде это **одно значение** — `ANSWER_COMMANDS`, — и на него смотрят оба отказа; в справке правило названо словами, в README — в разделе для агента. Поведение команды от правки не изменилось (оба отказа были и до неё): изменилось то, что правило стало одним и названным. Новые проверки — перебором: все четыре вызова с ответом (их stdout разбирается как JSON) и все четыре режима с тремя командами без ответа, каждый своим кодом и текстом, плюс «отвергнутый зов не записал страницу». **Честно про «красное до правки»: эта проверка на прежней ревизии зелёная** — здесь менялось не поведение, а формулировка, так что красным до правки было отсутствие самой проверки, а не её падение | `src/cli.js` +8 raw, в другом месте — 0; `test/cli.test.js` +43 | ✅ 122/122 в двух средах (23,16 / 24,04 с; 23,23 без настроек машины), `lint:strict` — 0, артефакт и страница из тарболла те же (22 112 Б / 59 358 Б), `parity:live` 95 × 27 с тем же sha256, `check:standards` 4 из 4 + 3 из 3, эталоны не пересматривались |
| R-1.5 | **Якорь фикстуры** (`fixtures/legacy/size-table.cjs`, 1145 строк) — на нём стоят 5 из 27 проверок (трио «движок пакета / замороженная копия» и две про ревизию эталона). Пока нужен: им различается «сломался движок» и «съехал эталон». Демотируется до одной проверки, удаляется из дерева на 1.0.0 (байты остаются в истории на `c939def`) | −1145 строк на 1.0.0 | `pnpm test` 27/27 без него: проверки, ссылающиеся на копию, обязаны быть явно вычеркнуты, а не пропущены молча |

## 4. Волна 2 — страница

| # | Пункт | Что сокращается | Приёмка |
|---|---|---|---|
| R-2.1 | **Программа страницы уезжает из строковой константы в модуль** (решение уже принято, `PLAN.md` §4.1: собирается на публикации пакета) — ✅ **сделано 2026-09-14**. Было: 184 строки JS текстом внутри движка (`APP_DOM`) плюс текст общего расчёта, снятый с функций через `toString()`. Стало: `src/page/app.js` (174 строки, 139 кода) и `src/derived.js` (113, 69 кода); движок вклеивает их исходники, построчно сняв модульный синтаксис (`stripModules` → `pageSource` → `pageScript`). Минус обещан «−(180…200) из движка, столько же в модуль» — по файлу даже больше: `src/size-table.js` 1782 → **1525** (raw −257, кода −197: 1316 → 1119). Но это **переезд, а не сокращение пакета**: в модулях 287 строк raw / 208 кода, то есть 1782 → 1812 в сумме (**+30 raw, +11 кода** — шапки файлов и `import`). Взамен объёма получено другое: программа страницы стала обычным кодом — её видит линтер (R-1.2), граница движка и страницы стала файловой. Остаток пункта — пре-собранный `dist/app.js` как артефакт публикации: он в §8, здесь не делается | −257 raw / −197 код из движка; в модулях +287/+208; пакет в сумме +30 raw | `pnpm test` **38/38** в обеих средах; артефакт байт в байт (225 673 Б, sha256 `1bdb27e1…`), `parity:live` зелёный; страница на фикстуре — тот же DOM, что у движка до пункта: 15 строк, 330 клеток, разметка панели, таблицы, примечания и заголовка совпала побайтово и после снятия метрики и файла; на живой истории 96 строк / 5376 клеток / 32 галочки, страница 104 355 Б, три `<script`, внешних ссылок и `type="module"` нет, ошибок в DOM нет |
| R-2.2 | **Один набор стилей + внешний вид страницы** — ✅ **сделано 2026-09-14**. Стилей больше не два: `src/table.css` (35 строк) — **общая часть таблицы**, тот же текст, что и раньше внутри движка, поэтому она одна на артефакт и страницу и получает липкую шапку и липкую колонку коммита, которые были только у артефакта; `src/artifact.css` (8) — оформление артефакта сверх неё; `src/page/app.css` (122) — страница сверх неё; читает их `src/css.js` (34). Соглашение о цвете дельт — **одно и принято по артефакту**: рост зелёный `#1e8449`, спад красный `#c0392b`; задано двумя строками в `table.css`, а больше ни одного цвета в оформлении пакета нет (стережёт проверка). Страница отдала своё прежнее соглашение (рост коричневый `#b04a00`, спад зелёный `#1a7f37`) — артефакт заморожен байтами и его цвета менять нельзя, не пересняв эталон, а два отчёта одной истории не могут показывать рост разными цветами. Смена соглашения — две строки в `table.css` + пересъёмка эталона артефакта, отдельного места у цвета нет. Вид страницы переделан: типографика и ритм, панель-карточка с легендой, таблица в своей рамке со своим скроллом, липкие шапка и колонка коммита, адаптация под узкое окно (было 300px на колонку — стало 190), состояния пустоты словами (снятые все метрики или все файлы), переключатели-метки с фокусом, который переживает пересборку панели | стилей в коде было **67 строк, в файлах стало 199** — **минуса нет**: 43 вернулись общими и артефактными файлами, 99 — новый вид страницы, 34 — чтение с диска. Обещанные −40…−60 относились к слиянию двух наборов, а не к переделке вида | `pnpm test` **42/42** в двух средах; артефакт **байт в байт** (22 112 Б, sha256 `5c0c73e4…` на фикстуре; 225 673 Б на живой истории), `parity:live` в двух средах без пересъёмки; `lint:strict` — 0; `pack:check` — страница из тарболла побайтово как из репозитория. **Страница в настоящем Chrome**: внешних ресурсов ноль (ни `link`, ни `img`, ни загрузок), ошибок консоли нет; на 1500px страница не переполняется, на 620px — тоже, при том что до правки страница уезжала вбок на 1518px при окне 620; после прокрутки на 400px шапка остаётся на месте (липкая) и колонка коммита стоит у левого края (тоже липкая); снятие всех метрик даёт подпись вместо сетки, снятие всех файлов — общий объём в 2 колонки и подпись; `Tab` + `Space` переключают метрику и пересчитывают таблицу (22 колонки → 11), фокус остаётся на том же переключателе; мутациями (5) доказано, что обе новые проверки и проверка клавиатуры стерегут именно своё: второй цвет дельт в `app.css`, повтор общего правила в `app.css`, молчание про пустоту, молчание про выключенные файлы, потеря фокуса — каждая роняет ровно свою проверку |
| R-2.4 | **Дерево файлов в панели** (возможность шага 2 плана, не чистка — записано здесь, потому что правит тот же файл страницы) — ✅ **сделано 2026-09-14**. Панель вместо плоского списка по категориям показывает дерево из тех же путей, что в подписи файла: у папки три состояния (`checked`/`indeterminate`/снята) и переключатель на всё поддерево, рядом число файлов; быстрые кнопки категорий остались строкой над деревом. Состояние по-прежнему хранят только файлы (`appView.files`), у папки и категории своего нет — поэтому расходиться нечему. Список файлов длиной больше `min(30vh, 320px)` прокручивается внутри панели: без этого 25 файлов живой истории выталкивали таблицу за экран | 0 строк экономии (возможность): `src/page/app.js` 230 → 288, `src/page/app.css` 132 → 143, `src/page/build.js` 89 → 91, 4 строки словаря в `src/locales.js` | `pnpm test` **54/54** в двух средах (новая проверка красная на коде до правки: папок в разметке нет, закрытый список функций оболочки не знает новых имён); артефакт **байт в байт** (22 112 Б на фикстуре, sha256 `5c0c73e4…`; 225 673 Б на живой истории), `parity:live` 95 × 27 в двух средах без пересъёмки; `lint:strict` — 0; `pack:check` — страница из тарболла побайтово как из репозитория (40 603 Б). В настоящем Chrome на живой истории: 9 папок до трёх уровней, 25 файлов, панель 429 px при окне 1080, сеть — только сам файл, консоль пуста; `docs/6` → третье состояние после выключения одного файла, 52 колонки → 40 после выключения папки целиком |
| R-2.5 | **Память выбора читателя** (возможность шага 2 плана, правит тот же файл страницы) — ✅ **сделано 2026-09-14**. Правило, по которому выбор применяется: запись привязана к **паспорту отчёта** (имя инструмента, схема данных, путь артефакта, заголовок и метки колонок в порядке отчёта; отпечаток — ключ записи), а внутри записи выбор лежит **по именам** — файл путём (путём на HEAD, а если файла там уже нет — последним из настроек), метрика ключом — и хранится только выключенное. Отсюда всё остальное: выбор чужого отчёта не подхватывается (в Chrome все страницы `file://` делят одну память — проверено, два отчёта держат два ключа); имя, которого в отчёте нет (колонку перенаправили на другой путь, метрику убрали из настроек), ничего не значит, а появившееся остаётся включённым; запись не своей версии, чужого паспорта или испорченная равносильна её отсутствию; «включил всё обратно» убирает запись, а не оставляет след. **Верхушки истории и версии пакета в паспорте нет намеренно:** подросшая история и обновлённый инструмент — тот же отчёт, и выбор должен пережить следующую сборку; цена решения — в паспорт при желании добавляется одно поле (верхушка требует поля в контракте — его сегодня нет). Восстановление идёт **до первой отрисовки**, поэтому первому читателю разметка по умолчанию достаётся неизменной — состояние читателя на числа и умолчание не влияет | 0 строк экономии (возможность): `src/page/app.js` 288 → 381, `test/contract.test.js` 527 → 651 |
| R-2.6 | **Обмен выбором ссылкой** (`PLAN.md` §4.8, пункт 6 — там же `location.hash` как запасной путь) — ✅ **сделано 2026-09-14**. Адрес и есть ссылка: в якорь ложится **та же запись**, что в память (`#size-report=…`, тот же JSON, только процентно закодированный — в адресе нет места сырым кавычкам и не-ASCII путям), поэтому отправитель копирует адрес, а не пользуется отдельной кнопкой. Порядок решает тот же код, что и для памяти: своя ссылка применяется (и заменяет вид целиком — в ней весь выбор, а не разница), чужая и нечитаемая не применяются и объясняются строкой над таблицей, имя, которого в отчёте нет, пропускается со счётчиком. Три обстоятельства, которые видны только в браузере и потому попали в решение: (1) присланный адрес не переписывается до первого действия читателя (иначе открыл ссылку — и потерял её); (2) присланный выбор в память читателя не идёт — память меняется только от его действия; (3) смена якоря не перезагружает документ, поэтому выбор из нового адреса применяется по `hashchange` — без этого ссылка срабатывала бы только в новой вкладке (нашлось в браузерной проверке, а не в тестах). Цена решения: адрес длинный (но он несёт только выключенное) и при каждом изменении выборки он обновляется через `replaceState` — истории браузера это не засоряет | 0 строк экономии (возможность): `src/page/app.js` 381 → 515, `src/page/app.css` 143 → 156, `src/locales.js` 99 → 111, `src/page/build.js` 91 → 95, `test/contract.test.js` 651 → 800; новых строк в контракте, числах и артефакте — ни одной | `pnpm test` **59/59** в двух средах; три новые проверки красные на коде до правки (клон `658f0ad`: «выбор не попал в адрес» — `-1 !== 0`; `#notice` в разметке нет), плюс расширенное утверждение о самодостаточности — оно тоже было красным на старом коде, но из-за слова `import(` в комментарии: проверка буквальная, поэтому комментарий переписан словами (записано в `WORKLOG.md` §25.5); `lint:strict` — 0; артефакт **байт в байт** (22 112 Б фикстура, 225 673 Б живая история, sha256 `1bdb27e1…`), `parity:live` 95 × 27 в двух средах без пересъёмки; `pack:check` — страница из тарболла побайтово (54 772 Б). В настоящем Chrome с диска: получатель без памяти по ссылке видит те же 25 колонок и итог 994 335; чужой адрес оставляет 52 колонки и 1 133 362 и объясняется; на открытой странице ссылка меняет вид 52 → 25 колонок; консоль пуста, сетевых запросов кроме самого файла нет | `pnpm test` **56/56** в двух средах; две новые проверки красные на коде до правки (клон `8e551b9`: `0 !== 1` — записи нет; `"undefined" is not valid JSON` — читать нечего), список функций оболочки расширен осознанно на 6 имён; артефакт **байт в байт** (22 112 Б фикстура, 225 673 Б живая история, sha256 `1bdb27e1…`), `parity:live` 95 × 27 в двух средах без пересъёмки; `lint:strict` — 0; `pack:check` — страница из тарболла побайтово (45 749 Б). В настоящем Chrome с диска: перезаход возвращает тот же выбор (25 колонок вместо 52, итог 994 335 вместо 1 133 362), чужой отчёт открывается целым и своей записи не теряет |
| R-2.7 | **Точность числа — по клетке, а не только по колонке** (`PLAN.md` шаг 4, срез 2 — **возможность, а не чистка**: закрывает остаток шага 3). Было: `accuracy` объявлялась метрике целиком, и один формат без минификатора делал приближённой всю колонку, хотя рядом лежали честно посчитанные клетки. Стало: правило одно на два ответа — `pointExact` (`src/metrics.js`) говорит, точное ли число у конкретного файла, и от него же берутся и подпись метрики, и пометки клеток. Подпись метрики — **худшее в колонке** (и теперь считается по клеткам, а не по названию способа: отчёт из одних JSON под снятием балласта объявлен точным, потому что таким и является), а сама клетка приходит в контракт знаком, а не выводится страницей из путей: форма — `approx: { min: { rows: "0/1 по клеткам строк", now: "0/1 по строке сейчас" } }`, метрика без ни одной пометки в отчёт не попадает вовсе. Из этого — ноль ручных правил на странице (список форматов живёт только в движке) и полное совпадение двух ответов; пунктир и подпись способа у клетки — `td.approx` в `src/page/app.css`, а способ каждой метрики теперь виден текстом под переключателями, а не только во всплывающей строке. **Решение второго остатка шага 4:** переключателя семейства словаря на странице не будет — страница получает готовые числа и другого словаря у неё нет, а предвычислять все семейства значит платить временем за числа, о которых могут и не спросить (`PLAN.md` §4.8.4 отменён там же). Выбор живёт в настройках запуска, а страница его называет — это и есть то, что из решения следует для текстов отчёта | **Минуса нет и не обещается — это возможность.** `src` в сумме **3110 → 3220 raw** (+110; из них кода +57, комментариев +53; счёт: непустые строки, кроме комментариев, все `src/**/*.js` и `src/**/*.css`). По файлам: `src/data.js` 84 → 104 (ряд пометок и его правило в шапке), `src/history.js` 179 → 187 (пометка ставится рядом с замером; в кэш содержимого не идёт — она свойство пути, а не блоба), `src/metrics.js` 253 → 268 (правило и его описание), `src/locales.js` 111 → 124, `src/page/app.js` 515 → 560, `src/page/build.js` 95 → 102, `src/page/app.css` 156 → 165, а `src/tokens.js` **83 → 76** (`binaryFormats` стал `isBinary`: список форматов больше не собирается на стороне метрики). Тесты: `test/contract.test.js` 800 → 906, `test/minify.test.js` 208 → 239 | ✅ **80/80** в двух средах (11,16 / 10,75 / 11,07 с; 11,30 без настроек машины), `lint:strict` — 0, `pack:check` — артефакт 22 112 Б и страница 59 358 Б из тарболла побайтово, `parity:live` 95 × 27 и 225 673 Б с тем же sha256 в двух средах, `check:standards` 4 из 4 + 3 из 3 — **ни один эталон не пересматривался**: `--json` и артефакт не тронуты (в артефакте точность метрики и не печаталась). Три новые проверки красные до правки (клон `e463b70` с новыми наборами: `Cannot read properties of undefined (reading 'raw')` — в контракте нет пометок; то же в `minify.test.js` под настоящим сжатием; в DOM «приближённая клетка не помечена»). Смена правила видна ровно там, где менялось поведение: в проверке подписи красным стало новое утверждение про отчёт из одного JSON под снятием балласта (на старом коде `'approximate'` там, где по клеткам `'exact'`). Фикстура проверена на **смешанном** отчёте, а не на однородном: `package.json` (JSON) точен, `.js` и `.md` — нет; под настоящим сжатием — наоборот, `.js` точен, а `.md/.toml/.txt` — нет. В настоящем Chrome с диска: на живой истории помечено 26 клеток из 27 у `min` (единственная точная — `manifest.json`), у `raw` — ни одной, у итога знак «худшее из включённых»; с оставленным одним точным файлом пометок в таблице не остаётся вовсе (0), у клетки — подпись способа, в панели — способ обеих метрик текстом, в легенде — что значит пунктир; сетевых запросов нет |
| R-2.3 | **Одна функция разрядов** — ✅ **сделано 2026-09-14 вместе с R-1.1**: `appGroup` с двойным экранированием больше нет, разряды ставит один `group` из общей вычислительной части | −5…−10 строк | `pnpm test`; артефакт байт в байт, страница в jsdom — те же числа |

## 5. Волна 3 — тесты

| # | Пункт | Что сокращается | Приёмка |
|---|---|---|---|
| R-3.1 | **Общий харнесс** — ✅ **сделано 2026-09-14**. `tools/harness.js` — единственный владелец обвязки: пути, клоны (обычный, CRLF, общий на среду), запуск инструмента, разбор отказов, хеши, первое расхождение. Ни один набор больше не заводит своей копии, и `grep` по именам помощников в `test/` не находит ни одной — вместе с копиями ушла и разошедшаяся сигнатура `runCli` (`env` был только в паритете). Осталась одна копия `firstDiff`/`MAX_BUF`/`runCli` в `tools/parity-live.js`: это отдельная команда со своим форматом вывода, а не набор проверок, и сведение их — не чистка тестов | харнесс 73 → 194 строки, `test/` 933 → 929 (кода −25: 662 → 637). На двое **+117 raw / +52 кода** — минуса нет: харнесс вырос на кэш прогонов, общий клон, клон CRLF и разбор отказов, а пять новых файлов — на шапки. Выгода пункта не в объёме, а в том, что копий больше нет | `pnpm test` 39/39 в двух средах; `grep` по именам помощников в `test/` — пусто (одна копия на пакет, а не на набор) |
| R-3.2 | **Не мерить одно и то же несколько раз** — ✅ **сделано 2026-09-14**. Прогоны на чтение идут через `readRun` (ключ: инструмент, каталог, ключи, окружение): одна и та же команда в одном окружении считается один раз на набор. Собирающие прогоны (`--write` и контрольный) не кэшируются намеренно — они зависят от состояния дерева, и кэш для них был бы ложью. Число проверок осталось 39, и имена сверены построчно: 35 совпали дословно, 4 переименованы (тройка замороженной копии и проверка её ревизии), ни одна не пропала и ни одно утверждение не переписано | время, не строки | сумма длительностей прогона **15 074 → 5 328 мс**, при том же числе и составе проверок |
| R-3.3 | **Один клон фикстуры на среду** вместо клона под каждую проверку — ✅ **сделано 2026-09-14**, но не буквально. Клон держится один **на набор и среду** (`sharedClone('plain')`, `sharedClone('crlf')`), а наборы — это отдельные процессы, поэтому клонов на прогон пять, а не один: общий на все наборы требовал бы межпроцессной синхронизации ради экономии, которая ничего не стоит. Проверки, правящие файлы (правка на диске, настройки, обрезанная история), берут свой клон — иначе сосед получил бы чужую правку | время | клонов за прогон, посчитанных по вызовам `git clone` (зиммер в `PATH`): **24 → 21**. Экономия клонов мала, и это надо сказать прямо: время дали не они, а распад тяжёлого файла на пять, которые пошли волной по ядрам — самая длинная часть упала с 14 436 до 3 357 мс, сам паритетный набор с 14 436 до 2 916 |
| R-3.4 | **Тройка замороженной копии демотируется** (её предмет — «эталон снят с той ревизии», а не поведение движка) — идёт вместе с R-1.5 | −60…−80 строк тестов | `pnpm test` 27/27 |
| R-3.5 | **Помощники инструментов — из общей обвязки** — ✅ **сделано частично 2026-09-14** (R-4.9): `tools/parity-freeze.js`, `tools/make-fixture.js` и `tools/pack-check.js` берут у `tools/harness.js` пути (`ROOT`, `SYNTH`, `BUNDLE`, `CONFIG`, `LEGACY`), `MAX_BUF`, `sha256` и закреплённое окружение — три копии `MAX_BUF` и две `sha256` ушли. Осталось у `tools/parity-live.js` — `MAX_BUF`, `parseArgs`, `sha256` и `firstDiff` (у последнего свой формат сообщения): их сведение — отдельный пункт. Зато в том же проходе туда ушла склейка вывода процессов (`collectOutput`): она собирала вывод как строку и падала на разорванном между кусками символе (случай — `WORKLOG.md` §21) | −25…−40 строк в инструментах (уже взято: 3 копии `MAX_BUF`, 2 `sha256`, `collectOutput` — итого −8 строк в `parity-live.js`) | `pnpm test` 53/53, `parity:live` зелёный; `grep` по `on('data')` в пакете находит только `tools/harness.js` |

Приёмка волны 3 — замером, не словом; машина, метод и обе среды — в `WORKLOG.md` §14.

| Прогон | До волны 3 (`923f965`) | После | Цель |
|---|---|---|---|
| `pnpm test` (медиана из трёх) | 15,7 с | **5,6 с** | ≤ 7 с — была достигнута на 39 проверках |
| `pnpm run parity:live` | 23,8 с | **13,3 с** | ≤ 15 с — достигнута |
| сумма длительностей `node --test` | 15 074 мс | 5 328 мс | — |
| число проверок | 39 | 39 | не может уменьшаться |

**Бюджет времени переснят (2026-09-14, R-4.9).** Цель «`pnpm test` ≤ 7 с» бралась на
39 проверках, а к 51-й набор стоит **6,9 с** — запас 0,1 с, то есть первая же
следующая проверка делала её красной молча. Поэтому цель переснята с запасом на
будущую проверку, а не под сегодняшнее число:

| Прогон | Сегодня | Цель | Запас |
|---|---|---|---|
| `pnpm test` (53 проверки) | **6,9 с** (6,94 / 6,97) | **≤ 10 с** | 3,0 с |
| `pnpm test` (66 проверок, R-5.5) | **7,9 с** (7,89 / 7,96 / 7,85) | **≤ 10 с** | 2,1 с |
| `pnpm test` (73 проверки, R-5.6) | **10,4 с** (10,35 / 10,44 / 10,37; 10,48 в среде без настроек) | **≤ 13 с** | 2,6 с |
| `pnpm test` (75 проверок, §B3) | **11,0 с** (10,74 / 10,80 / 11,30; 10,88 без настроек) | **≤ 13 с** | 2,0 с |
| `pnpm test` (77 проверок, §N8) | **11,0 с** (11,00 / 10,86 / 11,02; 11,12 без настроек) | **≤ 13 с** | 1,9 с |
| `pnpm test` (92 проверки, R-4.12) | **14,2–15,7 с** (14,43 / 14,44 / 14,91 / 15,71 / 14,23 / 15,27 без настроек) | **≤ 18 с** | 2,3 с |
| `pnpm test` (97 проверок, R-4.1) | **14,7–16,2 с** — 14,2–15,7 с при 92 плюс **0,46 с** сторож документации (`duration_ms` 464 при отдельном прогоне); число собрано, а не снято: в день замера машину занимали чужие сборки — та же ревизия шла **15,5 с** и **20,0 с** подряд при загрузке 48…193, и сравнивать числа между прогонами нельзя | **≤ 18 с** | 1,8 с |
| `pnpm test` (99 проверок, R-1.4) | **15,2–16,7 с** — 14,7–16,2 с при 97 плюс **0,53 с** сторож границы git (`duration_ms` 530 / 519 при отдельном прогоне); число собрано, а не снято, и по той же причине: на занятой машине та же ревизия шла **19,0–24,4 с** подряд (загрузка 55…100) | **≤ 18 с** | 1,3 с |
| `pnpm test` (107 проверок, `PLAN.md` §5 — `doctor`) | **13,3–13,5 с** (13,46 / 13,49 / 13,24; 13,25 без настроек машины) — восемь новых проверок стоят **0,9–1,0 с**: та же ревизия без `test/doctor.test.js` идёт **12,4 с** (12,44 / 12,36) при 99 проверках. Цель не двигалась, хотя числа ниже прошлых: разница — машина (та же ревизия шла 19,0–24,4 с подряд), вклад файла измерен отдельно | **≤ 18 с** | 4,5 с |
| `pnpm test` (117 проверок, `PLAN.md` §5 — хук) | **24,3–25,7 с** (24,43 / 24,26 / 24,74 / 24,28 / 25,68; 24,87 и 24,57 в среде без настроек машины) — десять проверок хука стоят **+8,5 с**: та же ревизия без `test/hook.test.js` идёт **15,8–16,6 с** при 107 проверках, а сам файл — **17,9–18,5 с** отдельным прогоном (это интеграционные прогоны: клон, коммиты, слияние, отказы; он стал самым долгим файлом набора). Цель поднята один раз и по этой причине, а не под сегодняшнее число | **≤ 28 с** | 2,3 с |
| `pnpm test` (119 проверок — отказы по словам и сторож пина, §N13 / R-4.13) | **25,5–25,7 с** (25,74 / 25,45; 25,45 в среде без настроек машины) — 24,3–25,7 с при 117 плюс **+1,5–2,4 с** за две новые проверки (13 запусков инструмента в `test/cli.test.js` и сторож пина в `test/docs-pin.test.js`): вклад измерен парным прогоном тех же двух наборов с новыми проверками и без них — 4,18 → 6,54 с, а не выведен из разброса прогонов. Тот же набор в другом окне шёл 28,9 и 29,1 с при загрузке 6,7 — числа прогонов на занятой машине несравнимы. Цель не двигалась: измеренное в неё укладывается | **≤ 28 с** | 2,3 с |
| `pnpm test` (120 проверок — разбор аргументов одним владельцем, §R-1.6) | **23,1–23,3 с** (23,09 / 23,25; 22,7–23,3 без настроек машины) при загрузке 3,2…27,5 — числа разных окон несравнимы (тот же набор при 119 проверках шёл 25,5–25,7 с). Новая проверка стоит **1,64 с** сама по себе (`duration_ms` отдельного прогона): тридцать запусков инструмента, где каждое сочетание режимов, несовместимые ключи и одинокие дефисы — по одному процессу на случай. Цель не двигалась: 23,3 с заведомо укладывается в 28 | **≤ 28 с** | 4,7 с |
| `pnpm test` (122 проверки — правило `--json`, причины отказа и разложение сторожа, §R-1.7 / R-4.14 / R-4.15) | **23,2–24,0 с** (23,16 / 24,04; 23,23 без настроек машины) при загрузке 3,4…10,1. Разложение сторожа по четырём файлам времени не прибавило (23,2 с и до него, и после), хотя процессов стало больше: файлы идут параллельно с прочими наборами. Две новые проверки стоят около секунды каждая — обе гоняют инструмент (`test/cli.test.js`: четыре вызова с ответом, четыре режима, три команды без ответа). Цель не двигалась: 24,0 с укладывается в 28 | **≤ 28 с** | 4,0 с |
| `pnpm run parity:live` (две среды) | **8,9–9,3 с** (R-4.11) | ≤ 15 с | 5,7 с |
| `pnpm run pack:check` | **4,1 с** | ≤ 10 с | 5,9 с |
| пересъём обоих эталонов | **12,2 с** | ≤ 30 с | 17,8 с |

Замер — `macOS 15.7.9`, `Node 22.23.2`, `/usr/bin/time -p`, по одному прогону за
раз (`WORKLOG.md` §20). Цель поднята, а не подогнана под сегодняшнее число:
девять новых проверок полноты и объяснения — это тринадцать запусков инструмента
(процесс Node плюс вызовы git на каждый), и без них та же ревизия даёт сегодня
**13,2–13,5 с**; сам файл проверок — самый дорогой в наборе (≈5 с). Разброс на
одном дереве велик (14,2…15,7), поэтому в цели запас от худшего числа, а не от
среднего. Сходимость вперемешку внутри файла была проверена и отвергнута:
выигрыша нет (те же 4,6–5,2 с), а на маленьком раннере девять процессов разом
только добавляют друг другу времени.

## 6. Волна 4 — документация

| # | Пункт | Что делается | Приёмка |
|---|---|---|---|
| R-4.1 | **У обещаний документации есть сторож** — ✅ **сделано 2026-09-14**, и 2026-09-14 же **разложен по обещаниям** (шесть обещаний в одном файле смешались, и пин с ревизией читался среди чужих: стали `test/docs-paths.test.js` — пути и таблица файлов, `test/docs-commands.test.js` — зовы, причины отказа и адреса разделов, `test/docs-numbers.test.js` — числа и цели, `test/docs-pin.test.js` — пин; читатель фактов один — `tools/docs-facts.js`, чтобы четыре файла не разъехались копиями, и ни одна проверка при переносе не потеряна и не ослаблена). Сторож проверяет машинно и только проверяемое: пути из кода-спанов есть в дереве git, в истории фикстуры или в списке чужих (ведёт человек — туда же вносятся новые плановые пути); таблица файлов `README.md` сходится с деревом **в обе стороны** (файл, которого в ней нет, — пробел, которого не заметили); команды и ключи инструкций (`README.md` без разделов о чужом проекте и об отсутствующем, `templates/README.md`) есть в `USAGE` — оба списка берутся из самой справки, а не переписаны в проверку; число проверок в `README.md` и последняя строка бюджета времени в этом файле равны числу объявлений `test(` в наборах (проверка в цикле запрещена — счёт стал бы выводом из кода); ссылки `§…` с именем документа ведут в существующий его раздел. **Чего сторож не берёт:** формулировки и смысл, обещания о будущем, верность описания роли файла, совпадение счёта объявлений с числом проверок в выводе раннера — это остаётся человеку, и это сказано в шапке проверки | +1 файл проверок (сторож: **295** строк в одном файле, после разложения — четыре файла плюс `tools/docs-facts.js`, и разбор адресов разделов — 57 строк из них), 0 строк в движке и инструментах | ✅ сторож заведён красным и нашёл **4 расхождения**, все починены фактически, без правки смысла: (1) таблица файлов не называла `fixtures/live/README.md`, `test/runner.test.js` и сам файл сторожа; (2) `README.md` обещал 92 проверки при 97 — число и бюджет пересняты честно; (3) две ссылки `PLAN.md` вели в разделы, которых в названных документах нет: первая — на подраздел о ручном управлении в документе, где такого подраздела нет вовсе (а в требованиях он есть и точно про «что включил пользователь»), вторая — на подраздел §8 требований, где подразделов нет (поправлено на самый §8 — про уведомления и отсутствие ограничений); Что верен документ, а не код, видно по обеим ссылкам: числа и разделы туда не подгонялись, а восстановлены. Мутация на живом документе роняет именно нужное утверждение (число — проверку чисел, несуществующий ключ — проверку команд, чужой путь — проверку путей, битый адрес — проверку ссылок); на пустом прогоне сторож молчит |
| R-4.14 | **Причины отказа названы — и в коде, и в документации** (аудит: «в таблице кодов сказано меньше, чем бывает») — ✅ **сделано 2026-09-14**. Было: код 2 означал два с лишним десятка разных причин, каждая — литералом на месте отказа, а таблица кодов README называла шесть. Стало: причины перечислены **одним списком** (`CONFIG_CAUSES` в `src/refusal.js`, пять групп: командная строка, настройки и проект, история, хук, измерение), справка печатает их из него, а `refuseCause` не пропускает отказ кодом 2, не назвавший причины (неназванная — дефект инструмента, а не тупик человека). Все 25 прежних отказов переведены на него **без изменения текстов**: для пользователя поведение то же. Таблица кодов в README называет тот же список, и это уже не обещание, а проверка: `test/docs-commands.test.js` сравнивает три стороны (реестр в коде, справка, таблица README) и требует, чтобы каждая причина кем-то выдавалась, а голого `refuse(EXIT.CONFIG` в движке не оставалось | `src/refusal.js` 86 → **124** raw, в остальных модулях +12 (перевод на `refuseCause`), `src` в сумме 4346 → **4396**; `README.md` +8 строк таблицы кодов; в измерении — ни строки | ✅ **красное до правки**: без неё проверка падает «таблица кодов README называет не те причины» и «причины объявлены, но никем не выдаются: нет ответа в JSON, два ответа сразу, нет коммита» — второе сразу нашло настоящую дыру: причина, спрятанная за тернарником, проверкой не читается, и эти два отказа разведены на отдельные вызовы, то есть механизм оказался живым, а не декоративным. Мутации: убрать причину из строки README — падает сверка таблицы; добавить причину в реестр — падает сверка со справкой; вернуть `refuse(EXIT.CONFIG` в `src/cli.js` — падает проверка обхода; `refuseCause('выдуманная причина', …)` бросает «причина отказа не объявлена». 120/120 (до новых проверок) и 122/122 в двух средах, линтер — 0 |
| R-4.15 | **Сторож документации разложен по обещаниям** (аудит: шесть разных обещаний в одном файле, и пин с ревизией читался среди чужих) — ✅ **сделано 2026-09-14**. Стало четыре файла, по обещанию в каждом: `test/docs-paths.test.js` (пути есть в дереве, таблица файлов сходится в обе стороны), `test/docs-commands.test.js` (зовы и ключи из справки, причины отказа, адреса разделов), `test/docs-numbers.test.js` (числа проверок и цели по времени), `test/docs-pin.test.js` (пин установки против справки закреплённой ревизии) — и общий читатель фактов `tools/docs-facts.js`, чтобы файлы не разъехались копиями (разбор зова инструмента и чтение справки из истории жили в двух местах и уже готовы были разойтись). Ни одна проверка не потеряна и не ослаблена: шесть прежних переехали как есть, седьмая — новая (причины отказа, R-4.14) | Сторож был одним файлом на **357** строк, стал четырьмя на **326** плюс читатель на **178** = **504** raw (**+147**): разложение стоит шапок, импортов и общего читателя, а не минуса, и это цена одного дома на обещание — названа прямо, а не округлена | ✅ те же шесть проверок зелёные после переноса, и перенос сразу нашёл настоящую неполноту — таблица файлов README не называла ни новых файлов сторожа, ни `tools/docs-facts.js` (проверка на полноту сработала на живом изменении, а не на мутации). Времени разложение не прибавило: набор 23,2 с и до него, и после (четыре файла идут параллельно с прочими наборами). 122/122 в двух средах, `lint:strict` — 0 |
| R-4.13 | **Пин в примере установки и молчаливое слово в командной строке** (найдено сквозным прогоном; тот же класс, что B1–B3 и N10 — числа верны, а путь ведёт в пустоту) — ✅ **сделано 2026-09-14**. Было: `README.md` §1 ставил пакет на ревизию `d63468d`, которая старше всех подкоманд, которым учат §5–§6, а лишнее слово там не отвергается, а молча пропускается — справка знает только флаги, и `size doctor` в такой установке отвечал нулём, ничего не сделав; хуже отказа именно этим: инструкция, по которой инструмент говорит «всё хорошо» в ответ на то, чего не умеет. Стало — четыре части одного исправления: (1) незнакомый ключ, ключ со значением без значения (`--config`), `--force` без `--init` и слово после режима со значением — отказ кодом 2 с названным виновником и готовой командой, а разбор аргументов идёт **до чтения проекта** (отказ обязан называть то, что набрал человек, а не то, чего в проекте нет); (2) пример установки ведёт на ревизию, где названные команды есть **и лишнее слово уже отвергается** (ревизия с починкой, а не та, что была под рукой — иначе текст сам себе противоречил бы); (3) сторож проверяет именно это: пин ведёт на ревизию **этого** репозитория, и её справка знает названные в тексте команды — справка читается из истории (`git show <pin>:src/refusal.js`), а не из дерева, потому что в дереве она своя; (4) пин в проекте-потребителе поднят на ту же ревизию (там не было ни шаблонов, ни подкоманд). Пятое нашлось, когда пин переставляли руками: **короткий sha pnpm разрешает только через видимые рефы** (`git ls-remote` отдаёт одни верхушки веток) — пока ревизия верхушка, короткий пин ставится, а на следующем коммите установка падает с `Could not resolve … to a commit`; проверено пробой. Поэтому в примере сорок знаков (или ветка/тег), и сторож требует именно этого. Побочно сторож поймал пятое расхождение того же класса: `REFACTOR.md` §0 утверждал «сейчас 77 проверок» при 119 — число убрано из второго места, потому что живое число живёт там, где сверяется (`README.md` и таблица §3) | `src/cli.js` **+26** строк (разбор аргументов и отказы), `test/cli.test.js` **+45**, сторож документации **+35**, `README.md` +9 строк абзаца о пине; в измерении — ни строки | ✅ новые проверки красные до правки (**0 из 2** на клоне `4b8dc8d` с новыми наборами: `--wite`, `--config` без значения, `--force` без `--init` и `--page a b` отвечали нулём). Мутации: старый пин (`d63468d`) — «у ревизии нет справки с разделом «Команды»»; пин на ревизию, которая есть, но хука не знает (`bb08bba`), — «не знает команд: install-hook, uninstall-hook»; выдуманная команда в тексте — «документация зовёт то, чего инструмент не знает: frobnicate». На закреплённой ревизии обещанное делается: `size` 0, `doctor` 0, `explain` 0, `install-hook` ставит оба файла, `uninstall-hook` их снимает (`WORKLOG.md` §35). 119/119 в двух средах, артефакт и страница — те же, `parity:live` 95 × 27, `check:standards` 4 из 4 + 3 из 3, `lint:strict` — 0 |
| R-4.2 | **Инструкция «как подключить инструмент к новому проекту»** — ✅ **сделано 2026-09-14**, раздел `README.md` «Как подключить к своему проекту» (место закреплено, `PLAN.md` §8.4 ссылается сюда). Написана не из планов, а из прогона: шесть шагов (установка → черновик настроек → правка конфига → скрипты и первый отчёт → проверка в CI → коды выхода), таблица ключей конфига, только проверенные коды выхода и отдельный раздел «Ловушки, найденные этой же инструкцией». Эта же инструкция — рецепт для шага 5 | +95 строк README (раздел целиком), из них 4 пункта — дефекты, найденные прогоном (R-4.5…R-4.8) | ✅ пройдена покомандно в свежем репозитории (три коммита, ESM в `src/`, `pnpm`); протокол — `WORKLOG.md` §16 |
| R-4.3 | Числа приёмки в доках приводятся к факту после каждой волны — ✅ **сделано 2026-09-14**, обновляется каждым проходом: `README.md` говорит о 50 проверках и двух средах, как и вывод прогонов; устаревшее «27 проверок, четыре среды» ушло ещё в волнах 1–3 | — | цифры README совпадают с `pnpm test` (50/50) и `parity:live` (2 среды) |
| R-4.4 | `REFACTOR.md` этот — в таблицу файлов `README.md` — ✅ **сделано** | — | ссылка существует |
| R-4.5 | **Найдено прогоном: подкоманд нет.** Инструкция обещала `size init`/`size update`, а CLI знает только флаги: `size init` в свежем проекте падает кодом 2 («нет файла настроек») — текст расходился с реальностью | текст README исправлен на `size --init`; **поведение не менялось** | — | реализация подкоманд — шаг 5 (`PLAN.md` §5), там же `doctor`/`explain` |
| R-4.6 | **Найдено прогоном: модуль в расширении `.js` роняет метрику `min`** — ✅ **сделано 2026-09-14**. Причина: гард компиляции пробовал только скрипт (`vm.Script`), а проект с бандлером пишет `export` в `.js`; прогон вылетал кодом 5 со стеком «стриппер испортил src/greet.js: Unexpected token 'export'» — то есть обвинял стриппер в том, что произошло до него. Починка: **форма определяется по тексту** (`^import|export` и `.mjs`), результат принимается, если разбирается **хотя бы одним** способом — скриптом или модулем; модуль разбирает сам Node через `node --check` по временному файлу `.mjs`. От этого гард не слабеет: настоящая поломка не разберётся ни тем, ни другим. Второй случай, который был тем же дефектом, теперь отказ с командой: если **исходный** текст тоже не разбирается, значит в графе не JavaScript (разметка или типы в `.js`) — код 2, названы причина и `minify.guard`/`minify.ext` | +38 строк в `src/strip.js` (гард вырос на второй разбор и различие «своя поломка / чужая графа»), −2 в `metrics.js`/`refusal.js` | ✅ `test/module.test.js` (5 проверок): оба варианта ESM-проекта (с `type: module` и без) собираются конфигом прямо из `--init` без правок, код 0; мутация «стриппер перестал понимать одинарные кавычки» роняет прогон честным текстом; JSX в `.js` — код 2 с причиной и командой; все пять проверок красные на коде до правки (`git worktree` на `b283122`) |
| R-4.7 | **Найдено прогоном: подсказки цитируют внутренний путь** — ✅ **сделано 2026-09-14**. Было: отказ предлагал `node node_modules/.pnpm/size-report@git+https+++…/node_modules/size-report/bin/size.js --init`, а `--help` печатал `node bin/size.js`. Стало: `invocation()` в `src/refusal.js` различает репозиторий пакета и установку — в чужом проекте подсказка и справка говорят `npx size-report --init` (npx запускает уже установленный пакет и в сеть не идёт) | 0 строк (переписаны 6) | ✅ проверено в наборе `test/module.test.js` (движок, положенный на место установки) и руками: `npx size-report --init` из подсказки создаёт настройки, `--write` по ним собирает таблицу |
| R-4.8 | **Найдено прогоном: мелочи первого запуска.** ✅ **сделано 2026-09-14** в R-4.9: подсказка `--init` больше не обещает «проверки едут вместе с пакетом» (их там нет — есть команда), а `DEFAULT_CONFIG.fixCommand` называет существующее имя пакета (`npx size-report --write`) вместо старого `npx size-table --write`, которое попадало в подпись отчёта у конфига, написанного руками без `fixCommand`. Осталось непринципиальное: `--init` не правит `.gitignore` (этого он и не обещает), а отчёт в потребителе решили оставить в git | подсказки — 0 строк (переписаны 2), `DEFAULT_CONFIG` — 0 строк (переписано значение) | текст отказа совпадает с фактом; `pnpm test` — те же 51 проверка |
| R-4.12 | **Полнота покрытия и объяснение пропущенной строки** (`PLAN.md` шаг 5, требования §4.2) — ✅ **сделано 2026-09-14**. Два ответа, которых не было: «всё ли в истории попало в отчёт» и «почему у этого коммита нет строки». Стало два модуля: `src/check.js` (полнота: объединение изменённых путей всех коммитов делится на три части — отслеживаемые колонками, исключённые (`skip` и файл отчёта) и **незнакомые**; незнакомые — код 1, с путём, коммитом-первопричиной и готовой починкой; тем же ответом идёт сводка по выпавшим коммитам с причинами и их sha) и `src/explain.js` (причина пропуска берётся у того же прохода, что и отчёты, улики — из списка изменённых путей коммита; различает четыре вида: слияние скрыто `rows.merges`, тронут только отчёт, ни одного файла колонок и «числа не сдвинулись»). Команды — словами (`size check`, `size explain <sha|префикс>`, у каждой `--json`), читаются любым аргументом, который не ключ и не его значение; неизвестное слово — код 2, команда вместе с режимом (`check --write`) — отказ. `skip` остаётся одним списком: он же и объявление исключений полноты (второй ключ не заводился). Внутри движка причина пропуска стала ключом — **строки контракта не тронуты** (`--json`, `--data` и артефакт побайтово те же). Проверки — `test/check.test.js` на настоящих коммитах фикстуры | Это возможность, а не чистка, и минуса здесь не обещалось. Счёт — непустые строки всех `src/**/*.js` и `src/**/*.css`: **2988 → 3311** (+323), из них кода **2180 → 2427** (+247), остальное — комментарии. По файлам (строк всего): `src/check.js` 0 → **142**, `src/explain.js` 0 → **98**, `src/cli.js` 265 → 348, `src/history.js` 187 → 202 (ключи причин и `skipLine`), `src/refusal.js` 68 → 75 (справка), `src/data.js` 104 → 105 (строки те же, собираются из ключей), `test/check.test.js` 0 → **168** | ✅ **83 → 92 проверки**: красное до правки доказано клоном `00eee02` с новым файлом — **0 из 9**, все падают на неизвестной команде (код 2); зелёное — 92/92 в двух средах. Случаи — из фикстуры, а не выдуманы: непокрытый путь `README.md` с коммитом `3294a69`, «только отчёт» `cd78fd9`, «числа не сдвинулись» `9fee206` (назван файл колонки `src/code.js`), «мимо колонок» `9dfe679` (журнал колонкой не отслеживается), слияние `9326134` при `rows.merges: false`; починка настроек (путь колонкой или в `skip`) закрывает нарушение и **не двигает числа отчёта** (14 строк в обоих вариантах), а коммит со строкой отвечает номером строки и не выдумывает непокрытых путей. На живой истории потребителя ответ говорит правду о его настройках: `check` — **20 путей мимо колонок** (`icons/*.svg`, `.gitignore`, `CHANGELOG.md`, `tests/golden/…`, сам `size-table.config.json`), 149 коммитов, 95 строк, выпало 54 (49 «только таблица» + 5 «без изменения объёма»), код 1, **1,5 с**. Эталоны не пересняты: `parity:live` — те же 95 × 27, 225 673 Б и sha256 `1bdb27e14bac`; `--json` и артефакт побайтово те же; `lint:strict` — 0, `pack:check`, `check:standards` — зелёные |
| R-4.11 | **Шаблоны для подключаемого проекта** (`PLAN.md` шаг 5) — ✅ **сделано 2026-09-14**. Три файла в `templates/` (10 104 Б): `size-report.config.json` — черновик настроек с колонками-примером (`README.md` и `package.json` есть почти в любом проекте, поэтому первый отчёт собирается сразу), тремя метриками и `fixCommand: npx size-report --write`; `ci.yml` — описание проверки (сборка таблицы и сверка с файлом на диске, два снимка чисел — обычный и в среде без настроек git — и их сравнение, секретов не требует, шаг с deploy key для приватного пакета закомментирован примером); `templates/README.md` — куда что кладётся, что менять и чего в шаблонах нет намеренно. **Блок для `AGENTS.md` не сделан сознательно** — требования такого файла не просят (в разделе 6.3 требуется машиночитаемый вывод, а не файл для агента), выдумывать формат чужого репозитория инструмент не должен; это сказано в `templates/README.md`, а в `README.md` — в «Чего ещё нет». Шаблон под проверкой (`test/templates.test.js`): черновик проходит ту же проверку настроек, которой его встретит первый запуск, и собирает настоящий отчёт; описание проверки разбирается разборщиком подмножества YAML и зовёт только те команды и ключи, что есть в справке; записка обязана называть свои файлы и то, чего в шаблонах нет. Файлы едут в поставке (`files` в `package.json`) — `pack:check` сравнивает каталог шаблонов наравне с `src`: состав и байты каждого файла | +3 файла шаблонов (10 104 Б), +1 файл проверок (`test/templates.test.js`, 14 КБ, из них разборщик YAML ~55 строк); `package.json` — одна строка (`templates` в `files`), `tools/pack-check.js` +14 строк (цикл по двум каталогам и побайтовая сверка шаблонов) | ✅ **приёмка прогоном в свежем репозитории**, а не на словах: `/tmp/fresh-tpl` — `git init`, два коммита, `pnpm add -D ./size-report-0.0.0.tgz`, `cp` обоих шаблонов как есть и ни одной ручной правки: `pnpm exec size --write` → таблица на 3 строки × 2 файла (8 КБ), `pnpm exec size` → **код 0** и сверка с историей, два снимка `--data` (3 314 Б) в обычной среде и с `GIT_CONFIG_GLOBAL=/dev/null` — `diff` пуст, а `npx size-report --write` из `fixCommand` работает без сети (код 0). Три новые проверки красные до правки (клон `9b0d592` с новым файлом: **0/3**, все три падают на отсутствующих шаблонах). Стерегут результат: `pack:check` — список `files` называет только существующее, в тарболле 23 исходника и **3 шаблона**, каждый шаблон в пакете побайтово тот же; `pnpm test` — **83/83** в двух средах; `lint:strict` — 0; артефакт 22 112 Б и страница 59 358 Б, `parity:live` 95 × 27 и 225 673 Б со `sha256 1bdb27e14bac` в двух средах, `check:standards` 4 из 4 + 3 из 3 — **эталоны не переснимались, числа отчёта не менялись** |
| R-4.10 | **Найдено CI: проверка эталонов зависела от версии git, а бандл живой истории — от его догадки** — ✅ **сделано 2026-09-14**. Второй прогон CI (первый закрыт в §9, R-3.5) упал на шаге воспроизводимости эталонов дважды. (1) `check-standards` сравнил `history.bundle` **побайтово**: упаковку пишет git, её байты зависят от версии, и проверка была зелёной на macOS и красной на ubuntu (`README` фикстуры это же и утверждает). Стало: побайтово сверяется только написанное нами (`README.md`, `config.json`, `golden.json`, `artifact.sha256`), у бандла — содержимое (`git bundle list-heads` плюс число коммитов из клона), манифест — по полям, потому что в нём записан хеш бандла. (2) `fixtures/live/history.bundle` лежал без `HEAD`: клон сам выбирал, какую ветку выложить, и на другой версии git печатал `hint: Using 'master' …`, а первая строка этого замечания становилась текстом нашего отказа — настоящая причина оставалась за ней. Стало: бандл пересобран с `HEAD` на ревизии эталона (803 581 Б вместо 791 182 Б, ревизия та же — `bd6ef9d`, 149 коммитов, 95 × 27), `check:standards` требует `HEAD` и ветку `main` у бандла, `execFileSync` в `parity-freeze` больше не дублирует stderr git в наш собственный, а отказ печатается целиком (до 12 строк) | `check-standards.js` 79 → 181 строка (+102), `parity-freeze.js` 205 → 210, `fixtures/live/README.md` 28 → 32, бандл 791 182 → 803 581 Б | ✅ мутации: правка конфига фикстуры роняет 4 утверждения с названной строкой; бандл без `HEAD` — «`history.bundle` больше не заменяет проект»; бандл с другой ревизией — 2 утверждения и настоящий текст причины. Локально весь список CI в том же порядке: `lint:strict` 0, `pnpm test` **53/53** в двух средах, `pack:check` (20 исходников, артефакт 22 112 Б и страница 37 193 Б), `parity:live` 95 × 27 в двух средах, `check:standards` 4 из 4 + 3 из 3 + ветка `main` на `bd6ef9d` |
| R-4.9 | **Обещания пакета сведены к факту** — ✅ **сделано 2026-09-14**. Список поставки называл четыре пути, которых в репозитории нет (`dist/`, `templates/`, `CHANGELOG.md`, `LICENSE`) — убраны, а `pnpm run pack:check` проверяет его с двух сторон: в списке нет несуществующего (мутация «вернуть `dist` в `files`» роняет ровно это утверждение) и в тарболл не попадает необещанное (при этом автоматические включения npm — манифест, README, лицензии — лишними не считаются: иначе проверка ругалась бы на обещанный планом `LICENSE`). Снятие обоих эталонов оживлено: инструментом стала встроенная копия (`fixtures/legacy/size-table.cjs`) вместо удалённого шагом 5 файла в потребителе, ревизия проекта берётся из манифеста, окружение снятия закреплено (`core.quotePath=false` — без него фикстура теряет строку, и эталон молча укорачивается) | — | пересъём побайтово: `data.json`, `config.json`, `artifact.sha256`, `golden.json`, `history.bundle` — те же; в манифестах изменилось только имя инструмента, а в README фикстуры — строка о встроенном инструменте и восстановленная из генератора CRLF-заметка (её правили руками после снятия — расхождение с манифестом теперь стережёт `test/frozen.test.js`) |

## 7. Волна 5 — производительность (движок, затем тесты)

Движок на живой истории уже приемлем: `--page` 107 КБ за **2,9 с**, `--data` 203 КБ,
`--check` зелёный. Поэтому здесь только то, что не меняет поведения, а настоящая
цель — тесты.

| # | Пункт | Приёмка |
|---|---|---|
| R-5.1 | **Один проход по истории на вызов.** Сейчас `--write`, `--page` и `--data` строят историю каждая; внутри одного вызова повторных проходов быть не должно | время каждого режима не выросло; `--page`/`--data` совпадают побайтово с эталоном |
| R-5.2 | **Скорость тестов — ✅ сделано волной 3** (R-3.1–R-3.3, замеры там же): `pnpm test` 15,7 → **5,6 с**, сумма длительностей 15 074 → 5 328 мс, `parity:live` 23,8 → **13,3 с**. ⚠️ **Бюджет переснят 2026-09-14 (R-4.9):** цель ≤ 7 с бралась на 39 проверках, к 51-й набор стоит 6,9 с (запас 0,1 с), поэтому цель — `≤ 10 с` с запасом на следующую проверку; таблица целей — в конце §3 | — |
| R-5.3 | **Среды живой сверки — на одном клоне — снят по факту** (R-3.3): обе среды собирают артефакт (`--write`) в своём дереве, и на общем клоне это была бы гонка за один и тот же файл. Диагноз пункта был неверен, и время взято не экономией клона, а тем, что среды идут вперемешку: 23,8 → 13,3 с при двух клонах, как и было | — |

| R-5.4 | **Цена разбора модуля** (появился с R-4.6) — ✅ **сделано 2026-09-14**. Было: гард разбирал модуль запуском `node --check` на каждую клетку — **97 мс на запуск**, а на синтетической истории с модулями (31 коммит, 3 файла, файл меняется каждым коммитом) `--write` **5,8 с против 0,46 с** у такой же истории со скриптами. Стало: модуль разбирает **один рабочий поток на прогон** (`src/parse.js` + `src/parse-worker.js`): `vm.SourceTextModule` под `execArgv: ['--experimental-vm-modules', '--no-warnings']`, обмен синхронный (`postMessage` + `Atomics.wait` + `receiveMessageOnPort`), поток поднимается лениво — при первом модуле. Замер по 5 прогонов на каждой стороне: модульная история **5,59 → 0,47 с**, скриптовая **0,39 → 0,39 с** (за поток не платит вовсе), проект с одной изменённой клеткой 0,44 → 0,40 с. Цена не убрана, а перенесена: **старт потока ≈ 54 мс раз на прогон** вместо 86 мс на клетку, и сам обмен стоит ~0,085 мс. Три отступления назад к `node --check` (нет файла потока в упаковке, поток не ответил за 2 с, в Node нет модулей vm) | законность ускорения доказывает `test/guard.test.js`: разбор идёт потоком, а не тихим отступлением; оба пути дают **тот же вердикт** на восьми образцах; отступление без файла потока отвечает тем же вердиктом; 300 разборов дешевле одного запуска. Гард жив **после** ускорения: мутация стриппера (перестал понимать одинарные кавычки) роняет прогон на модуле честным текстом «стриппер испортил src/greet.js на 70168e3: SyntaxError: Invalid or unexpected token», после откатa мутации — 0. Артефакт, страница и числа — те же: 50/50 в двух средах, `parity:live` 95 × 27 без пересъёмки, `pack:check` — 20 исходников и новое утверждение «разбор модуля в пакете идёт потоком» |

| R-5.5 | **Настоящее сжатие** (`PLAN.md` шаг 3, срез 1 — **возможность, не чистка**; записана здесь, потому что правит тот же слой: реестр метрик и границу измерения). `min` перестал быть только «снятием балласта»: способ выбирает настройка `minify.engine` — `strip` (умолчание: под ним сняты оба замороженных эталона, и числа потребителя не могли поехать молча) или `esbuild` (настоящее сжатие JS/TS/CSS **необязательной зависимостью**, загружается лениво и синхронно через `createRequire`, потому что замер — синхронный проход; отсутствие зависимости возвращается ответом, а не исключением). Новый слой `src/minify.js` (92 строки) держит таблицу форматов как единственный источник правды и для замера, и для подписи метрики — подпись не может разойтись с тем, что происходит. Описание метрики для читателя появилось в одном месте (`metricView`): у `min` оно зависит от способа, и оба потребителя (`--data` и примечание артефакта) берут его оттуда. Три решения с ценой: (1) `accuracy` говорит про худшее в колонке — один формат без минификатора делает метрику приближённой целиком, а способ называет, где именно; (2) без минификатора прогон **не падает**: числа те же, что у `strip` (побайтово с эталоном), способ говорит «приближение», а код выхода — **4**, иначе приближение уехало бы в CI как успех; (3) файл, который минификатор не разобрал, — отказ кодом 2 с причиной от него самого и двумя выходами (`minify.ext` или `engine: strip`), а не молчаливое упрощение. Черновик `--init` ведёт новые проекты на сжатие. **Цена названа числами:** на фикстуре сжатие меньше упрощения в 44 клетках и ни разу не больше (`src/code.js` 276 → 185 Б, `src/style.css` 55 → 43, по фикстуре −1 372 Б), на живой истории (95 × 27) прогон **1,48 → 1,71 с** — запуск минификатора и разбор тех файлов, которые он берёт; JSON минифицируется разбором и остаётся точным, HTML, JSX/TSX и прочие форматы — упрощение с названной пометкой | **Минуса нет и не обещается — это возможность.** `src` **+233 raw / +144 кода** (2486/1713 → 2719/1857), комментариев +1 строка. По файлам: `src/minify.js` **0 → 86** (новый, 49 кода), `src/metrics.js` 66 → 187 (51 → 135 кода: второй способ и одно место описания), `src/cli.js` 245 → 262, `src/config.js` 103 → 108, `src/strip.js` 223 → 230 (список точных стратегий), `src/render.js` 128 → 131, а `src/data.js` **90 → 84** (сборка дескриптора метрики стала одной строкой); тесты: `test/minify.test.js` **0 → 207** (новый), `test/module.test.js` 166 → 204 (прогон мутации в копии движка и два способа в отказе); `package.json` — `optionalDependencies.esbuild: 0.28.2` (точная версия: от неё зависит число) и строка в `pnpm-lock.yaml` | ✅ 7 новых проверок; **шесть из семи красные на коде до правки** (клон `5680462` с положенным рядом новым модулем, чтобы отличие было в поведении, а не в отсутствующем файле: «минификатор не сократил ни одной ревизии «code.js»»; способ — прежний; ожидался код 4, получен 0; ожидался код 2, получен 0; в черновике нет `minify`; ожидался код 2, получен 0 — седьмая проверяет сам новый модуль, на старом коде его нет вовсе). Отступление проверяется тем же окружением, которым пользуется установка без необязательных зависимостей (`SIZE_REPORT_NO_OPTIONAL`), и числа при нём **побайтово равны эталону**. Артефакт и страница — те же (22 112 Б и 54 772 Б), `parity:live` 95 × 27 в двух средах без пересъёмки, `check:standards` 4 из 4 + 3 из 3, `lint:strict` — 0, `pack:check` — все исходники в тарболле |

| R-5.6 | **Токены** (`PLAN.md` шаг 4, срез 1 — **возможность, не чистка**; записана здесь, потому что правит тот же слой: реестр метрик, необязательные зависимости и черновик настроек). Третье измерение отчёта: `tok` — вес файла для языковой модели. Устройство необязательных зависимостей стало общим (`src/optional.js`, 31 строка): ленивая **синхронная** загрузка через `createRequire` (замер — синхронный проход), версия берётся у самого пакета (в подписи метрики она часть способа, а не подробность `node_modules`), недоступность возвращается ответом, а не исключением, шов отсутствия — окружение `SIZE_REPORT_NO_OPTIONAL`. Из этого `src/minify.js` стал **короче на 22 строки** (86 → 64): загрузчик был отдельным куском того же устройства. Новый `src/tokens.js` (83 строки) держит семейства и кодировки, счёт словарём и оценку по длине, список форматов без текста; в реестре метрик у `tok` свой слот и своё описание для читателя (`tokView`) — способ называет словарь и кодировку, а `accuracy` объявляет число точным или приближённым. Три решения с ценой: (1) **семейство одно — `openai`**: у остальных нет словаря, который можно было бы назвать их собственным, а считать чужим словарём и называть это семейством значило бы обещать то, чего нет (в `PLAN.md` шаг 4 записано три семейства — решение изменено здесь и отмечено там); (2) **кодировка — часть числа**, а не подробность: один и тот же файл даёт 168 токенов в `o200k_base` и 196 в `cl100k_base`, поэтому она выбирается в настройках и называется в подписи, а отказ настроек перечисляет кодировки семейства; (3) **приближение — свойство колонки целиком**: формат без текста (картинка, шрифт, архив) делает метрику приближённой и называет причину, а отчёт без словаря считает оценкой по длине с названным коэффициентом и отдаёт **код 4** — иначе приближение уехало бы в CI как успех. Числа при отступлении те же, что у прежнего отчёта без токенов. **Цена названа числами:** байт на токен отличается по файлам фикстуры в **2,5 раза** (2,56 у `package.json` … 6,30 у `crlf.txt`) — токены не отношения;в браузере страница с `tok` несёт **41 колонку** вместо 31, а выключение метрики
переключателем возвращает 31 и меняет адрес; на живой истории (95 × 27, 1,23 МБ
текста) тот же отчёт идёт **1,56 → 6,32 с** (три прогона: 1,56/1,56/1,55 и
6,42/6,28/6,26), а в «сейчас» **303 705 токенов** — 4,05 Б на токен; словарь читается
**0,3 с на процесс**, из-за чего `pnpm test` **7,3–7,9 → 10,4 с** при 66 → 73
проверках (бюджет переснят в §3) | **Минуса нет и не обещается — это возможность.** `src` **2719 → 2893 raw (+174)**, `test/` **1914 → 2112 (+198)**. По файлам: `src/metrics.js` 187 → 253, `src/optional.js` 0 → 31, `src/tokens.js` 0 → 83, `src/config.js` 108 → 121, `src/cli.js` 262 → 265, `src/minify.js` 86 → **64** (загрузчик ушёл в общий модуль), `test/tokens.test.js` 0 → 195, `test/module.test.js` 204 → 206, `test/minify.test.js` 207 → 208; `package.json` — `optionalDependencies.gpt-tokenizer: 4.0.0` (точная версия: от неё зависит число) и строка в `pnpm-lock.yaml` | ✅ 7 новых проверок; **до правки они невозможны, и это названо, а не спрятано**: на ревизии до прохода движок отвечает отказом кодом 2 («неизвестная метрика «tok» (есть: raw, min, gzip)»), а файл проверок не загружается — в нём нет `src/tokens.js`. Числа: `code.js` фикстуры — 735 Б `raw`, 276 упрощением, 185 сжатием, **168** токенов (196 в `cl100k_base`); набор **73/73** в двух средах; артефакт 22 112 Б и `parity:live` 95 × 27 со `sha256 1bdb27e14bac` — те же, эталоны не переснимались; `lint:strict` — 0, `pack:check` — исходники и страница из тарболла побайтово, `check:standards` 4 из 4 + 3 из 3. Отступление без словаря проверяется окружением `SIZE_REPORT_NO_OPTIONAL` (код 4, подпись называет оценку и её коэффициент) |

Кэш «sha + датчик + версия» (`PLAN.md` §4.4) сюда **не** входит: это возможность,
а не чистка.

## 8. Не в этом плане

Следующая возможность отчёта — чтобы её не смешали с чисткой: темы, категории как часть
панели, пре-собранный `dist/app.js` как артефакт публикации, новые команды
(`init/measure/render/check/doctor/explain`) и их семантика, автообновление по
коммиту, шаблон CI для потребителя и `templates/` (свой CI сделан — шаг 6), блок для файлов агентов, остаток шага 4 (семейства токенов кроме `openai`, переключатель семейства на странице), удаление метрики
`gzip` (`PLAN.md` пункт **D4**), кэш замеров — всё это `PLAN.md` §5, шаги 2–6.

Отдельно, как мелкие правки поведения (не чистка, но и не возможность) — идут
сразу после волны 0 или вместе с ней:

- ✅ **Дерево файлов по папкам** (R-2.4), **память выбора** (R-2.5) и **обмен
  выбором ссылкой** (R-2.6) — сделано 2026-09-14;
- ✅ **Соглашение о цвете дельт** (R-2.2): принято по артефакту — рост зелёный,
  спад красный, двумя строками в `src/table.css`. Разбор решения и цена смены —
  в строке R-2.2;
- ✅ **Вырожденное состояние страницы** (R-2.2): снятые все метрики и снятые все
  файлы объясняются словами, сетка без метрик не строится вовсе;
- категории и их источник в контракте (`categoryBy`) — уже есть, но правила
  категорий пока живут в движке; переносить их в настройки — шаг 2.

## 9. Что уже установлено и куда отнесено

| Установлено аудитом | Пункт |
|---|---|
| Пять обязанностей в одном файле (1669 строк) | R-1.3 ✅ |
| Два рендерера одной таблицы | R-1.1 ✅ |
| Два набора стилей одной таблицы | R-2.2 ✅ |
| Программа страницы текстом в шаблонной строке (не линтуется) | R-2.1 ✅ (осталось `dist/app.js` — §8) |
| Дублированная тестовая обвязка с разошедшимися сигнатурами | R-3.1 ✅ |
| Нет линтера; отсюда склейка двух операторов в строку (1062) | R-1.2 ✅ |
| Поля реестра метрик под `gzip`, которого в v1 не должно быть | §8 (`PLAN.md` D4) |
| Случайная правка рендера, сделанная «заодно» при переносе | R-1.1 (снимается) + R-1.2 (стережёт) |
| Соглашение о цвете дельт расходилось у артефакта и страницы | R-2.2 ✅ |
| Вырожденное состояние страницы (`colspan="0"`, пустая сетка) | R-2.2 ✅ |
| Склейка вывода процессов как строки — живой паритет падал не на расхождении, а на разорванном символе (нашёл CI шага 6) | R-3.5 ✅ |
| Проверка эталонов сравнила байты бандла истории, которые пишет git: красная на чужой версии git (нашёл CI шага 6) | R-4.10 ✅ |
| Бандл живой истории без `HEAD`: клон выбирает ветку догадкой, и замечание git хоронило настоящую причину отказа (нашёл CI шага 6) | R-4.10 ✅ |
| Дублирование прогонов в тестах | R-3.2, R-3.3 ✅ |
| Плоский список файлов вместо дерева (требование §8.2 `module-design.md`) | R-2.4 ✅ |
| Выбор читателя терялся при перезаходе (`module-design.md` §8.4) | R-2.5 ✅ |
| Колонка, чей файл удалён до HEAD, роняет прогон отказом с бессмысленным текстом (нашёл проход по токенам) | `BLOCKERS.md` §B3 ✅ |
| Выбор пути для состояния по порядку настроек, а не по тому, что есть в коммите (граница B3, `diff.renames=false` + псевдонимы) | R-0.7 ✅ (`BLOCKERS.md` §N8 закрыт) |
| Выбор нельзя передать коллеге, а без памяти браузера его нет вовсе (`PLAN.md` §4.8, пункт 6) | R-2.6 ✅ |
| Якорь фикстуры на 1145 строк | R-1.5 |
| Мутация живого дерева в наборе: проверка гарда ломала корневой `src/strip.js`, а соседние наборы идут параллельно — падали чужие проверки (нашлось проходом о сжатии, когда рядом появился ещё один набор) | R-5.5 ✅ (ломается копия движка) |
