/* Разделение набора проверок на быстрый прогон и полный.
 *
 * **Почему делим именно так.** Цена проверки в этом наборе — не объём файла, а
 * сколько раз файл запускает инструмент и git: каждый запуск это процесс Node
 * (десятки миллисекунд), а клон фикстуры и сборка артефакта — сотни. Поэтому в
 * быстром прогоне остаются файлы, которым для доказательства хватает прочитанного:
 * исходников, дерева git, справки, эталонных чисел на общей фикстуре. В полном —
 * те, что гоняют инструмент по многу раз на своих клонах, коммитят и ставят хуки;
 * их причина названа в `SLOW` построчно, чтобы «дорого» не оставалось на слово.
 *
 * **Умолчание — полный прогон.** Быстрым файл становится только здесь, явно, с
 * причиной и измеренной стоимостью. Поэтому новое дорогое не может тихо уехать в
 * быстрый: файл, которого тут нет, идёт в полный, а сторож (`test/suites.test.js`)
 * потребует для него причину — то есть объявление придётся сделать, и оно будет в
 * диффе.
 *
 * **Стоимость — снимок,** как и все числа этого проекта: прогон каждого файла по
 * отдельности (`node --test <файл>`), окно замера названо рядом. Снимок может
 * устареть, поэтому у разделения есть и измеряемая граница: прогон (`tools/run-tests.js`)
 * печатает стоимость каждого файла своим замером и валится, если прогон вышел за
 * бюджет вдвое — так «дорогой в быстром» видно не по объявлению, а по прогону.
 *
 * Перемерить снимок: `pnpm run suites:measure`.
 */

import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

export const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
export const TEST_DIR = path.join(ROOT, 'test');

/* Потолок стоимости для быстрого файла и цели прогонов в секундах (спокойное окно).
 * Цели сверяются с документацией (`test/docs-numbers.test.js`), а не живут здесь
 * второй копией без присмотра.
 *
 * Цель полного прогона поднята 28 → **32 с** проходом об отказах (R-4.18): набор
 * вырос на исполняющую проверку отказов (7 с своим замером), прогон пошёл 26,7–27,3 с
 * в окнах с загрузкой 17–67, то есть запас стал 0,7 с — не запас. Цель берётся с
 * запасом на следующую проверку, а не под сегодняшнее число (как и в R-5.7: без
 * запаса следующая правка валит прогон чужой нагрузкой, а не своей ошибкой). */
export const LIMIT = 4.5;
export const BUDGET = { fast: 10, full: 32 };

/* Окно, в котором снят снимок стоимости: числа разных окон несравнимы. */
export const MEASURED = '2026-09-14, 19:42–19:44 (весь снимок, кроме файлов отказов) и 20:45–20:47 (файлы отказов), загрузка машины 2,6–20';

/* Быстрый прогон: `pnpm test`. Причина — по чему файл сюда попал, стоимость — снимок. */
export const FAST = [
  { file: 'test/api.test.js', cost: 0.15, why: 'публичный API заморожен списком: любая правка движка обязана его не тронуть' },
  { file: 'test/refusals-catalog.test.js', cost: 0.23, why: 'каталог отказов против исходников: читает файлы и считает места отказа, инструмент не запускает ни разу' },
  { file: 'test/runner.test.js', cost: 0.33, why: 'чтение вывода процесса: куски склеиваются буферами — чистая функция, без git и без запусков' },
  { file: 'test/docs-paths.test.js', cost: 0.42, why: 'пути документации против дерева и таблицы файлов: правится в каждом проходе, значит и проверяться должен в каждом' },
  { file: 'test/docs-commands.test.js', cost: 0.28, why: 'зовы, причины отказа и адреса разделов против справки: то же обещание документации' },
  { file: 'test/docs-numbers.test.js', cost: 0.30, why: 'числа проверок и цели по времени против факта: документ стареет молча, если его не читать каждый раз' },
  { file: 'test/docs-pin.test.js', cost: 0.36, why: 'пин установки против справки закреплённой ревизии: путь, по которому ставится пакет' },
  { file: 'test/git-pins.test.js', cost: 0.43, why: 'граница вызова git: читает исходники и историю, инструмент не запускает' },
  { file: 'test/templates.test.js', cost: 0.88, why: 'шаблоны для чужого проекта: черновик настроек и описание проверки целиком — на свежем маленьком репозитории' },
  { file: 'test/guard.test.js', cost: 0.94, why: 'разбор модуля: идёт в рабочем потоке, сотни разборов в одном процессе — запусков нет вовсе' },
  { file: 'test/changelog.test.js', cost: 1.34, why: 'сторож выпуска: версия против манифеста и числа против прогона на фикстуре (два запуска)' },
  { file: 'test/parity.test.js', cost: 2.02, why: 'паритет с эталоном: главное обещание переноса — числа и артефакт байт в байт; прогоны кэшированы и идут на общей фикстуре' },
  { file: 'test/frozen.test.js', cost: 2.86, why: 'замороженная копия: та ли ревизия и воспроизводит ли она эталон — различитель «поехал эталон» и «сломался движок»' },
  { file: 'test/contract.test.js', cost: 4.05, why: 'контракт данных и страница: производные против чисел артефакта, дерево, память выбора, ссылка — считает в памяти, инструмент зовёт дважды' },
  { file: 'test/suites.test.js', cost: 0.10, why: 'сторож самого разделения: читает объявление и исходники, ничего не запускает' }
];

/* Полный прогон: `pnpm test:all`. Всё, чего нет в `FAST`, плюс причина для самых
 * дорогих — почему они здесь, а не в быстром. */
export const SLOW = [
  { file: 'test/cli-paths.test.js', cost: 2.22, why: 'куда инструмент пишет: на каждый случай свой клон и свой `--write` — без записи не проверить' },
  { file: 'test/crlf.test.js', cost: 1.95, why: 'выкладка с CRLF: сверка с рабочим деревом на клоне с чужой настройкой переводов строк' },
  { file: 'test/environment.test.js', cost: 3.52, why: 'герметичность вывода: тот же набор в четырёх чужих окружениях — по клону и прогону на каждое' },
  { file: 'test/check.test.js', cost: 4.12, why: 'полнота и объяснение на коммитах фикстуры: девять проверок, каждая — прогон команды' },
  { file: 'test/minify.test.js', cost: 7.36, why: 'настоящее сжатие: числа на всей истории против упрощения, плюс шов отсутствия минификатора' },
  { file: 'test/tokens.test.js', cost: 6.49, why: 'токены: счёт словарём против оценки на всей истории, кодировка как часть числа' },
  { file: 'test/refusals.test.js', cost: 7.10, why: 'отказы инструмента: тридцать три запуска, по процессу на отказ, плюс свои клоны (чужой хук, обрезанная история, ветка мимо отчёта) — цена здесь в запусках, а не в объёме' },
  { file: 'test/module.test.js', cost: 7.53, why: 'модуль в `.js` и гард разбора: шесть сборок на диске и прогон на каждой — гард доказан мутацией, а не словом' },
  { file: 'test/disk.test.js', cost: 7.44, why: 'сверка с рабочим деревом и три вида потери: по клону на случай, каждый случай — правка на диске' },
  { file: 'test/doctor.test.js', cost: 10.25, why: 'диагностика на пяти состояниях проекта: по прогону инструмента на состояние и на проверку' },
  { file: 'test/cli.test.js', cost: 11.02, why: 'разбор аргументов и отказы: перебор сочетаний режимов, ключей и слов — каждый случай отдельным процессом' },
  { file: 'test/hook.test.js', cost: 22.13, why: 'хук автообновления на свежем клоне: настоящие коммиты, слияние, отказы и снятие — самая дорогая проверка набора' }
];

/* Файлы набора: то, что лежит в `test/` и кончается на `.test.js`. Список берётся с
 * диска, а не из объявления: файл, которого в объявлении нет, обязан быть замечен. */
export function testFiles() {
  return fs.readdirSync(TEST_DIR).filter((f) => /\.test\.js$/.test(f)).sort()
    .map((f) => 'test/' + f);
}

/* Проверки файла: объявление в начале строки — то же правило, по которому числа
 * проверок считает сторож документации. Объявление внутри цикла сделало бы число
 * выводом из кода, и сверять его было бы не с чем. */
export function checksIn(file) {
  let n = 0;
  fs.readFileSync(path.join(ROOT, file), 'utf8').split('\n').forEach((line) => {
    if (/^test\(/.test(line)) n++;
  });
  return n;
}

/* Файлы прогона: быстрый — объявленный список, полный — весь набор с диска (в том
 * числе файл, который ещё не классифицирован: непроверенный файл увидит полный
 * прогон, а не пропуск — сторож найдёт его раньше CI). */
export function filesOf(mode) {
  if (mode === 'fast') return FAST.map((e) => e.file);
  if (mode === 'full') return testFiles();
  throw new Error('неизвестный прогон «' + mode + '» (есть: fast, full)');
}
