/* Каталог отказов: что инструмент говорит, когда отказывается работать.
 *
 * **Зачем он отдельным файлом.** Отказ — это ответ человеку, и ответ обязан быть
 * правдивым: названная причина совпадает с тем, что случилось, а команда починки
 * существует. Прозой это не гарантируется, а живой прогон находил ложную причину
 * четыре раза подряд (B1, B3, разбор аргументов, `explain HEAD`) — и каждый раз
 * случайно. Поэтому здесь по строке на каждый отказ, и проверок у каталога две —
 * на две половины обещания: `test/refusals.test.js` **вызывает** отказ и сверяет,
 * что он сказал обещанное, а `test/refusals-catalog.test.js` читает исходники и
 * требует, чтобы **у каждого места отказа был свой пункт** — карты `SITES` (броски)
 * и `PRINTED` (отказы, напечатанные без исключения). Новое место без пункта —
 * красный прогон: отказ не может появиться без проверки.
 *
 * **Что значит «сказал обещанное».** `must` — фразы, которых в выводе не хватает,
 * если текст стал врать или разучился помогать: причина названа, а виновника нет;
 * команда починки пропала; вместо причины пришла подсказка не о том. Фразы
 * проверяются дословно, поэтому они и есть договор о тексте.
 *
 * `truth` — одна строка на русском: что именно этот отказ обязан донести. Машиной
 * она не проверяется (это смысл, а не подстрока) — она существует, чтобы автор
 * нового отказа не мог написать её, не подумав.
 *
 * **Три способа проверки.**
 *   - `scenario` — отказ вызывается прогоном и сверяется с кодом и фразами: в
 *     клоне фикстуры, в пустом каталоге или в подготовленном сценарии (их список и
 *     устройство — `test/refusals.test.js`).
 *   - `coveredBy` — отказ требует тяжёлой подготовки (свой коммит в клоне), и его
 *     уже стережёт другая проверка: каталог называет файл и фразы, которые та
 *     проверка утверждает. Так «кто это проверяет» не остаётся догадкой, а
 *     исчезнувшая проверка видна — файла или фразы в нём не станет.
 *   - `uncatchable` — отказ нечем вызвать проверкой. Таких в наборе ровно один, и
 *     причина сказана словами: это и есть честный ответ на вопрос, что осталось
 *     на человеке.
 *
 * **Чего каталог не берёт.** Формулировки вне `must` (смысл и тон), полноту
 * объяснения, верность того, что человек видит в `--json`, и знак «!» — это не
 * отказ, а примечание (приближение, смешанный коммит, выключенная автоматика,
 * черновик без колонок), и свой код выхода у него нулевой. Это остаётся на
 * человеке, и об этом сказано в доке проверки, а не подразумевается.
 */

/* Места отказа, которые бросают исключение: причина (`refuseCause`) или код
 * (`refuse(EXIT.…)`), и сколько таких мест в исходниках. Число — не украшение:
 * новое место с уже объявленной причиной меняет его и роняет проверку, а значит
 * автор посмотрит на каталог и допишет пункт. `src/refusal.js` не считается: он и
 * есть механизм отказа, а не место, где инструмент отказывается. */
export const SITES = {
  // командная строка (src/cli.js)
  'незнакомый ключ': 1,
  'повтор ключа': 1,
  'ключ без значения': 1,
  'два режима сразу': 1,
  'несовместимый ключ': 2,
  'лишнее слово': 3,
  'неизвестная команда': 1,
  'команда и режим': 1,
  'нет коммита': 1,
  'нет ответа в JSON': 1,
  'два ответа сразу': 1,
  'конфиг уже есть': 1,
  // настройки и проект (src/config.js)
  'нет git': 1,
  'не git-репозиторий': 1,
  'нет файла настроек': 1,
  'настройки не разобраны': 1,
  'настройки неверны': 1,
  // история (src/explain.js, src/git.js)
  'нет такого коммита': 1,
  'коммит назван неточно': 1,
  'коммит вне истории': 1,
  'EXIT.SHALLOW': 1,
  // хук (src/hook.js)
  'чужой хук': 2,
  'чужой core.hooksPath': 1,
  'нечем звать инструмент': 1,
  // измерение (src/strip.js, src/minify.js)
  'файл не JavaScript': 1,
  'минификатор не разобрал': 1,
  // сверка с деревом (src/history.js) — не причина, а код: это расхождение, а не
  // неверный вызов
  'EXIT.VIOLATION': 2
};

/* Отказы, которые печатаются и возвращают код, а не бросают: их механизм другой
 * (знак «✗» плюс код выхода), и потому их считает своя карта. `src/hook.js` и
 * `src/doctor.js` здесь тоже есть, но не как отказы человека: первый пишет в
 * журнал хука, второй ставит метки в отчёте — они в карте, чтобы новое «✗» в этих
 * файлах не проскочило молча, а не потому, что это отказ. */
export const PRINTED = {
  'src/cli.js': 4,
  'src/check.js': 1,
  'src/doctor.js': 2,
  'src/hook.js': 4
};

/* Заготовки в выводе: подстановки в `args` (пути и имена, которые создаёт сама
 * проверка). Держатся здесь, а не в тесте, чтобы каталог читался как договор. */
export const PLACEHOLDER = '@';

/* По строке на отказ. Порядок — по группам, как в `CONFIG_CAUSES`. */
export const CASES = [
  { key: 'незнакомый ключ', scenario: 'fixture', args: ['--wite'], code: 2,
    must: ['незнакомый ключ «--wite»', '--help'],
    truth: 'названо то слово, которое человек набрал, а не похожее, и есть команда' },
  { key: 'повтор ключа', scenario: 'fixture', args: ['--config', '@config', '--write', '--write'], code: 2,
    must: ['ключ «--write» назван дважды'],
    truth: 'назван именно повторённый ключ' },
  { key: 'ключ без значения', scenario: 'fixture', args: ['--config'], code: 2,
    must: ['у ключа «--config» нет значения', '--config <файл>'],
    truth: 'сказано, что не хватает значения, и как его задать' },
  { key: 'два режима сразу', scenario: 'fixture', args: ['--config', '@config', '--write', '--data'], code: 2,
    must: ['два режима сразу: «--write» и «--data»', 'починка: '],
    truth: 'названы оба режима, потому что выбор из них — выбор человека' },
  { key: 'несовместимый ключ', scenario: 'fixture', args: ['--config', '@config', '--force'], code: 2,
    must: ['«--force» работает только с «--init»', 'починка: '],
    truth: 'сказано, с чем ключ работает, и это верно' },
  { key: 'несовместимый ключ', scenario: 'fixture', args: ['--init', '@draft', '--config', '@config'], code: 2,
    must: ['у «--init» свой файл', '«--config» называет настройки проекта'],
    truth: 'объяснена разница между двумя файлами, а не просто «нельзя»' },
  { key: 'лишнее слово', scenario: 'fixture', args: ['--config', '@config', '--page', 'out.html', 'extra'], code: 2,
    must: ['лишнее слово «extra»', '«--page» принимает одно значение'],
    truth: 'лишним названо слово человека, а не значение режима' },
  { key: 'лишнее слово', scenario: 'fixture', args: ['--config', '@config', 'check', 'extra'], code: 2,
    must: ['команда «check» аргументов не принимает', '«extra» лишний'],
    truth: 'виновато лишнее слово, и оно названо' },
  { key: 'лишнее слово', scenario: 'fixture', args: ['--config', '@config', 'explain', 'HEAD', 'HEAD~1'], code: 2,
    must: ['«explain» принимает один коммит', 'починка: '],
    truth: 'сказано, что коммит один, а не «слишком много слов»' },
  { key: 'неизвестная команда', scenario: 'fixture', args: ['sizes'], code: 2,
    must: ['неизвестная команда «sizes»', '--help'],
    truth: 'слово названо командой не зря: человек звал именно команду, и ему нужен их список' },
  { key: 'команда и режим', scenario: 'fixture', args: ['--config', '@config', 'check', '--write'], code: 2,
    must: ['команда «check» и режим «--write»', 'починка: '],
    truth: 'названы оба виновника: человек считает их одним и тем же, а они разное' },
  { key: 'нет коммита', scenario: 'fixture', args: ['--config', '@config', 'explain'], code: 2,
    must: ['команде «explain» нужен коммит', 'имя ревизии'],
    truth: 'сказано, чем коммит можно назвать, а не только «нужен коммит»' },
  { key: 'нет ответа в JSON', scenario: 'fixture', args: ['--config', '@config', 'install-hook', '--json'], code: 2,
    must: ['у команды «install-hook» нет ответа в JSON', 'починка: '],
    truth: 'сказано, что ответа нет именно у этой команды' },
  { key: 'два ответа сразу', scenario: 'fixture', args: ['--config', '@config', '--write', '--json'], code: 2,
    must: ['«--json» и режим «--write»'],
    truth: 'сказано, что «--json» — форма ответа, а режим — то же самое своими словами' },

  { key: 'нет файла настроек', scenario: 'fixture', args: ['--config', '@missing'], code: 2,
    must: ['нет файла настроек', 'создайте его: ', '--init'],
    truth: 'назван путь, по которому искали, и команда, которая файл создаст' },
  { key: 'настройки не разобраны', scenario: 'fixture', args: ['--config', '@broken'], code: 2,
    must: ['не разобран', 'починка: правьте'],
    truth: 'причина от синтаксиса JSON, а не «что-то не так с настройками»' },
  { key: 'настройки неверны', scenario: 'fixture', args: ['--config', '@empty'], code: 2,
    must: ['не задано ни одной колонки', 'починка: правьте'],
    truth: 'названо, какой именно ключ неверен, а не «настройки плохие»' },
  { key: 'настройки неверны', scenario: 'fixture', args: ['--config', '@badtype'], code: 2,
    must: ['должна быть {label, paths:', 'починка: '],
    truth: 'путь, который не имя файла, — отказ, а не колонка с нулём строк за успех' },
  { key: 'не git-репозиторий', scenario: 'barren', args: [], code: 2,
    must: ['git не видит здесь репозитория', 'сейчас смотрю в ', 'git init'],
    truth: 'сказано, куда смотрел инструмент и что делать, если истории ещё нет' },
  { key: 'нет git', scenario: 'no-git', args: [], code: 2, env: { PATH: '/nonexistent' },
    must: ['git не запустился: его нет в PATH', 'поставьте git'],
    truth: 'два разных тупика — «git не нашёлся» и «репозитория нет» — названы врозь, а не одним текстом с оговоркой «или»' },
  { key: 'конфиг уже есть', scenario: 'draft-twice', args: ['--init', '@draft'], code: 2,
    must: ['конфиг уже есть', '--init --force'],
    truth: 'сказано и как править, и как перезаписать — потому что решает человек' },

  { key: 'нет такого коммита', scenario: 'fixture', args: ['--config', '@config', 'explain', 'maser'], code: 2,
    must: ['не имя ревизии и не начало sha', 'git log'],
    truth: 'отказ говорит про имя, а не про коммит: коммита тут никто не терял' },
  { key: 'коммит назван неточно', scenario: 'fixture', args: ['--config', '@config', 'explain', '9'], code: 2,
    must: ['неоднозначен', 'починка: назовите больше знаков'],
    truth: 'показаны подходящие коммиты: выбрать из них может только человек' },
  { key: 'коммит вне истории', scenario: 'side-branch', args: ['--config', '@config', 'explain', 'side'], code: 2,
    must: ['нет в истории отчёта', 'починка: '],
    truth: 'сказано, что коммит есть и он в другой ветке, а не «такого коммита нет»' },
  { key: 'EXIT.SHALLOW', scenario: 'shallow', args: ['--config', '@config', '--write'], code: 3,
    must: ['история обрезана (shallow clone)', 'git fetch --unshallow', 'fetch-depth: 0'],
    truth: 'названы обе починки: для себя и для CI' },

  { key: 'чужой хук', scenario: 'foreign-hook', args: ['--config', '@config', 'install-hook'], code: 2,
    must: ['уже есть и поставлен не этим инструментом', 'перезаписи чужого файла нет намеренно'],
    truth: 'объяснено, почему инструмент не перезаписывает, и что сделать вместо этого' },
  { key: 'чужой хук', scenario: 'foreign-hook', args: ['--config', '@config', 'uninstall-hook'], code: 2,
    must: ['поставлен не этим инструментом — не трогаю', 'починка: '],
    truth: 'сказано, что снятие тоже не трогает чужое' },
  { key: 'чужой core.hooksPath', scenario: 'hooks-path', args: ['--config', '@config', 'install-hook'], code: 2,
    must: ['задан core.hooksPath', 'починка: '],
    truth: 'назван каталог из настроек и причина, по которой в него не лезут' },
  { key: 'нечем звать инструмент', scenario: 'src-copy', args: ['--config', '@config', 'install-hook'], code: 2,
    must: ['не нашлось чем звать инструмент', 'поставьте пакет зависимостью'],
    truth: 'сказано, что хук ставить нечем (а не «хук будет молчать»: его ещё нет)' },

  /* Гард разбора и минификатор требуют своего коммита в клоне — их стережёт
   * `test/module.test.js`, где тот же случай доведён до конца. */
  { key: 'файл не JavaScript', coveredBy: 'test/module.test.js',
    must: ['не JavaScript'],
    truth: 'сказано, что дело в исходном тексте, а не в стриптере, и что правится' },
  { key: 'минификатор не разобрал', coveredBy: 'test/module.test.js',
    must: ['esbuild не разобрал'],
    truth: 'назван и файл, и минификатор, и оба выхода из тупика' },

  /* Сверка с деревом требует потерянной правки — её стережёт `test/disk.test.js`. */
  { key: 'EXIT.VIOLATION', coveredBy: 'test/disk.test.js',
    must: ['перенос состояния между коммитами пропустил правку'],
    truth: 'сказано, что потерялась правка при переносе, и как починить' },
  { key: 'EXIT.VIOLATION', coveredBy: 'test/disk.test.js',
    must: ['правка есть только на диске'],
    truth: 'сказано, что правка не потерялась, а не закоммичена, и как её вернуть' },

  /* Отказы, которые печатаются и возвращают код (карта `PRINTED`). */
  { id: 'нет файла таблицы', scenario: 'fixture', args: ['--config', '@notable'], code: 1,
    must: ['таблица размеров: нет файла docs/nope.html', 'соберите её: '],
    truth: 'назван файл, который не найден, и команда, которая его соберёт' },
  { id: 'таблица разошлась с историей', scenario: 'drift', args: ['--config', '@config'], code: 1,
    must: ['расходится с историей git', 'починка: ', 'закоммитить'],
    truth: 'сказано, где именно расходится, и что после сборки нужен отдельный коммит' },
  { id: 'покрытие неполно', scenario: 'fixture', args: ['--config', '@few', 'check'], code: 1,
    must: ['покрытие:', 'починка: допишите эти пути колонкой или в «skip»'],
    truth: 'названы пути и коммиты, которые их завели, и готовое действие' },
  { id: 'приближение вместо точного счёта', scenario: 'fixture', args: ['--config', '@sensor', '--write'], code: 4,
    env: { SIZE_REPORT_NO_OPTIONAL: '1' },
    must: ['метрика «min» считает упрощением', 'метрика «tok» считает оценкой', 'починка: '],
    truth: 'приближение названо приближением и не уезжает как успех' },

  { id: 'внутренняя ошибка', uncatchable: 'чтобы уронить инструмент, нужно испортить его самого;'
    + ' проверка, держащая в наборе заведомо сломанный движок, стерегла бы своё, а не отказ'
  }
];
